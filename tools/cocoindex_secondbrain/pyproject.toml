[project]
name = "yuto-cocoindex-secondbrain"
version = "0.1.0"
description = "Sandbox CocoIndex derived index for Yuto Markdown second brain. Markdown remains source of truth."
requires-python = ">=3.11"
dependencies = [
  "cocoindex>=1.0.3",
]

[tool.uv]
package = false
