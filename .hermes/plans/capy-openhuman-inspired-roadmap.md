# Capy OpenHuman-Inspired Roadmap

> **For Hermes:** Use this as the canonical strategy file for OpenHuman-inspired Capy/Hermes/Spaces work. Execute it with `subagent-driven-development` task-by-task, with strict TDD for code changes and visual/browser QA for UI-visible slices.

**Decision:** Adopt selected OpenHuman product/architecture ideas clean-room. Do **not** pivot to OpenHuman, do **not** rewrite Hermes/Capy as Rust/Tauri, and do **not** copy GPLv3 OpenHuman code, schemas, tests, comments, or fixtures.

**Goal:** Extend Hermes + Capy Spaces with a local context operating layer: Memory Tree, auto-fetch/freshness, output compaction, source provenance, autonomy/security policy visibility, model-routing hints, and structured progress events.

**Architecture:** Hermes remains the persistent autonomous gateway/tool/cron/subagent layer. Capy Spaces remains the safe metadata-only production workspace/canvas with recovery, revision history, and visual QA. The new context layer is additive: it ingests trusted summaries of local/remote work into local storage and exposes bounded, redacted, cited context back to Spaces and Hermes.

**Primary evidence:** `/tmp/openhuman-review.md` and `.hermes/plans/2026-05-17_123717-openhuman-inspired-capy-roadmap.md` summarize the OpenHuman review at commit `f9de38d6f9bc252501ef79f772b96aedf3926a4d`. Treat those files as evidence and design input, not implementation source.

**Latest competitive audit:** `.hermes/plans/2026-05-24_123241-capy-spaces-openhuman-space-agent-feature-audit.md` refreshes the OpenHuman + Space Agent feature matrix using current repo/docs/release evidence and should guide sprint ordering after the active widget-mutation safety receipt slice lands.

---

## Non-negotiables

1. **Clean-room only:** No copying from GPLv3 OpenHuman source, tests, schemas, prompts, or comments.
2. **No platform pivot:** Hermes/Capy stays Python/server/WebUI/Telegram/local-first, not Rust/Tauri desktop-first.
3. **Spaces safety stays primary:** Generated widget execution remains disabled by default. Metadata-only rendering, sandbox preview, visual QA, approval gates, and revision rollback remain required.
4. **Memory is untrusted context:** Retrieved or auto-fetched memory can inform the agent, but cannot bypass prompt-injection checks, creator-loop gates, sandbox checks, approval gates, or recovery quarantine.
5. **Local-first storage:** User/private source records and memory-tree vault content live in local state paths, not Git-tracked repo directories.
6. **Visible trust controls:** Autonomy mode, freshness, provenance, model-routing hints, prompt-injection preflight status, and progress events should be visible in the product UI where relevant.

---

## Phase roadmap summary

### Phase 0 — Plan hygiene and architecture spec

Status: **complete**.

Delivered:
- This canonical roadmap file exists and is referenced by scheduled Capy Spaces sprints.
- `capy-spaces-space-agent-parity.md` includes the OpenHuman-inspired expansion track.
- `capy-spaces-video-demo-parity-checklist.md` includes visible context-layer demo criteria.
- The appendix below inventories reusable components and current gaps.

### Phase 1 — Capy Memory Tree design/MVP

Status: **implemented as a local-first MVP; continue hardening advisory/context enforcement**.

Delivered:
- `docs/capy-memory-tree.md` defines the clean-room schema, storage, provenance, redaction, and TDD plan.
- `api/capy_memory.py` implements deterministic source/chunk canonicalization, SQLite tables, redacted Markdown vault writes, status/search APIs, relevant-memory lookup, and metadata-only source refresh-job registration.
- `tests/test_capy_memory_tree.py` covers hostile Space manifests, revision events, widget events, visual-QA reports, storage/search/status routes, source refresh registration, deterministic IDs, bounded traversal, and leak prevention.
- Live Spaces boundaries now auto-ingest metadata-only Space manifests/revision events, widget events, repair events, rollback/restore revision anchors, and creator visual-QA reports into the Memory Tree. Local knowledge sources are registered as source records with provenance/freshness metadata rather than copied into prompts.

Remaining:
- Keep all memory as advisory untrusted context that cannot bypass creator, approval, recovery, or prompt-injection gates.

### Phase 2 — TokenJuice-style output compaction

Status: **backend helper and product-visible metadata receipts implemented for the first creator/context/demo/progress boundaries plus receipt-only browser/development adapters; broader high-volume tool/subagent/recovery integration remains**.

Delivered:
- `api/capy_compaction.py` implements `compact_output(...)` with bounded receipts, unsafe-marker redaction, path collapsing, repeated-line dedupe, approval-prompt preservation, error-block preservation, invalid-cap rejection, and `rules_applied` metadata.
- `tests/test_capy_output_compaction.py` verifies size accounting, error/approval preservation, redaction status, cap enforcement, and unsafe-marker omission.
- `space_demo_run_all()` now emits a metadata-only `output_compaction` receipt built from allow-listed demo-suite summary lines, so the existing Spaces UI can show original/compacted character counts, redaction status, and allow-listed rules without rendering raw output, prompts, widget bodies, or credentials.
- Individual demo smokes, direct `/api/spaces/update` active-instruction safety receipts, creator preview/commit receipts, active-space context receipts, Research Harness progress-update and artifact-completion receipts, scoped progress status receipts, manual and scheduled Memory Tree source-refresh receipts, receipt-only Browser Surface tool actions, receipt-only development terminal/shell tool actions, approved camera-stream ingestion, queued widget events, shared data slot set/read/list/delete tool responses, Space checkpoint anchors, revision/history list responses, source-style Space create/duplicate/delete tool responses, source-style Space metadata/layout/repair/rearrange/reposition helper responses, source-style widget upsert helper responses, source-style widget definition/blueprint preview/source/render helper responses, source-style/current widget patch/delete/bulk-delete/toggle helper responses, generic `widget.patch` tool-route responses, legacy generic `widget.list`/`widget.read`/`widget.get` tool-route responses, source-style/current widget read/list/see helper responses, source-style Space read/get helper responses (`space.get`, `space.spaces.get`, `space.spaces.read`, `space.spaces.getSpace`, `space.spaces.readSpace`), source-style Space collection/current read helper responses, widget-runtime contract detail-panel receipt displays, source-style Space open/reload navigation helper responses, logical app URL helper responses, logical storage path-helper responses, Big Bang template resets, whole-Space recovery toggles, whole-Space repair-event list responses, recovery-module repair-event list responses, widget recovery toggles, recovery-module quarantine/toggles, and trusted session recovery repair-safe responses now reuse the same product-visible compaction evidence pattern without rendering raw URLs, terminal commands, logical paths, prompts, markdown bodies, generated bodies, renderer/source/API-auth fields, scripts, credentials, module bodies, camera hosts, ports, query tokens, runtime widget prompts, exception text, raw slot values/metadata, ignored source-style widget/layout/viewport/browser-navigation/path-helper payloads, raw widget request/list/detail payloads, raw Space descriptions/instructions, checkpoint/recovery reasons, source-refresh origin URIs/fetched content/job internals, or local session/audit paths.

- Native `upsert_widget(..., include_safety_receipts=True)` direct helper responses now include metadata-only `output_compaction` evidence beside prompt-preflight, autonomy-policy, and progress receipts, without leaking renderer/source/API-auth/script/secret fixture fields.

Remaining:
- Extend compaction receipts to remaining long external tool/subagent/recovery-output boundaries where they add product value.
- Preserve safety-relevant prompts, failures, approval prompts, and artifact handles/citations when broader execution paths adopt compaction.

### Phase 3 — Auto-fetch source registry and freshness

Status: **source registry/status UI, local-knowledge bridge, source-refresh job queueing, safe metadata-only refresh worker, and product-home scheduled refresh tick trigger implemented; broader cron/daemon automation and source-specific fetcher breadth remain**.

Delivered:
- `api/capy_memory.py` registers source references idempotently, queues metadata-only `source.refresh` jobs, strips credential/query/fragment markers from public `origin_uri`, requeues terminal jobs, and preserves active leased payloads.
- `run_source_refresh_jobs(...)` leases queued source-refresh jobs, enforces allow-listed HTTP(S) origins, ingests safe HTML/plain/Markdown, RSS/Atom, JSON Feed, and allow-listed GitHub issue/PR/repository/readme/contents/release/latest-release/release-list/release-assets/branch/branch-list/tag/labels/topics/workflow/workflow-list, workflow-run, workflow-runs-list, workflow-jobs, workflow-artifacts, repository-artifacts, Actions private-name, environment private-name, environment variables, pages, check-runs, commit-statuses, issue-list, issue-event-list, PR-list, PR requested-reviewers-list, PR commit-list, PR review-list, PR file-list, issue/PR comments, contributors, stargazers, subscribers/watchers, fork-list, commit, commit-list, participation stats, traffic views, traffic clones, environments, rulesets, Dependabot-alerts, collaborators, and community-profile API metadata, runs `auto_fetched_source` prompt preflight, persists only sanitized advisory summaries, and emits metadata-only `memory.ingest.*` progress events; the manual Memory refresh trigger and product-home scheduled tick trigger now return metadata-only action-policy receipts with destructive-external-action approval and `hint:summarize` routing evidence.
- GitHub Actions repository private-name source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/actions/secrets` API payloads, reconstructing summaries only from the safe repository path, count, bounded safe names, and safe created/updated timestamps while ignoring raw values, row/global URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; text fallback payloads and lookalike-host route-shaped URLs fail closed before persistence/search output.
- GitHub environment private-name source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/environments/{environment_name}/secrets` API payloads, reconstructing summaries only from the safe repository path, environment name, count, bounded safe names, and safe created/updated timestamps while ignoring raw values, row/global URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; text fallback payloads, lookalike-host route-shaped URLs, encoded path tricks, and malformed environment-secret routes fail closed before persistence/search output.
- GitHub environment variables source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/environments/{environment_name}/variables` API payloads, reconstructing summaries only from the safe repository path, environment name, variable count, bounded variable names, and safe created/updated timestamps while ignoring raw values, row/global URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed bypass payloads and lookalike-host route-shaped URLs fail closed before persistence/search output.
- GitHub Dependabot alerts source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/dependabot/alerts` API payloads, reconstructing summaries only from the safe repository path, alert count, and a bounded prefix of alert numbers/states/package ecosystems/package names/manifest paths/severities while ignoring advisory summaries/descriptions, CVE/GHSA ids, vulnerable/patched version ranges, dismissed comments/reasons, raw URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed bypass payloads, unsafe tail rows, legacy queued userinfo payloads, non-JSON fallback content, and malformed Dependabot-alert routes fail closed.
- GitHub repository events source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/events` API payloads, reconstructing summaries only from the safe repository path, event count, type counts, and a bounded prefix of event ids/types/actors/public flags/timestamps while ignoring event payloads, commit messages, repo/actor URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed bypass payloads, unexpected raw/non-schema row keys, malformed rows, malformed event-shaped paths, unsafe hosts, legacy queued userinfo payloads, userinfo/HTTP/explicit-port authorities, cross-repo redirects, non-JSON fallback content, and secret-looking fixture values fail closed before persistence/search output.
- GitHub repository environments source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/environments` API payloads, reconstructing summaries only from the safe repository path, total environment count, bounded environment ids/names, and safe created/updated timestamps while ignoring raw URLs, protection-rule/branch-policy payloads, descriptions, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed-looking bypass payloads and malformed tail rows fail closed.
- GitHub repository community-profile source-refresh metadata now has a narrow JSON-only parser for exact HTTPS `/repos/{owner}/{repo}/community/profile` API payloads, reconstructing summaries only from the safe repository path, health percentage, content-report flag, update timestamp, and bounded allow-listed file name/path metadata while ignoring raw URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, descriptions, bodies, and secret-like markers; JSON Feed-looking payloads, invalid health percentages, unsafe file fields, non-JSON fallback content, insecure HTTP origins, and malformed/encoded/case-mismatched routes fail closed.
- GitHub repository traffic-clones source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/traffic/clones` API payloads, reconstructing summaries only from the safe repository path, total clone count, unique cloner count, bounded timestamped samples, and per-sample counts while ignoring raw URLs, API-auth fields, prompt/summary/body fields, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed-looking bypass payloads and malformed tail rows fail closed.
- GitHub repository traffic-views source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/traffic/views` API payloads, reconstructing summaries only from the safe repository path, total view count, unique visitor count, bounded timestamped samples, and per-sample counts while ignoring raw URLs, API-auth fields, prompt/summary/body fields, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed-looking bypass payloads and malformed tail rows fail closed.
- GitHub repository source-refresh metadata now has its own narrow allow-listed parser for `/repos/{owner}/{repo}` API payloads, reconstructing summaries only from repository name, description or safe fallback, default branch, visibility/private/archived status, counts, topics, and timestamps while ignoring raw body/URL/clone/homepage/auth fields and query/fragment markers.
- GitHub README source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/readme` API payloads, reconstructing summaries only from safe repository path, README name/path, file size, and SHA prefix while ignoring raw README content, encoding, download/html/git URLs, link maps, API-auth fields, prompts, scripts, renderer/source fields, and secret-like markers; JSON Feed/content-type bypass payloads, uppercase hosts, README-like suffix paths, and malformed payloads fail closed.
- GitHub repository contents source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/contents` and `/repos/{owner}/{repo}/contents/{path...}` API payloads, reconstructing summaries only from safe repository path, content path, item count, bounded item type/name/path rows, file size, and SHA prefixes while ignoring raw/encoded file content, download/html/git/API URLs, link maps, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed/text fallback payloads, explicit-port authorities, percent-encoded route tricks, case-mismatched routes, and malformed tail rows fail closed.
- GitHub latest-release source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/releases/latest` API payloads, reconstructing summaries only from the safe repository path, release id, release name/tag, draft/prerelease flags, and publish timestamp while ignoring release bodies, assets, URLs, API-auth fields, scripts, prompts, renderer fields, and secret-like markers; JSON Feed-looking bypass payloads at the same endpoint fail closed.
- GitHub release-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/releases` API payloads, reconstructing summaries only from the safe repository path, release count, bounded release names/tags, draft/prerelease flags, and publish timestamps while ignoring release bodies, assets, archive URLs, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub release-assets source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/releases/{release_id}/assets` API payloads, reconstructing summaries only from the safe repository path, release id, asset count, bounded asset names, ids, sizes, download counts, states, content types, and timestamps while ignoring download URLs, uploader objects, API-auth fields, raw prompts, scripts, renderer/source fields, and secret-like markers; JSON Feed/text fallback bypass payloads at the same endpoint plus encoded/case-mismatched release-assets-shaped path variants fail closed.
- GitHub branch source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/branches/{branch}` API payloads, reconstructing summaries only from the branch name, protected flag, and commit SHA prefix while ignoring commit URLs, protection details, bodies, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub branch-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/branches` API payloads, reconstructing summaries only from the safe repository path, branch count, bounded branch names, protected flags, and commit SHA prefixes while ignoring branch protection details, URLs, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub Actions workflow source-refresh metadata now has narrow allow-listed parsers for `/repos/{owner}/{repo}/actions/workflows/{workflow_id}` and `/repos/{owner}/{repo}/actions/workflows` API payloads, reconstructing summaries only from workflow id/list count, names, states, and timestamps while ignoring workflow paths/YAML/jobs, URLs, body fields, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub Actions workflow-run source-refresh metadata now has narrow allow-listed parsers for `/repos/{owner}/{repo}/actions/runs/{run_id}` and `/repos/{owner}/{repo}/actions/runs` API payloads, reconstructing summaries only from run id/list count, run names, statuses, conclusions, events, run numbers/attempts, branches, SHA prefixes, and timestamps while ignoring logs/jobs/html URLs, commit payloads, job payloads, API-auth fields, scripts, prompts, and secret-like markers; JSON Feed-looking bypass payloads and malformed rows at the list endpoint fail closed.
- GitHub Actions workflow-jobs source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/actions/runs/{run_id}/jobs` API payloads, reconstructing summaries only from run id, total count, and a bounded prefix of safe job names/status/conclusion/timestamps while ignoring job steps, runner labels/names, logs/html URLs, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub repository Actions artifacts source-refresh metadata now has a narrow JSON-only parser for `/repos/{owner}/{repo}/actions/artifacts` API payloads, reconstructing summaries only from the safe repository path, artifact count, and bounded artifact ids/names/sizes/expired flags/safe timestamps while ignoring archive/download/API/html URLs, workflow-run commit payloads, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed-looking bypass payloads, unsafe userinfo authorities, and non-JSON fallback content fail closed.
- GitHub Pages source-refresh metadata now has a narrow JSON-only parser for `/repos/{owner}/{repo}/pages` API payloads, reconstructing summaries only from the safe repository path, Pages status, build type, public/custom-404/HTTPS booleans, safe CNAME, and protected-domain state while ignoring raw URLs, certificate descriptions/domains, source branch/path objects, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed bypass payloads, malformed Pages-shaped paths, legacy queued userinfo payloads, and non-JSON fallback content fail closed.
- GitHub check-runs source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/commits/{sha}/check-runs` API payloads, reconstructing summaries only from the safe repository path, commit SHA prefix, check-run count, bounded check-run ids/names/statuses/conclusions, and timestamps while ignoring output summaries, details/html URLs, pull-request rows, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub commit-status source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/commits/{sha}/statuses` API payloads, reconstructing summaries only from the safe repository path, commit SHA prefix, status count, state counts, bounded status ids/contexts/creator logins, and timestamps while ignoring status descriptions, target URLs, row URLs, avatar URLs, API-auth fields, scripts, prompts, renderer/source fields, and secret-like markers.
- GitHub deployment-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/deployments` API payloads, reconstructing summaries only from the safe repository path, deployment count, bounded deployment ids/environments/refs/SHA prefixes/tasks/production-transient flags, and timestamps while ignoring deployment descriptions, payloads, status URLs, creator URLs, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub deployment-status source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/deployments/{deployment_id}/statuses` API payloads, reconstructing summaries only from the safe repository path, deployment id, status count, state counts, bounded status ids/states/environments/creator logins, and timestamps while ignoring descriptions, target/log/deployment/repository/environment URLs, payloads, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub tag source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/tags` API payloads, reconstructing summaries only from bounded tag names, tag count, and commit SHA prefixes while ignoring archive URLs, raw URLs, tag bodies, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub label-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/labels` API payloads, reconstructing summaries only from a non-URL labels origin label, safe repository path, label count, and bounded safe label names/colors/default flags while ignoring descriptions, row URLs, ids/node_ids, and failing closed on URL-like label names, non-boolean default values, raw/body/code/content/API-auth fields, scripts, prompts, and secret-like markers.
- GitHub repository-languages source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/languages` API payloads, reconstructing summaries only from the safe repository path, language count, total byte count, and a bounded top-language list while ignoring generic JSON feed fields, query/fragment markers, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub repository-topics source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/topics` API payloads, reconstructing summaries only from the safe repository path, topic count, and bounded topic names while ignoring raw bodies, JSON Feed-looking fields, query/fragment markers, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub commit source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/commits/{sha}` API payloads, reconstructing summaries only from the path-matched commit SHA prefix, first safe commit-message line, safe author/committer dates, parent count, changed-file count, and aggregate line stats while ignoring emails, signatures, file paths, patches, raw URLs, API-auth fields, scripts, raw message bodies, prompts, and secret-like markers.
- GitHub commit-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/commits` API payloads, reconstructing summaries only from the safe repository path, commit count, bounded commit SHA prefixes, first safe commit-message lines, author dates, and parent counts while ignoring raw message bodies, emails, signatures, avatars, HTML/API URLs, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub pull-request list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/pulls` API payloads, reconstructing summaries only from the safe repository path, PR count, bounded PR numbers, safe titles, allow-listed states, draft flags, safe authors, and timestamps while rejecting non-JSON fallback content and ignoring PR bodies, head/base refs, URLs, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub pull-request requested-reviewers source-refresh metadata now has a narrow JSON-only parser for `/repos/{owner}/{repo}/pulls/{number}/requested_reviewers` API payloads, reconstructing summaries only from PR number, reviewer/team counts, bounded safe reviewer logins, and bounded safe team slugs while ignoring avatar/profile/API URLs, team descriptions, raw prompt/source/renderer/html/script/data fields, API-auth/query/fragment/userinfo markers, tokens, and secret-like markers; JSON Feed/text fallback payloads, explicit-port authorities, malformed requested-reviewers-shaped paths, and non-string reviewer login/team slug rows fail closed.
- GitHub pull-request review-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/pulls/{number}/reviews` API payloads, reconstructing summaries only from PR number, review count, safe reviewer logins, allow-listed review states, review ids, and submitted timestamps while ignoring review bodies, HTML/review URLs, commit ids, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub pull-request commit-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/pulls/{number}/commits` API payloads, reconstructing summaries only from PR number, commit count, bounded commit SHA prefixes, first safe commit-message lines, author dates, and parent counts while ignoring raw message bodies, emails, signatures, files, patches, HTML/API URLs, API-auth fields, scripts, prompts, renderer/source fields, and secret-like markers.
- GitHub pull-request file-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/pulls/{number}/files` API payloads, reconstructing summaries only from PR number, file count, aggregate additions/deletions/changes, and bounded status counts while ignoring filenames, previous filenames, patches, raw/content URLs, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub issue-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/issues` API payloads, reconstructing summaries only from the safe repository path, issue count, bounded issue/pull-request numbers, safe titles, allow-listed states, safe labels, and update timestamps while ignoring issue bodies, HTML, URLs, raw pull-request payloads, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub issue-label source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/issues/{number}/labels` API payloads, reconstructing summaries only from the safe repository path, issue number, label count, bounded label names, colors, and default flags while ignoring descriptions, URLs, ids/node_ids, query/fragment markers, API-auth fields, scripts, prompts, renderer/source fields, and secret-like markers.
- GitHub issue and pull-request comments source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/{issues|pulls}/{number}/comments` API payloads, reconstructing summaries only from issue/PR number, comment count, safe commenter logins, comment ids, and timestamps while ignoring comment bodies, HTML, reactions, URLs, author association, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub contributors source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/contributors` API payloads, reconstructing summaries only from the safe repository path, contributor count, bounded safe logins, and contribution counts while ignoring avatar/user/profile URLs, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub subscribers/watchers source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/subscribers` API payloads, reconstructing summaries only from the safe repository path, subscriber count, and bounded safe logins while ignoring avatar/user/profile URLs, query/fragment/userinfo markers, API-auth fields, scripts, prompts, renderer/source/data fields, and secret-like markers; JSON Feed-looking bypass payloads and malformed tail rows fail closed.
- GitHub issue-event-list source-refresh metadata now has a narrow allow-listed parser for exact lowercase-host `/repos/{owner}/{repo}/issues/{number}/events` API payloads, reconstructing summaries only from issue number, event count, event-type counts, bounded event ids/types, safe actor logins, safe label names, and timestamps while failing closed on raw event bodies, commit ids/aliases, URLs, non-allow-listed fields, non-JSON fallback content, malformed/encoded path variants, non-lowercase GitHub hosts, API-auth fields, scripts, prompts, renderer/source fields, and secret-like markers.
- GitHub stargazer-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/stargazers` API payloads, reconstructing summaries only from the safe repository path, stargazer count, bounded safe logins, and `starred_at` timestamps when present while ignoring avatar/profile/API URLs, JSON Feed/generic JSON bypass fields, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub fork-list source-refresh metadata now has a narrow allow-listed parser for exact lowercase-host `/repos/{owner}/{repo}/forks` API payloads, reconstructing summaries only from the safe repository path, fork count, bounded safe fork full names, owner logins, default branches, and update timestamps while ignoring fork descriptions, clone/profile/API URLs, JSON Feed/generic text bypass fields, malformed/encoded path variants, non-lowercase GitHub hosts, API-auth fields, scripts, prompts, renderer/source fields, and secret-like markers.
- GitHub participation-stats source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/stats/participation` API payloads, reconstructing summaries only from safe repository path, week count, all/owner commit totals, and active-week count while ignoring raw bodies, URLs, query/fragment/userinfo auth markers, API-auth fields, scripts, prompts, renderer/source fields, and secret-like markers; JSON Feed-looking bypass payloads and malformed aggregate rows fail closed.
- `GET /api/capy-memory/status` returns local-only source/chunk/stale/error/refresh-job counts.
- `static/spaces.js` renders the product-home Memory freshness card from `api/capy-memory/status` with hostile fields ignored/redacted.
- `api/knowledge.py` / Memory Tree bridge registers local knowledge sources as metadata-only Memory Tree source records with local provenance/freshness status.

Remaining:
- Broaden cron/daemon triggering and additional source-specific fetchers while keeping existing manual/scheduled UI trigger receipts, GitHub issue/PR/repository/readme/contents/release/latest-release/release-list/release-assets/branch/tag/labels/topics/workflow/workflow-list/workflow-run/workflow-runs-list/workflow-jobs/workflow-artifacts/repository-artifacts/actions-private-name/environment-private-name/environment-variables/check-runs/commit-statuses/issue-list/issue-event-list/PR-list/PR-requested-reviewers-list/PR-review-list/PR-commit-list/PR-file-list/issue-PR-comments/contributors/stargazers/subscribers/fork-list/commit/commit-list/participation-stats/traffic-views/traffic-clones/environments/collaborators/community-profile metadata ingestion, and all public UI metadata-only.

### Phase 4 — Spaces-aware memory integration

Status: **Space detail, creator preview, production artifact auto-ingest, and active-space context advisory/preflight surfaces implemented; broader advisory enforcement remains**.

Delivered:
- `GET /api/spaces/memory?space_id=...` returns bounded relevant Memory Tree slices with metadata-only snippets.
- `static/spaces.js` renders a Space detail `Memory Tree context` card and creator-preview `Memory assist` evidence without leaking renderer/source/API-auth/raw-prompt markers.
- New artifact canonicalizers cover Space manifests, revision events, widget events, and visual/UI QA reports as searchable local artifacts.
- Production boundaries now call the canonicalizers automatically for Space manifest/revision writes (including rollback/restore anchors), widget events, recovery/repair events, and creator visual-QA commit reports.
- Active-space context now includes cited advisory relevant-memory slices only through a prompt-preflighted, metadata-only envelope with compaction, context status, progress-event, and action-policy evidence.

Remaining:
- Extend the same preflighted advisory-memory envelope to remaining source/tool/browser/development boundaries where memory can influence actions.

### Phase 5 — Autonomy policy, prompt-injection preflight, model-routing hints

Status: **metadata-only status surface plus creator preview/commit, active-space instruction/context, repair prompt, source-refresh preflight, camera-stream tool receipts, package policy receipts, logical path-helper policy receipts, widget reload/refresh required-preflight receipts, safe model-route resolution receipts, and initial actual model-route invocation for safe summary workloads implemented; broader per-action enforcement and model invocation routing remain**.

Delivered:
- `api/capy_policy.py` exposes allow-listed autonomy modes, approval gates, prompt-preflight status, model-routing hints, and bounded/deduplicated action-policy receipts without echoing raw env/provider/model secrets.
- `static/spaces.js` renders the product-home Autonomy policy card from `api/capy-policy/status`.
- Native `update_space(...)` / `/api/spaces/update` active-instruction writes now run `active_space_instructions` prompt preflight before persistence, fail hostile instruction updates closed without revisioning or raw-prompt leakage, preserve safe instruction text, and can return optional metadata-only prompt-preflight/action-policy/progress receipts when `includeSafetyReceipts` is requested.
- Creator preview responses now include a metadata-only `autonomy_policy` receipt with approval gates, prompt-preflight status, and model-route hint evidence, and the creator-preview UI renders this Action policy card without exposing raw prompts, generated bodies, renderer/source fields, API-auth fields, or credentials.
- Source-style layout repair (`space.spaces.repairLayout`) now returns metadata-only prompt-preflight and action-policy evidence beside its structured repair progress receipt, so automated repair boundaries expose approval/preflight/model-route safety metadata without raw layout/source/renderer leakage.
- Creator commit responses now carry forward the preview prompt-preflight receipt and add a commit-scoped `autonomy_policy` receipt after the sandbox preview, visual-QA, and explicit approval gates pass, giving the revisioned-commit result metadata-only policy evidence without persisting raw prompts, generated bodies, renderer/source fields, API-auth fields, credentials, or unsafe screenshot paths.
- Direct active-space instruction aliases (`space.current.agentInstructions` / `space.current.specialInstructions`) now run `active_space_instructions` prompt preflight, return metadata-only action-policy receipts, and withhold hostile instruction text before it can be injected into agent context.
- Source-style Space creation (`space.create` / `space.spaces.create`) now runs `active_space_instructions` prompt preflight before persisting supplied instructions, blocks hostile create-time instruction injection before manifest creation, and returns metadata-only preflight/action-policy/compaction evidence without echoing raw instruction text in the tool response.
- Source-style logical app URL resolution (`space.spaces.resolveAppUrl`) now returns a metadata-only action-policy receipt with destructive-external-action approval and `hint:fast` route evidence, so browser/app navigation helpers expose trust metadata without echoing raw unsafe paths or source/API-auth fields.
- Browser-surface tool actions now run `browser_surface` prompt-preflight classification over an internal-only, bounded corpus of high-risk browser payload fields for receipt-only `open`, `snapshot`, `click_ref`, `type_ref`, `back`, `forward`, `press`/`key`, and `scroll` adapters; hostile prompt/text/URL/DOM/source/renderer/API-auth/token/ref/history payloads, including nested object values and camelCase aliases, block with metadata-only categories, oversized or deeply nested high-risk payloads fail closed, browser execution remains disabled, and raw URL/history/key/ref/text/source/renderer/API-auth payloads are not stored or echoed.
- Receipt-only development terminal/shell tool actions now run `development_tool` prompt preflight over common command/args/message/auth payload shapes, thread pass/block status into autonomy-policy receipts, and return metadata-only progress/output-compaction evidence without executing commands, enabling filesystem writes, or storing raw command/prompt/source/html/renderer/API-auth payloads.
- Source-style Space open/reload navigation helpers (`space.spaces.open` / `openSpace` and `space.spaces.reloadCurrentSpace` / `reloadSpace`) now return metadata-only required prompt-preflight receipts, action-policy receipts, and `space.open:<space_id>` / `space.reload:<space_id>` progress receipts while `get`/`read` aliases remain read-only.
- Local Service Dashboard template installs now return metadata-only `local_service_template` prompt-preflight and `space.template.install.local_service` autonomy-policy receipts for direct, route, and tool-adapter boundaries, keeping service connector/browser-surface setup redacted; high-risk template installs for Browser Surface, Local Service Dashboard, Model Provider Setup, Game Sandbox, and Music Sequencer now also include metadata-only `output_compaction` receipts that cite only template/space/widget-count/preflight/policy/progress metadata, and the template-install UI renders Local Service / Model Provider compaction evidence without leaking hostile fixture fields.
- Source-style current-space viewport/reposition helpers (`space.spaces.repositionCurrentSpace`) now return metadata-only prompt-preflight, action-policy, and structured progress receipts with safe `layout.reposition:<space_id>` run ids, so browser/canvas viewport control exposes visible trust evidence without executing browser movement or leaking renderer/source/API-auth/script/secret request markers.
- Source-style logical storage path helpers (`space.spaces.buildSpace*Path`) now return metadata-only action-policy receipts with destructive-external-action approval and `hint:fast` route evidence, so development/path-boundary helpers expose visible trust metadata without echoing hostile renderer/source/API-auth/script/secret request markers.
- Space metadata mutation aliases (`space.spaces.saveSpaceMeta` / `space.current.saveMeta`) now preflight `agentInstructions`/`specialInstructions` before persistence, block hostile instruction writes without creating revisions, and return metadata-only action-policy receipts for passing instruction updates.
- Source-style layout mutation aliases (`space.spaces.saveSpaceLayout` / `space.current.saveLayout` and `space.spaces.rearrangeWidgets`) now preflight raw prompt-bearing payload fields before persistence/mutation, block hostile prompt injection before revisioning widget layouts, and keep public receipts/state metadata-only.
- Camera-stream tool actions (`space.camera.add_stream` / `camera.add_stream`) now return metadata-only required prompt-preflight receipts, autonomy-policy receipts with the `destructive_external_action` gate and `hint:vision` model-route hint, and structured `camera.stream.add:<space_id>` progress receipts, while approved stream references remain url-digest/host-class metadata only.
- Shared data slot delete tool actions (`space.data.delete` / `space.current.data.delete`) now return metadata-only `space.shared_slot.delete` autonomy-policy receipts with creator-commit approval, required prompt-preflight status, and `hint:summarize` route evidence while preserving existing redacted progress telemetry.
- Space Agent package export responses now include a metadata-only `space.agent.export` autonomy-policy receipt with supervised-mode approval gates, required prompt-preflight status, and `hint:reasoning` model-route evidence; the export UI renders the policy beside package progress without displaying package YAML, widget bodies, renderer/source/API-auth fields, prompts, scripts, archives, or credentials.
- Space Agent package import tool aliases now return per-invocation metadata-only autonomy-policy receipts, so `space.import`, `space.package.import`, and `space.agent.import` can be distinguished in product/tool evidence while preserving prompt-preflight status, approval gates, model-route hints, and import quarantine redaction.
- Action-policy receipts now include a metadata-only `model_route_resolution` decision derived from safe configured route fields or deterministic default fallbacks, and Spaces UI receipts render the selected route plus fallback reason without exposing raw provider config, API-auth fields, renderer/source/script markers, prompts, or credentials.
- Active-space context receipts, repair prompts and empty-prompt whole-Space/widget/module repair queues, whole-Space and recovery-module repair-event list responses, source-style Space revision/history list responses, widget-event list responses, widget-runtime prompts (including direct sandbox queue-status UI receipts), widget-runtime contract helper receipts, fixed widget reload/refresh events without free-form prompts, source-style layout repair, source-style widget definition/blueprint preview/source helpers, and source-refresh ingestion paths now use protected prompt-preflight boundaries before advisory context or user-provided instructions can influence an agent/tool action.
- Source-refresh ingest can now use the configured safe `hint:summarize` route for actual model-backed metadata-only source summaries after auto-fetched-source prompt preflight passes; missing/unsafe/unconfigured/unavailable routes still fall back to deterministic sanitized source records, custom providers can resolve through their configured connection when generic runtime lookup is unavailable, and public receipts/prompts omit raw renderer/source/API-auth fields, prompts, scripts, and credentials.
- The trusted system-widget add flow now renders backend-produced action-policy, system-widget progress, and output-compaction receipts in `static/spaces.js` after the widget manager refresh, keeping first-party panel attachment visibly metadata-only without leaking hostile renderer/script/API-auth/raw-prompt/secret fixture fields.

Remaining:
- Extend pass/warn/block preflight and action-policy receipts to additional high-risk recovery/tool boundaries beyond the current creator/context/repair/repair-list/widget-refresh/source-refresh/browser/development/camera/package-export coverage.
- Wire model-routing resolution into actual Capy/Hermes invocation selection while preserving Brendan's provider-agnostic OpenAI/xAI/LM Studio setup.

### Phase 6 — Structured progress events

Status: **recorder/status/product-home and Space-detail cards implemented; broader producer coverage remains**.

Delivered:
- `api/capy_progress.py` records and summarizes bounded metadata-only progress events.
- The structured progress taxonomy now accepts the full roadmap's non-content delta/event types (`thinking.delta`, `text.delta`, `tool.args.delta`, `subagent.spawned`, and `subagent.progress`) as metadata-only events, with product-home/status family chips for thinking/text streams and no persistence of raw prompt/text/tool arguments.
- `static/spaces.js` renders product-home Progress events stats, family-count chips, recent event types, timestamps, recent stream rows, and queued widget-event prompt-preflight/compaction evidence without raw payload/prompt leakage; backend local widget runtime no-ops now return metadata-only progress/compaction receipts for receipt-aware clients without persisting widget events.
- Space detail now loads `/api/capy-progress/status?space_id=...` and renders a Space-scoped progress card next to Memory Tree context/revision history, showing bounded active-run/recent-event metadata without unsafe event families, generated bodies, renderer/source fields, raw prompts, or credentials.
- Recent progress family counts cover conservative event families such as `run`, `thinking`, `text`, `tool`, `subagent`, `taskboard`, `memory.ingest`, and `space.visual_qa`.
- Research Harness progress updates now emit metadata-only `taskboard.updated` structured progress events so product-home progress status can reflect real research taskboard/workflow activity without storing raw prompts, source text, renderer fields, or credentials.
- Creator-loop commits that pass sandbox preview, visual QA, and explicit approval now emit `space.visual_qa.completed` progress events and render the creator visual-QA progress receipt in the commit success card, giving the product-home stream and creator receipt real visual-QA gate coverage without storing prompts, generated bodies, renderers, sources, or credentials.
- Source refresh workers now emit metadata-only `memory.ingest.started` / `memory.ingest.completed` / `memory.ingest.failed` progress events from opaque source/job run ids, so Memory Tree ingest activity appears in the product progress stream without storing origin URLs, raw summaries, prompts, renderer fields, exception text, or credentials.
- The metadata-only Space demo smoke suite now emits bounded `run.started` / `run.completed` progress events under a fixed safe run id, so demo-suite compaction/context evidence also appears in the product progress stream without storing raw demo output, prompts, widget bodies, renderers, sources, or credentials.
- Individual Browser Surface demo smoke runs now emit metadata-only `run.started` / `run.completed` / `run.failed` progress events under safe `space-demo:<demo>` run ids, so the product progress stream reflects targeted browser parity smokes without storing raw browser output, prompts, widget bodies, renderer/source fields, exception text, or credentials.
- Source-style `space.spaces.repairLayout` now emits metadata-only `tool.completed` progress events with Space-scoped run ids and fallback-safe receipts, giving the progress stream its first direct layout-repair producer without exposing renderer/source/API-auth fields, prompts, script markers, or exception text.
- Creator-loop sandbox previews now emit metadata-only `tool.completed` progress events keyed by their opaque preview receipt ids, so the product progress stream reflects creator preview activity before commit without storing raw prompts, generated widget bodies, renderer/source fields, API-auth data, script markers, or credentials.
- Shared data slot set/read/list/delete tool actions now emit metadata-only `tool.completed` progress events using safe `shared-slot.*` run ids, so Space cooperation/data handoff activity appears in Space-scoped progress without exposing slot values, renderer/source fields, API-auth fields, prompts, or credentials.
- Source-style widget upsert, renderWidget, defineWidget, and widget-blueprint preview/source tool actions plus widget-runtime contract helper inspections now emit metadata-only `tool.completed` progress events with safe `widget.upsert:<space_id>`, `widget.render:<space_id>`, `widget.blueprint.define:<space_id>`, `widget.blueprint.create:<space_id>`, `widget.blueprint.preview:<space_id>`, and `runtime-contract:<space_id>` run ids, so agent-driven Space construction, generated-widget quarantine/preview/render boundaries, and sandbox-contract inspection appear in Space-scoped progress without storing widget bodies, renderer/source/API-auth fields, prompts, scripts, or credentials.
- Source-style layout rearrange/toggle actions now emit metadata-only `tool.completed` progress events with safe `layout.rearrange:<space_id>` and `layout.toggle:<space_id>` run ids, so Space-scoped progress reflects agent-driven layout mutations without storing renderer/html/source/API-auth fields, prompts, scripts, or credentials.
- Source-style current-space viewport/reposition actions now emit metadata-only `tool.completed` progress events with safe `layout.reposition:<space_id>` run ids, so Space-scoped progress reflects browser/canvas viewport requests without storing raw viewport source fields, renderer/html/API-auth markers, scripts, prompts, or credentials.
- Source-style Space open/reload navigation helpers now emit metadata-only `tool.completed` progress events with safe `space.open:<space_id>` and `space.reload:<space_id>` run ids, so browser/canvas navigation requests appear in scoped progress without exposing hostile renderer/source/html/API-auth/script/token request markers.
- Recovery widget quarantine/enable tool actions and recovery-module disable/enable toggles now emit metadata-only `tool.completed` progress events with safe `recovery.widget.*:<space_id>` and `recovery.module.*:recovery-modules` run ids, so Space/recovery-scoped progress reflects recovery admin toggles without exposing disabled widget bodies, module bodies, renderer/source fields, prompts, scripts, or credentials.
- Space Agent package import/export boundaries now emit metadata-only `tool.completed` progress events with safe `package.import:<space_id>` and `package.export:<space_id>` run ids after successful sanitized package operations, without storing package YAML, widget bodies, renderer/source/API-auth fields, prompts, scripts, or credentials; unsupported export formats fail before recording false completion telemetry.
- Recovery revision restores now return metadata-only autonomy-policy evidence and `tool.completed` progress receipts with `recovery.restore:<space_id>` run ids, so rollback/time-travel actions surface in Space-scoped progress without exposing stored renderer/source/API-auth fields, prompts, scripts, disabled widget bodies, or secret-looking fixture markers.
- Space checkpoint anchors now return metadata-only prompt-preflight and autonomy-policy evidence, `tool.completed` progress receipts with safe `checkpoint:<space_id>` run ids, and output-compaction receipts beside the revision event id, and the Space detail checkpoint receipt now renders the returned Action policy plus Checkpoint progress evidence without rendering generated widget bodies or exposing hostile reason text, renderer/source/API-auth fields, prompts, scripts, or secret-looking fixture markers.
- Source-style Space deletion/removal aliases now return metadata-only autonomy-policy receipts and `tool.completed` progress receipts with safe `space.delete:<space_id>` run ids after successful revisioned deletion, without exposing hostile renderer/source/html/API-auth fields, scripts, tokens, or secret-looking fixture markers.
- Source-style Space duplicate/clone aliases now return metadata-only autonomy-policy receipts and `tool.completed` progress receipts with safe `space.duplicate:<space_id>` run ids after successful sanitized Space creation, without exposing hostile renderer/source/html/API-auth fields, prompts, scripts, tokens, or secret-looking fixture markers.
- Source-style logical storage path helpers now emit metadata-only `tool.completed` progress receipts with safe `path.helper:<space_id>` run ids, so development/path-boundary helper calls appear in the product progress stream without storing raw renderer/source/html/API-auth/script/token/secret request markers.
- Individual Space demo smoke results now include product-visible metadata-only `space.demo.run.<demo>` autonomy-policy receipts, so rollback/recovery/browser/demo walkthrough cards expose approval gates, required prompt-preflight status, and `hint:reasoning` route evidence beside compaction/context evidence without rendering prompts, widget bodies, renderer/source/API-auth fields, scripts, tokens, or secret-looking fixture markers.
- Receipt-only development terminal/shell actions now emit metadata-only `tool.completed` progress receipts with safe `development.terminal:<space_id>` run ids, so high-risk development-tool requests appear in Space-scoped progress without executing commands or storing raw command text, prompts, renderer/source/html/API-auth fields, scripts, paths, or credentials.
- WebUI streaming tool lifecycle callbacks now emit metadata-only `tool.started`, `tool.completed`, and `tool.failed` progress events under safe stream-scoped run ids, so real interactive development/tool runs appear in the progress stream without persisting tool names, args, previews, outputs, prompts, renderer/source fields, scripts, paths, or credentials.
- Widget-event list tool responses now include top-level required prompt-preflight, autonomy-policy, and `tool.completed` progress receipts with safe `widget.events:<space_id>` run ids in addition to regenerated metadata-only compaction evidence, so inbox reads expose the same widget-runtime trust envelope and appear in Space-scoped progress without echoing forged event prompts, renderer/source/API-auth fields, scripts, tokens, or secret-looking fixture values.
- Recovery-module and whole-Space repair-event list responses now emit metadata-only `tool.completed` progress receipts with safe `recovery.module.repair_events:recovery-modules` / `recovery.space.repair_events:<space_id>` run ids and thread those run ids into compaction evidence beside prompt-preflight/action-policy status, without exposing module source, renderer, prompt text, session ids, bearer strings, or secret-looking fixture values.

Remaining:
- Emit progress events from more real long-running browser, development, repair, and creator workflows.

### Phase 7 — Optional integration catalog/sidecar exploration

Status: **not started; hold until remaining Phase 1-6 integration work is proven end-to-end**.

Only after the remaining Phase 1-6 integration items are working:
- Evaluate connector catalog UX.
- Explore sidecar/native app integration only if it clearly improves Capy's existing self-hosted server product.

---

## Next implementation slices for autonomous sprints

1. **Remaining prompt-preflight + memory/context enforcement**
   - Extend pass/warn/block receipts from the implemented creator preview/commit, active-space instruction/context, source-style Space creation, repair prompt, widget-runtime prompt, source-style widget definition/blueprint preview/source helpers, source-refresh paths, receipt-only browser adapters, and receipt-only development terminal/shell adapters to remaining high-risk recovery/tool boundaries.
   - Keep Memory Tree content untrusted advisory context that cannot bypass creator gates, approval gates, sandbox preview, visual QA, or rollback/recovery controls.

2. **Broader compaction producers**
   - Extend the implemented run-all, individual-demo, creator preview/commit, active-context, Research Harness progress/artifact, scoped-progress, receipt-only Browser Surface, receipt-only development terminal/shell, high-risk template installs, approved camera-stream, queued widget-event, shared data slot set/read/list/delete, source-style Space create/duplicate/delete, source-style Space metadata/layout/repair/rearrange/reposition, source-style widget upsert, source-style/current widget patch/delete/bulk-delete/toggle, source-style/current widget read/list/see, source-style Space read/get (`space.get`, `space.spaces.get`, `space.spaces.read`, `space.spaces.getSpace`, `space.spaces.readSpace`), source-style Space collection/current read, source-style Space open/reload navigation, logical app URL helper, Big Bang template reset, widget recovery toggle, and recovery-module toggle `output_compaction` receipt pattern to remaining long tool/subagent/recovery-output boundaries where it adds product value.
   - Preserve failures, approval prompts, and artifact handles/citations; never compact away safety-relevant evidence.

3. **Broader source-refresh scheduling/fetcher coverage**
   - Expand due-job scheduler/cron coverage and source-specific fetchers now that the safe metadata-only refresh worker and manual trigger path exist; keep raw fetched content out of public receipts/UI.

4. **Progress producer expansion**
   - Record structured events from browser/development/repair flows so the product-home and Space-detail streams reflect real autonomous work. Research Harness progress updates, creator visual-QA commit gates, Memory Tree source-refresh ingest workers, the demo smoke suite, individual browser demo smokes, source-style layout-repair actions, shared-data set/read/list/delete and widget-upsert actions, widget-event list reads, recovery widget toggles, receipt-only development terminal/shell actions, and WebUI streaming `tool.started`/`tool.completed`/`tool.failed` lifecycle callbacks now cover the first workflow/gate/ingest/run/browser/repair/cooperation/construction/recovery/development producers.

5. **Model-route invocation plumbing**
   - Reuse the metadata-only `model_route_resolution` receipts as the visible decision envelope, then continue wiring safe configured hints into actual Capy/Hermes invocation selection without exposing provider config or weakening Brendan's provider-agnostic OpenAI/xAI/LM Studio setup. Initial safe summary workloads (handoff/source-refresh summaries) now prove the pattern; next targets should stay internal and metadata-only before any creator/widget execution path.

---

## Current reusable components inventory

### Hermes core memory/session components

- `/Users/bschmidy10/.hermes/hermes-agent/agent/memory_manager.py`
  - Existing persistent memory injection/management surface.
  - Reuse conceptually for memory lifecycle boundaries; do not overload durable user/agent memory with bulky source records.
- `/Users/bschmidy10/.hermes/hermes-agent/tools/memory_tool.py`
  - User/agent memory CRUD tool; useful redaction/behavior reference for what should **not** become bulky task logs.
- `/Users/bschmidy10/.hermes/hermes-agent/tools/session_search_tool.py`
  - Existing searchable session-history recall tool; Memory Tree should complement this, not replace it.
- `/Users/bschmidy10/.hermes/hermes-agent/hermes_state.py`
  - SQLite session store and FTS search foundation.
- `/Users/bschmidy10/.hermes/hermes-agent/cron/scheduler.py`
  - Existing autonomous recurring execution; future auto-fetch/freshness jobs can integrate here or with WebUI-local jobs.
- `/Users/bschmidy10/.hermes/hermes-agent/plugins/memory/`
  - Optional external memory-provider plugins. Capy Memory Tree should remain local-first and provider-agnostic.

### Capy WebUI local knowledge surfaces

- `/Users/bschmidy10/hermes-webui/api/knowledge.py`
  - Local-only knowledge adapter with status/search/read/ask/capture note helpers, source redaction, and Obsidian URL support.
  - Strong candidate for integration or reuse when implementing Memory Tree search/read and freshness cards.
- `/Users/bschmidy10/hermes-webui/api/routes.py`
  - Existing routes: `/api/knowledge/status`, `/api/knowledge/search`, `/api/knowledge/read`, `/api/knowledge/ask`, `/api/memory`, and rollback/checkpoint routes.
  - Existing model/provider context handling around session load paths can inform model-routing hints.
- Local knowledge default paths from `api/knowledge.py`:
  - `~/.hermes/local-knowledge`
  - `~/.hermes/local-knowledge/index_config.json`
  - Obsidian vault default `~/Documents/Obsidian Vault`

### Capy Spaces context/revision/progress surfaces

- `/Users/bschmidy10/hermes-webui/api/spaces.py`
  - `build_agent_context(space_id)` already builds compact active-Space prompt context.
  - `queue_widget_event(...)` and `list_widget_events(...)` provide widget-event history.
  - `list_revision_events(...)`, restore helpers, checkpoint/recovery helpers, and creator preview/commit receipts provide revision/provenance anchors.
  - `set_research_progress(...)` already updates Research Harness progress metadata.
- `/Users/bschmidy10/hermes-webui/api/routes.py`
  - Existing Spaces routes include `/api/spaces/revisions`, `/api/spaces/widget/events`, creator/recovery/research routes, and local rollback endpoints.
- `/Users/bschmidy10/hermes-webui/static/spaces.js`
  - Existing rendering for creator previews, revision history, recovery/admin, widget events, smoke receipts, and Space detail canvas.
  - Future Memory Tree/freshness/relevant-memory cards should load the actual checked-out file in real-static tests and browser harnesses.
- `/Users/bschmidy10/hermes-webui/tests/test_spaces_foundation.py`
  - Main backend regression surface for Spaces metadata safety and route/tool contracts.
- `/Users/bschmidy10/hermes-webui/tests/test_spaces_ui_js_behaviour.py`
  - Main real-`static/spaces.js` fake-DOM regression surface for UI safety and behavior.

### Gaps to close / next integration targets

The first roadmap pass has now delivered the Memory Tree module, source/job schema, safe refresh worker, compaction helper, memory freshness card, relevant-memory UI, policy card, active-context advisory/preflight receipts, and product-home plus Space-detail progress cards. Current remaining work is narrower:

- **Automated artifact ingestion:** Core Space manifest/revision, rollback/restore anchor, repair, widget-event, and visual-QA production boundaries now auto-ingest metadata-only Memory Tree records; keep future artifact producers on the same canonicalizer path.
- **Local knowledge bridge:** Local knowledge now registers metadata-only Memory Tree source records with freshness/provenance rather than copying raw files wholesale into prompts; next work is safe refresh scheduling/fetcher breadth.
- **Advisory active context:** Active-Space context and creator preview now use cited, redacted Memory Tree snippets through metadata-only/preflighted envelopes; remaining source/tool/browser/development boundaries should use the same pattern before memory can influence actions.
- **Compaction execution path:** `api/capy_compaction.py` now backs run-all, individual-demo, creator preview/commit, active-context, Research Harness progress/artifact, scoped-progress, receipt-only Browser Surface, high-risk template installs, approved camera-stream, queued widget-event, source-style Space create/duplicate/delete, source-style Space metadata/layout/repair/rearrange/reposition, source-style widget upsert, source-style widget definition/blueprint preview/source/render, source-style/current widget patch/delete/bulk-delete/toggle, source-style/current widget read/list/see, legacy generic `widget.list`/`widget.read`/`widget.get`, source-style Space read/get (`space.get`, `space.spaces.get`, `space.spaces.read`, `space.spaces.getSpace`, `space.spaces.readSpace`), source-style Space collection/current read, widget-runtime contract helper inspections, source-style Space open/reload navigation, logical app URL helper, logical storage path helper, Big Bang template reset, whole-Space recovery toggle, widget recovery toggle, recovery-module toggle, and trusted session recovery repair-safe receipts; remaining long tool/subagent/browser/development/recovery outputs still need product-visible compaction integration where useful.
- **Source refresh worker:** Metadata-only `source.refresh` jobs can now be consumed safely for allow-listed HTML/plain/Markdown, RSS/Atom, JSON Feed, and GitHub issue/PR/repository/readme/contents/release/latest-release/release-list/release-assets/branch/branch-list/tag/labels/languages/topics/workflow/workflow-list/workflow-run/workflow-jobs/check-runs/commit-statuses/issue-list/issue-event-list/PR-list/PR-requested-reviewers-list/PR-review-list/PR-commit-list/PR-file-list/issue-PR-comments/contributors/stargazers/subscribers/fork-list/commit/commit-list/participation-stats/traffic-views/traffic-clones/environments/collaborators API metadata, emitting progress events without raw bodies, README content/encoding/download URLs, issue bodies, event bodies, comment bodies/HTML/reactions/URLs/author association, contributor avatar/user/profile URLs, commit-list avatar/html/API URLs, branch protection details, archive URLs, commit URLs/messages beyond the first safe commit-title line, PR filenames/patches/raw-content URLs, commit emails/signatures/file paths/patches, workflow paths/YAML, workflow-list URLs/jobs, workflow-run logs/jobs URLs, workflow-job steps/runner labels, repository-language generic JSON feed fields, URLs, scripts, prompts, or credentials; remaining work is broadening production scheduler/cron coverage and additional source-specific fetchers around the existing manual trigger path.
- **Per-action policy/preflight:** Product-home policy visibility exists, and creator/context/source-style Space creation/repair/runtime/source-refresh/demo-smoke boundaries have initial pass/warn/block or required-preflight receipts; source-style open/reload navigation helpers, path helpers, camera/package/recovery/checkpoint boundaries, and remaining high-risk browser/development/recovery/tool boundaries need equivalent action-policy evidence as they are added.
- **Progress producers:** Structured progress recording/status/card exist, and Research Harness progress updates, creator visual-QA commits, Memory Tree source-refresh ingest, demo-suite run events, source-style layout/open/reload/repair events, shared-data/widget-upsert actions, recovery widget toggles, recovery-module toggles, and Space Agent package import/export operations now emit metadata-only events; more real long-running browser/development/repair workflows should emit events.

---

## Demo acceptance additions

A roadmap slice is not complete until the product can show bounded evidence, not just backend state:

- Memory freshness card: source counts, stale counts, last run status, and local-only indicator.
- Relevant memory panel: cited, redacted memory snippets attached to a Space or creator preview.
- Compaction evidence: receipt showing original size, compacted size, retained artifact handles/citations, and redaction status.
- Autonomy mode: visible mode label and explanation for the current action/Space.
- Prompt-injection preflight: visible pass/block/warn status for high-risk ingested content.
- Progress panel: structured tool/subagent/creator events with bounded metadata and no raw prompt or secret-like leakage.

---

## Validation defaults

- Plan-doc-only slices: `read_file`, `search_files`, and `git diff --check`; commit plan files only.
- Backend code slices: strict RED/GREEN pytest, `py_compile`, targeted full regression subset, `git diff --check`.
- Frontend-visible slices: real-`static/spaces.js` tests, `node --check static/spaces.js`, browser console/leak check, screenshot, and Visual/UI QA.
- Production WebUI changes: restart `com.capy.webui` and verify local/tailnet health after commit.
