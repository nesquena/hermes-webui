# Capy OpenHuman-Inspired Roadmap

> **For Hermes:** Use this as the canonical strategy file for OpenHuman-inspired Capy/Hermes/Spaces work. Execute it with `subagent-driven-development` task-by-task, with strict TDD for code changes and visual/browser QA for UI-visible slices.

**Decision:** Adopt selected OpenHuman product/architecture ideas clean-room. Do **not** pivot to OpenHuman, do **not** rewrite Hermes/Capy as Rust/Tauri, and do **not** copy GPLv3 OpenHuman code, schemas, tests, comments, or fixtures.

**Goal:** Extend Hermes + Capy Spaces with a local context operating layer: Memory Tree, auto-fetch/freshness, output compaction, source provenance, autonomy/security policy visibility, model-routing hints, and structured progress events.

**Architecture:** Hermes remains the persistent autonomous gateway/tool/cron/subagent layer. Capy Spaces remains the safe metadata-only production workspace/canvas with recovery, revision history, and visual QA. The new context layer is additive: it ingests trusted summaries of local/remote work into local storage and exposes bounded, redacted, cited context back to Spaces and Hermes.

**Primary evidence:** `/tmp/openhuman-review.md` and `.hermes/plans/2026-05-17_123717-openhuman-inspired-capy-roadmap.md` summarize the OpenHuman review at commit `f9de38d6f9bc252501ef79f772b96aedf3926a4d`. Treat those files as evidence and design input, not implementation source.

**Latest competitive audit:** `.hermes/plans/2026-05-24_123241-capy-spaces-openhuman-space-agent-feature-audit.md` refreshes the OpenHuman + Space Agent feature matrix using current repo/docs/release evidence and should guide sprint ordering after the active widget-mutation safety receipt slice lands.

**Integration checkpoint:** `.hermes/plans/capy-spaces-integration-readiness-2026-06-28.md` records the stabilization and review-stack plan for `feat/capy-spaces-foundation`: the original checkpoint passed 3,400 targeted Memory Tree/Spaces tests, JS/Python syntax checks, and `git diff --check`; the 2026-06-30 refresh passed 3,417 targeted tests plus the same syntax/diff checks while the branch was 313 commits ahead before the first documentation checkpoint commit; the 2026-06-30 19:53 CDT targeted-validation refresh again passed `3417 passed, 3 warnings in 51.58s` plus `node --check static/spaces.js`, Python compile checks, `git diff --check`, and a clean `feat/capy-spaces-foundation` status while the branch was 315 commits ahead; the 2026-07-01 00:23 CDT targeted-validation refresh passed `3417 passed, 3 warnings in 52.37s` plus the same syntax/diff checks at 319 commits ahead, with source-refresh evidence-index counts unchanged at 134 GitHub drift regressions, 111 before-body-read regressions, 26 relevant-memory-empty regressions, and 154 positive metadata-only GitHub ingestion regressions; the 2026-07-01 01:33 CDT targeted-validation refresh passed `3417 passed, 3 warnings in 51.43s` plus the same syntax/diff checks at 320 commits ahead, with the same unchanged source-refresh evidence-index counts; the 2026-07-01 02:42 CDT targeted-validation refresh passed `3417 passed, 3 warnings in 51.44s` plus the same syntax/diff checks at 321 commits ahead, with the same unchanged source-refresh evidence-index counts; the 2026-07-01 03:52 CDT targeted-validation refresh passed `3417 passed, 3 warnings in 55.22s` plus the same syntax/diff checks at 322 commits ahead, with the same unchanged source-refresh evidence-index counts; the 2026-07-01 05:02 CDT targeted-validation refresh passed `3417 passed, 3 warnings in 67.15s` plus the same syntax/diff checks at 323 commits ahead, with the same unchanged source-refresh evidence-index counts; and the 2026-07-01 06:10 CDT targeted-validation refresh passed `3417 passed, 3 warnings in 52.00s` plus the same syntax/diff checks at 324 commits ahead, with the same unchanged source-refresh evidence-index counts. A 2026-06-30 visual/UI checkpoint for UI-visible Spaces surfaces also passed using a `/tmp` harness that loaded the checked-out `static/index.html` plus real `static/spaces.js`/CSS with metadata-only mock APIs; final browser console warnings/errors were zero, Memory freshness/source queue/connectors/autonomy policy/progress/recovery evidence rendered, and no hostile sentinels leaked. Because the active branch has crossed the 300-commit threshold, autonomous sprint work should prefer integration/doc consolidation, review-stack refresh, and stabilization over adding new source-refresh route families unless a blocking safety gap is found.

**Source-refresh evidence index:** `.hermes/plans/capy-spaces-source-refresh-evidence-index.md` is the review-oriented index for the noisy GitHub source-refresh safety history. Use it for route-family counts, invariants, and reviewer entry points before reading the chronological per-route status log below.

**Completed slice (2026-07-09 02:41 CDT):** Browser queued widget-event receipts now explicitly label and render the server-returned safety envelope for Ask Capy (`agent.prompt`) and widget refresh events. The checked-out `static/spaces.js` regression keeps the existing event POST bodies unchanged while proving the visible “Widget event receipt” card includes prompt-preflight, supervised action-policy/model-route, metadata-only widget-event progress, Memory Tree advisory/no-authority, output-compaction stats/artifacts, and omission of hostile raw-prompt/trusted-memory/renderer/script/API-auth/token/secret fixture fields from the scoped DOM.

**Completed slice (2026-07-09 01:36 CDT):** Direct widget create/upsert receipts now include and render the server-generated Memory Tree advisory/no-authority envelope. Native `upsert_widget(..., include_safety_receipts=True)` and `/api/spaces/widget/upsert` expose metadata-only `untrusted_advisory`, gate-bypass false, required prompt-preflight/approval/sandbox/visual-QA/rollback gates, and matching output-compaction lines; the save-widget UI regression renders the Memory advisory card while hostile trusted-memory/raw-context/raw-prompt/renderer/script/API-auth/token/secret fixture fields stay out of the scoped receipt DOM.

**Completed slice (2026-07-09 00:31 CDT):** Direct widget create/upsert browser receipt evidence is now aligned with the live `/api/spaces/widget/upsert` helper shape: the checked-out save-widget regression expects the direct upsert `hint:fast` model-route receipt and retained `space:<space_id>` plus `revision:<event_id>` compaction artifacts, while explicitly rejecting invented widget artifact handles for this route. The fixture remains metadata-only and continues proving hostile raw-context/raw-prompt/renderer/script/API-key/secret fields stay out of the scoped receipt DOM.

**Completed slice (2026-07-08 23:06 CDT):** Direct widget create/upsert saves now have explicit browser receipt evidence. The checked-out `static/spaces.js` save-widget regression covers the typed `/api/spaces/widget/upsert` request with `includeSafetyReceipts`, widget-list refresh, and prepended “Widget create/update receipt” with prompt preflight, supervised `space.widget.upsert` action-policy/model-route evidence, `widget.upsert:lab` metadata-only progress, output-compaction stats/artifacts, and hostile raw-context/raw-prompt/renderer/script/API-key/secret fixture omission from the DOM.

**Completed slice (2026-07-08 21:51 CDT):** Direct widget edit/save actions now have explicit widget-update receipt evidence. The checked-out `static/spaces.js` generic edit-form regression covers the typed `/api/spaces/widget/patch` request, widgets refresh, and prepended “Widget update receipt” with prompt preflight, supervised `space.widget.patch` action-policy/model-route evidence, `widget.patch:lab:weather` metadata-only progress, Memory Tree advisory/no-authority, output-compaction stats/artifacts, and hostile raw-context/raw-prompt/renderer/script/API-key/secret/trusted-memory fixture omission from the DOM.

**Completed slice (2026-07-08 20:37 CDT):** Direct widget notes edits now have explicit widget-update receipt evidence. The checked-out `static/spaces.js` notes-save regression covers the typed `/api/spaces/widget/patch` request, detail refresh, and prepended “Widget update receipt” with prompt preflight, supervised `space.widget.patch` action-policy/model-route evidence, `widget.patch:lab:weather` metadata-only progress, Memory Tree advisory/no-authority, output-compaction stats/artifacts, and hostile raw-context/raw-prompt/renderer/script/API-key/secret/trusted-memory fixture omission from the DOM.

**Completed slice (2026-07-08 19:23 CDT):** Direct widget patch actions now request widget safety receipts and render a visible browser “Widget update receipt” after the refresh. The checked-out `static/spaces.js` tracer covers the move-widget layout patch path with `includeSafetyReceipts`, prompt preflight, supervised action-policy/model-route evidence, `widget.patch:<space_id>:<widget_id>` metadata-only progress, Memory Tree advisory/no-authority evidence, output-compaction stats, and safe widget/Space artifact handles while hostile raw-context/raw-prompt/renderer/script/API-key/secret/trusted-memory fixture fields stay out of the DOM.

**Completed slice (2026-07-08 17:56 CDT):** Direct Space duplicate browser receipts now render after confirmed duplicate actions. The checked-out backend regression covers `/api/spaces/duplicate` as a thin explicit-selector wrapper over `space.spaces.duplicateSpace`, and the `static/spaces.js` regression covers the browser flow after the Spaces home refresh, showing the active-space-instructions prompt preflight, supervised action policy/model-route evidence, `space.duplicate:<new_space_id>` metadata-only progress, server-generated Memory Tree advisory/no-authority evidence, output-compaction stats, and safe duplicated Space/revision artifact handles while hostile trusted-memory/raw-context/renderer/script/API-auth/credential/token/secret/path fixture fields stay out of the route response and DOM.

**Completed slice (2026-07-08 16:36 CDT):** Direct Space delete browser receipts now explicitly describe the full safety envelope after confirmed deletion. The checked-out `static/spaces.js` regression covers the real `/api/spaces/delete` flow after the Spaces home refresh, showing delete prompt preflight, supervised action policy/model-route evidence, `space.delete:<space_id>` metadata-only progress, server-generated Memory Tree advisory/no-authority evidence, output-compaction stats, and the safe delete revision artifact while hostile trusted-memory/raw-context/renderer/script/API-auth/credential/token/secret/unsafe-path fixture fields stay out of the DOM.

**Completed slice (2026-07-08 15:31 CDT):** Direct Space create browser receipts now render after new Space saves. The checked-out `static/spaces.js` regression covers the real `/api/spaces/create` direct-save response without create-time instructions: no prompt-preflight card is rendered, the supervised action policy/model-route hint reports prompt preflight `required`, `space.create:<space_id>` metadata-only progress, server-generated Memory Tree advisory/no-authority evidence, and output-compaction stats render while hostile trusted-memory/raw-context/renderer/script/API-auth/credential/token/secret fixture fields stay out of the DOM and the only visible artifact handle is the safe `space:<space_id>` Space create metadata handle.

**Completed slice (2026-07-08 14:01 CDT):** Direct Space update browser receipts now render after edit/save actions. The checked-out `static/spaces.js` regression covers the `/api/spaces/update` response, showing the active-space-instructions prompt preflight, supervised action policy/model-route hint, `space.update:<space_id>` metadata-only progress, server-generated Memory Tree advisory/no-authority evidence, and output-compaction stats while omitting hostile trusted-memory/raw-context/renderer/script/API-auth/credential/token/secret fixture fields and preserving only the safe revision handle.

**Completed slice (2026-07-08 12:47 CDT):** Active Space lifecycle browser receipts now render after `Use in chat` and `Clear from chat` actions. The checked-out `static/spaces.js` regression covers both activation and deactivation responses, showing required active-space-switch prompt preflight, supervised autonomy policy/model-route hint, metadata-only lifecycle progress, server-generated Memory Tree advisory/no-authority evidence, and output-compaction stats while omitting hostile trusted-memory/raw-context/renderer/script/source/data/API-auth/credential/token/secret fixture fields.

**Completed slice (2026-07-08 10:22 CDT):** Creator preview and commit browser result cards now render the server-generated Memory Tree advisory/no-authority receipt from `memory_advisory` between prompt preflight and output-compaction evidence. The focused checked-out `static/spaces.js` behavior regression covers both `space.creator.preview` and `space.creator.commit`, showing only `untrusted_advisory`, gate-bypass false, and required prompt-preflight/approval/sandbox/visual-QA/rollback gates while omitting hostile trusted-memory, raw-context, renderer/script/source/data/API-auth, credential, token, and secret fixture fields.

**Completed slice (2026-07-08 09:04 CDT):** Creator preview and commit receipts now carry a server-generated Memory Tree advisory/no-authority envelope in their public responses and metadata-only output-compaction text. Focused regressions prove `space.creator.preview` and `space.creator.commit` ignore hostile caller-supplied `memory_advisory` authority/gate-bypass/raw-context fields, expose only `untrusted_advisory`, gate-bypass false, and required prompt-preflight/approval/sandbox/visual-QA/rollback gates, and continue omitting raw prompts, renderer/script/source/data/API-auth, credentials, tokens, and secret-looking fixture values.

**Completed slice (2026-07-06 17:36 CDT):** Recovery widget repair queue receipts now carry a server-generated Memory Tree advisory/no-authority envelope in the public response, persisted widget event summary, recovery snapshot latest-event metadata, and metadata-only output-compaction text. The focused regressions prove `queue_recovery_widget_repair_event(...)` and `/api/spaces/recovery/repair-widget` expose only `untrusted_advisory`, gate-bypass false, and required prompt-preflight/approval/sandbox/visual-QA/rollback gates while omitting hostile trusted-memory, renderer/source/html/API-auth, raw prompt/session, generated-body, credential, token, and secret-looking fixture values.

**Completed slice (2026-07-06 17:14 CDT):** Structured progress status now carries a server-generated Memory Tree advisory/no-authority envelope alongside local-only progress taxonomy, recent-event metadata, and output-compaction receipts. The focused regression proves `progress_status(space_id=...)` exposes metadata-only `untrusted_advisory`, gate-bypass false, and required prompt-preflight/approval/sandbox/visual-QA/rollback gates while omitting hostile trusted-memory and secret-looking fixture values from public output.

**Completed slice (2026-07-06 16:26 CDT):** Recovery space and widget enable/disable toggles now have explicit backend evidence that their server-generated Memory Tree advisory/no-authority envelopes are preserved in both the public response and metadata-only output-compaction receipt. The focused regressions assert `untrusted_advisory`, gate-bypass false, required creator/sandbox/visual-QA/rollback gates, and no leakage of recovery reason text, renderer/source/API-auth, bearer, credential, token, or secret-looking fixture values.

**Completed slice (2026-07-06 15:06 CDT):** Widget delete receipts now carry and render the server-generated Memory Tree advisory/no-authority card between widget-delete progress and output-compaction evidence. Native `delete_widget(..., include_safety_receipts=True)` responses include metadata-only advisory status and compaction lines for `advisory_context`, `untrusted_advisory`, gate-bypass false, and required gates; the checked-out `static/spaces.js` widget-delete receipt shows only that allow-listed advisory evidence while hostile trusted-memory/raw-context/renderer/API-auth fixtures remain omitted from public output.

**Completed slice (2026-07-06 13:52 CDT):** Shared-data delete confirmations now render the server-generated Memory Tree advisory/no-authority card inside the Spaces UI delete receipt, between structured progress and output-compaction evidence. The checked-out `static/spaces.js` shared-data surface shows only metadata-only `untrusted_advisory`, gate-bypass false, and required-gate labels while hostile trusted-memory/raw-context/renderer/API-auth fixtures remain omitted from the DOM.

**Completed slice (2026-07-06 11:01 CDT):** Big Bang template reset results now render the server-generated Memory Tree advisory/no-authority card between template-reset progress and output-compaction evidence. The checked-out `static/spaces.js` reset surface shows only metadata-only advisory status (`untrusted_advisory`, gate-bypass false, and required gate labels) while hostile fixture fields such as trusted-memory claims, raw memory context, renderer/script/source HTML, API-auth, credentials, tokens, and secret-looking values remain omitted from public output.

**Completed slice (2026-07-06 09:45 CDT):** Demo-suite `space.demo.run_all` browser results now render the server-generated Memory Tree advisory/no-authority card between progress evidence and output compaction evidence. The checked-out `static/spaces.js` suite surface shows only metadata-only advisory status (`untrusted_advisory`, gate-bypass false, and required gate labels) while hostile fixture fields such as raw context, renderer/source/html/script/data/API-auth, credentials, tokens, and secret-looking values remain omitted from public output.

**Completed slice (2026-07-06 08:27 CDT):** Individual `space.demo.run` smoke receipts now carry the same server-generated Memory Tree advisory/no-authority envelope as demo catalog, demo-suite, and current-context reads. The individual demo output-compaction receipt now includes only allow-listed advisory status (`advisory_context`, `untrusted_advisory`, gate-bypass false, and required gate labels) plus demo/widget/revision metadata, and `static/spaces.js` renders the Memory advisory card inside individual demo-smoke results while public output omits raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, and secret-looking values.

**Completed slice (2026-07-06 07:17 CDT):** Demo-suite `space.demo.run_all` receipts now carry the same server-generated Memory Tree advisory/no-authority envelope as demo catalog and current-context reads. The suite compaction receipt includes only allow-listed advisory status (`advisory_context`, `untrusted_advisory`, gate-bypass false, and required gate labels) plus demo pass/fail metadata, while public output omits raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, and secret-looking values.

**Completed slice (2026-07-05 17:27 CDT):** GitHub Actions organization runner-group runners source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /orgs/{org}/actions/runner-groups/{runner_group_id}/runners` jobs: query/fragment auth, lookalike-host drift, adjacent runner-group id drift, and route-tail drift fail closed before any response-body read, leaving no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile runner payloads, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.

**Completed slice (2026-07-05 04:53 CDT):** GitHub code-scanning single-alert source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}` jobs: query/fragment auth, repository drift, alert-id drift, and alert-instances route-tail drift fail closed before any response-body read, leaving no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile alert body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.

**Completed slice (2026-07-05 03:44 CDT):** GitHub code-scanning single-analysis source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/code-scanning/analyses/{analysis_id}` jobs: query/fragment auth, repository drift, analysis-id drift, and route-tail drift fail closed before any response-body read, leaving no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile analysis body sentinels, raw prompts, refs, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.

**Completed slice (2026-07-04 22:03 CDT):** GitHub repository single-invitation source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/invitations/{invitation_id}` jobs: query/fragment auth, invitation-id drift, userinfo credentials, and lookalike-host drift fail closed before any response-body read, leaving no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, invitee/inviter/body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.

**Completed slice (2026-07-04 19:47 CDT):** GitHub README source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/readme` jobs: repository drift with query/fragment auth fails closed before any response-body read, leaving no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, README content/body, download/html/git links, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.

**Completed slice (2026-07-04 16:28 CDT):** GitHub commit-statuses source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/commits/{sha}/statuses` jobs: cross-repository final-URL drift with query/fragment auth fails closed before any response-body read, leaving no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile status contexts/descriptions/target URLs, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.

**Completed slice (2026-07-04 08:31 CDT):** GitHub Actions organization runner labels source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /orgs/{org}/actions/runners/{runner_id}/labels` jobs: runner-id drift with query/fragment auth fails closed before any response-body read, leaving no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile label rows, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.

**Completed slice (2026-07-03 19:09 CDT):** GitHub single-deployment source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/deployments/{deployment_id}` jobs: query/fragment/userinfo auth, repository/id/path/tail drift, lookalike authority, HTTP scheme drift, and non-string final URLs fail closed before any response-body read, leaving no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, deployment rows, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.

**Completed slice (2026-07-03 18:03 CDT):** GitHub Contents source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/contents/{path}` jobs: drift to another repository fails before body read and leaves no vault/search/relevant-memory artifact; public envelopes omit raw final URLs, content/download/html/git links, raw content bodies, prompts, renderer/source/html/script/data/API-auth, credentials, tokens, and secret-looking values.

**Completed slice (2026-07-03 16:55 CDT):** GitHub deployment-statuses source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/deployments/{deployment_id}/statuses`: drift fails before body read and leaves no vault/search/relevant-memory artifact; public envelopes omit raw final URLs, hostile rows, prompts, renderer/source/html/script/data/API-auth, credentials, tokens, and secret-looking values.

**Completed slice (2026-07-03 15:43 CDT):** GitHub deployments source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/deployments`: query/fragment/userinfo auth, repository/path/tail drift, lookalike authority, HTTP scheme drift, and non-string final URLs fail closed before any response-body read, leaving no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, deployment rows, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixture values.

**Completed slice (2026-07-02 12:52 CDT):** GitHub issue-events source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/issues/{number}/events`: query/fragment auth drift fails closed before any response-body read, leaving no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile event rows, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixture values.

**Completed slice (2026-07-02 06:27 CDT):** GitHub Actions organization runner-group selected-repositories source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories`: query/fragment auth, organization/group-id drift, route-tail drift, userinfo credentials, and lookalike host drift fail closed before any response-body read, leaving no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile repository names, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixture values.

**Completed slice (2026-07-02 00:42 CDT):** GitHub Actions organization runner-groups source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /orgs/{org}/actions/runner-groups`: organization drift in the response final URL fails closed before any response-body read, leaving no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile runner-group names, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixture values.

**Completed slice (2026-07-01 23:32 CDT):** GitHub repository custom-properties source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/properties/values`: query/fragment drift fails closed before any response-body read, leaving no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, rejected property values, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixture values.

**Completed slice (2026-07-01 22:20 CDT):** GitHub commit-comments source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/commits/{sha}/comments`: query/fragment drift fails closed before any response-body read, leaving no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, comment bodies, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.

**Completed slice (2026-07-01):** GitHub secret-scanning alert/list/locations source refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/secret-scanning/alerts`, `GET /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}`, and `GET /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}/locations`: clean fetches still add `hide_secret=true`, while missing `hide_secret` or query/fragment auth drift fails closed before any response-body read, leaving jobs refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.

**Completed slice (2026-06-30):** GitHub issue-comment reactions source refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions`: query/fragment auth drift fails closed before any response-body read, leaving the job refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile reaction body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.

**Completed slice (2026-06-30):** GitHub repository topics source refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/topics`: query/fragment auth drift fails closed before any response-body read, leaving the job refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile topic body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.

**Completed slice (2026-06-30):** GitHub issue reactions source refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/issues/{number}/reactions`: query/fragment/userinfo auth plus repository/issue drift fails closed before any response-body read, leaving the job refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile reaction body/user sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.

**Completed slice (2026-06-30):** GitHub issue-comments source refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/issues/{number}/comments`: query/fragment auth drift fails closed before any response-body read, leaving the job refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile comment body/user sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.

**Completed slice (2026-06-30):** GitHub workflow run attempt-jobs source refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt}/jobs`: query/fragment/userinfo auth, repository/run/attempt, or route-tail drift fails closed before any response-body read, leaving the job refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile job body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-30):** GitHub repository community-profile source refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/community/profile`: query/fragment/userinfo auth or route drift fails closed before any response-body read, leaving the job refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile community profile/file sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-30):** GitHub single pull-request source refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/pulls/{number}`: userinfo/query/fragment auth, repository, or PR-number drift fails closed before any response-body read, leaving the job refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile PR body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-30):** GitHub PR requested-reviewers source refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/pulls/{number}/requested_reviewers`: query/fragment/userinfo auth, repository, PR-number, or path drift fails closed before any response-body read, leaving the job refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, reviewer/team sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-30):** GitHub Actions organization variables source refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET /orgs/{org}/actions/variables`: query/fragment auth/raw-prompt drift fails closed before any response-body read, leaving the job refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, variable names/values, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-30):** GitHub Actions repository variables source refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/actions/variables`: query/fragment auth/raw-prompt drift fails closed before any response-body read, leaving the job refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, variable names/values, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-29):** GitHub repository License source refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/license`: repository drift plus query/fragment auth/raw-prompt material fails closed before any response-body read, leaving the job refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, license content/body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-29):** GitHub issue-list source refresh now has explicit final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/issues`: query/fragment auth/raw-prompt drift fails closed before any response-body read, leaving the job pending/refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile issue body/user/label sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-29):** GitHub repository Events source refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/events`: query/fragment auth/raw-prompt drift fails closed before any response-body read, leaving the job pending/refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile event actor/body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-29):** GitHub workflow-runs source refresh now has explicit final-URL no-body-read + relevant-memory-empty evidence for exact unscoped `GET /repos/{owner}/{repo}/actions/runs` and workflow-scoped `GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}/runs`: repository/workflow-id drift plus query/fragment auth/raw-prompt material fails closed before any response-body read, leaving jobs pending/refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile workflow-run body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-29):** GitHub Actions organization single-secret source refresh now has explicit final-URL no-body-read + relevant-memory-empty evidence for exact `GET /orgs/{org}/actions/secrets/{secret_name}`: organization drift plus query/fragment auth/raw-prompt material fails closed before any response-body read, leaving the job pending/refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile secret metadata body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-29):** GitHub Pages builds source refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/pages/builds`: drift to another repository plus query/fragment auth/raw-prompt material fails closed before any response-body read, leaving the job pending/refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, drifted build rows, pusher markers, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-29):** GitHub repository Actions artifacts source refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/actions/artifacts`: query/fragment auth drift fails closed before any response-body read, leaving the job pending/refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, artifact names, archive/download URLs, workflow/commit bodies, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-29):** GitHub deploy keys source refresh now has stronger final-URL no-body-read + relevant-memory-empty evidence for exact `GET /repos/{owner}/{repo}/keys`: query/fragment auth drift, userinfo/credential drift, repository drift, and route-tail/single-key drift fail closed before any response-body read, leaving the job pending/refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, hostile deploy-key body sentinels, SSH/public-key material, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-29):** GitHub Dependabot repository public-key source refresh now has exact final-URL no-body-read evidence for `GET /repos/{owner}/{repo}/dependabot/secrets/public-key`: query/fragment auth drift, repository drift, and Actions public-key route drift fail closed before any response-body read, leaving the job pending/refresh-failed with no vault/search/relevant-memory artifact and no unsafe public leakage of raw final URLs, public-key material, auth/prompt/widget fields, credentials, tokens, or secret-looking fixtures.

**Completed slice (2026-06-29):** GitHub Dependabot single-alert source refresh now has focused final-URL drift no-body-read evidence for exact `GET /repos/{owner}/{repo}/dependabot/alerts/{alert_number}`, covering query/fragment auth drift, repository drift, alert-number drift, pending/refresh-failed rejection, no vault/search/relevant-memory artifact, and no unsafe public leakage of drifted URL/body/auth/prompt fields.

**Completed slice (2026-06-29):** GitHub code-scanning alert-instances source refresh now has focused final-URL drift no-body-read evidence for exact `GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/instances`, covering clean canonical fetch, pending/refresh-failed rejection, no vault/search/relevant artifact, and no unsafe public leakage of drifted URL/body/auth/prompt fields.

**Completed slice (2026-06-29):** GitHub dependency graph SBOM source refresh now has tightened final-URL drift no-body-read evidence for exact `GET /repos/{owner}/{repo}/dependency-graph/sbom`: response final URLs that drift to another repository fail closed before any response-body read, leaving the job pending/refresh-failed with no vault/search/relevant-memory artifact and no public leakage of raw SBOM/package/body/auth/prompt fields.

---

## Non-negotiables

1. **Clean-room only:** No copying from GPLv3 OpenHuman source, tests, schemas, prompts, or comments.
2. **No platform pivot:** Hermes/Capy stays Python/server/WebUI/Telegram/local-first, not Rust/Tauri desktop-first.
3. **Spaces safety stays primary:** Generated widget execution remains disabled by default. Metadata-only rendering, sandbox preview, visual QA, approval gates, and revision rollback remain required.
4. **Memory is untrusted context:** Retrieved or auto-fetched memory can inform the agent, but cannot bypass prompt-injection checks, creator-loop gates, sandbox checks, approval gates, or recovery quarantine.
5. **Local-first storage:** User/private source records and memory-tree vault content live in local state paths, not Git-tracked repo directories.
6. **Visible trust controls:** Autonomy mode, freshness, provenance, model-routing hints, prompt-injection preflight status, and progress events should be visible in the product UI where relevant.

## Autonomous sprint checkpoint policy

Until the 324-commit `feat/capy-spaces-foundation` local delta is integrated, the autonomous sprint should shift from pure feature accumulation to checkpoint-aware hardening:

1. Every 10 new commits or 12 hours, run the targeted Memory Tree/Spaces verification command recorded in `.hermes/plans/capy-spaces-integration-readiness-2026-06-28.md`; latest active-branch refresh: 2026-07-01 06:10 CDT at 324 commits ahead, `3417 passed, 3 warnings in 52.00s` plus syntax/diff checks and unchanged source-refresh evidence-index counts.
2. If tests fail, stop feature work and produce a stabilization-only fix.
3. If the branch exceeds 300 commits ahead, prefer integration/doc consolidation over adding new route families.
4. Keep new route work small: one route family, one focused drift/no-body-read behavior, one plan-doc update.
5. Avoid broad refactors until the review stack exists.

---

## Phase roadmap summary

### Phase 0 — Plan hygiene and architecture spec

Status: **complete**.

Delivered:
- This canonical roadmap file exists and is referenced by scheduled Capy Spaces sprints.
- `capy-spaces-space-agent-parity.md` includes the OpenHuman-inspired expansion track.
- `capy-spaces-video-demo-parity-checklist.md` includes visible context-layer demo criteria.
- The appendix below inventories reusable components and current gaps.

### Phase 1 — Capy Memory Tree design/MVP

Status: **implemented as a local-first MVP; continue hardening advisory/context enforcement**.

Delivered:
- `docs/capy-memory-tree.md` defines the clean-room schema, storage, provenance, redaction, and TDD plan.
- `api/capy_memory.py` implements deterministic source/chunk canonicalization, SQLite tables, redacted Markdown vault writes, status/search APIs, relevant-memory lookup, metadata-only source refresh-job registration, direct `/api/spaces/memory` relevant-memory trust receipts, and `/api/capy-memory/search` prompt-preflight/autonomy-policy receipts.
- `tests/test_capy_memory_tree.py` covers hostile Space manifests, revision events, widget events, visual-QA reports, storage/search/status routes, source refresh registration, deterministic IDs, bounded traversal, Memory Tree search/relevant-memory receipt envelopes, receipt fallback behavior, query hardening, and leak prevention.
- Live Spaces boundaries now auto-ingest metadata-only Space manifests/revision events, widget events, repair events, rollback/restore revision anchors, and creator visual-QA reports into the Memory Tree. Local knowledge sources are registered as source records with provenance/freshness metadata rather than copied into prompts.

Remaining:
- Keep all memory as advisory untrusted context that cannot bypass creator, approval, recovery, or prompt-injection gates.

### Phase 2 — TokenJuice-style output compaction

Status: **backend helper and product-visible metadata receipts implemented for the first creator/context/demo/progress boundaries plus receipt-only browser/development adapters; broader high-volume tool/subagent/recovery integration remains**.

Delivered:
- `api/capy_compaction.py` implements `compact_output(...)` with bounded receipts, unsafe-marker redaction, path collapsing, repeated-line dedupe, approval-prompt preservation, error-block preservation, invalid-cap rejection, and `rules_applied` metadata.
- `tests/test_capy_output_compaction.py` verifies size accounting, error/approval preservation, redaction status, cap enforcement, and unsafe-marker omission.
- `space_demo_run_all()` now emits a metadata-only `output_compaction` receipt built from allow-listed demo-suite summary lines, so the existing Spaces UI can show original/compacted character counts, redaction status, and allow-listed rules without rendering raw output, prompts, widget bodies, or credentials.
- Individual demo smokes, direct `/api/spaces/update` active-instruction safety receipts, creator preview/commit receipts, active-space context receipts, Research Harness progress-update and artifact-completion receipts, scoped progress status receipts, manual and scheduled Memory Tree source-refresh receipts, receipt-only Browser Surface tool actions, receipt-only development terminal/shell tool actions, approved camera-stream ingestion, queued widget events, shared data slot set/read/list/delete tool responses, Space checkpoint anchors, revision/history list responses, source-style Space create/duplicate/delete tool responses, source-style Space metadata/layout/repair/rearrange/reposition helper responses, source-style Space health helper responses (`space.api.health` / `space.health`), source-style widget upsert helper responses, source-style widget definition/blueprint preview/source/render helper responses, source-style/current widget patch/delete/bulk-delete/toggle helper responses, generic `widget.patch` tool-route responses, legacy generic `widget.list`/`widget.read`/`widget.get` tool-route responses, source-style/current widget read/list/see helper responses, source-style Space read/get helper responses (`space.get`, `space.spaces.get`, `space.spaces.read`, `space.spaces.getSpace`, `space.spaces.readSpace`), source-style Space collection/current read helper responses, widget-runtime contract detail-panel receipt displays, source-style Space open/reload navigation helper responses, logical app URL helper responses, logical storage path-helper responses, Big Bang template resets, whole-Space recovery toggles, whole-Space repair-event list responses, recovery-module repair-event list responses, widget recovery toggles, recovery-module quarantine/toggles, and trusted session recovery repair-safe responses now reuse the same product-visible compaction evidence pattern without rendering raw URLs, terminal commands, logical paths, prompts, markdown bodies, generated bodies, renderer/source/API-auth fields, scripts, credentials, module bodies, camera hosts, ports, query tokens, runtime widget prompts, exception text, raw slot values/metadata, ignored source-style widget/layout/viewport/browser-navigation/path-helper payloads, raw widget request/list/detail payloads, raw Space descriptions/instructions, checkpoint/recovery reasons, source-refresh origin URIs/fetched content/job internals, or local session/audit paths.

- Camera stream add receipts (`space.camera.add_stream` / `camera.add_stream`) now include a server-generated Memory Tree advisory/no-authority envelope beside browser-surface prompt-preflight, `hint:vision` autonomy-policy, structured progress, and metadata-only output-compaction evidence. The compaction path clamps forged memory authority through the shared advisory summary and omits private stream URLs, hosts/ports, query/bearer tokens, renderer/source/html/script/data/API-auth fields, `trusted_system_memory` / bypass claims, and secret-looking values.
- Space Agent package import/export safety receipts now include server-generated Memory Tree advisory/no-authority envelopes beside package prompt-preflight, autonomy-policy, progress, and output-compaction evidence, ignoring caller-forged package memory authority while omitting package YAML/archive/widget bodies, renderer/source/API-auth fields, scripts, tokens, and secret-looking fixtures.
- Native `upsert_widget(..., include_safety_receipts=True)` direct helper responses now include metadata-only `output_compaction` evidence beside prompt-preflight, autonomy-policy, and progress receipts, without leaking renderer/source/API-auth/script/secret fixture fields.
- Space Agent package import compaction receipts now preserve the import prompt-preflight status from the action-policy envelope, matching package export receipt parity while keeping package YAML, widget bodies, renderer/source/API-auth fields, scripts, and secret-looking values out of compaction evidence.

Remaining:
- Extend compaction receipts to remaining long external tool/subagent/recovery-output boundaries where they add product value.
- Preserve safety-relevant prompts, failures, approval prompts, and artifact handles/citations when broader execution paths adopt compaction.

### Phase 3 — Auto-fetch source registry and freshness

Status: **source registry/status UI, local-knowledge bridge, source-refresh job queueing, safe metadata-only refresh worker, and product-home scheduled refresh tick trigger implemented; broader cron/daemon automation and source-specific fetcher breadth remain**.

Delivered:
- GitHub Actions runner labels source-refresh now has explicit final-URL drift no-body-read and relevant-memory-empty evidence for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/actions/runners/{runner_id}/labels` jobs: response final URLs with query/fragment auth material, runner-id drift, or route-tail drift fail closed before response-body reads, JSON parsing, vault persistence, search artifacts, or Spaces relevant-memory results, with no public leakage of raw final URLs, hostile runner-label body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub pull-list source-refresh now has explicit final-URL drift no-body-read and relevant-memory-empty evidence for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/pulls` jobs: response final URLs with query/fragment/userinfo auth material or repository drift fail closed before response-body reads, JSON parsing, vault persistence, search artifacts, or Spaces relevant-memory results, with no public leakage of raw final URLs, hostile pull-list body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub contributors source-refresh now has explicit final-URL drift no-body-read and relevant-memory-empty evidence for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/contributors` jobs: response final URLs with query/fragment auth material or repository drift fail closed before response-body reads, JSON parsing, vault persistence, search artifacts, or Spaces relevant-memory results, with no public leakage of raw final URLs, hostile contributor body sentinels/logins, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub Actions runner-groups source-refresh now has focused final-URL drift no-body-read and relevant-memory-empty evidence for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/actions/runner-groups` jobs: response final URLs with query/fragment/userinfo auth material, route-tail drift, repository drift, or lookalike hosts fail closed before response-body reads, JSON parsing, vault persistence, search artifacts, or Spaces relevant-memory results, with no public leakage of raw final URLs, hostile runner-group body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub Actions cache usage source-refresh now has focused final-URL drift no-body-read and relevant-memory-empty evidence for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/actions/cache/usage` and `GET https://api.github.com/repos/{owner}/{repo}/actions/cache/usage-by-ref` jobs: response final URLs with query/fragment auth material fail closed before any response-body read, JSON parsing, vault persistence, search artifact, or Spaces relevant-memory result, with no public leakage of raw final URLs, cache-size/ref sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub Actions single artifact source-refresh now has focused final-URL drift no-body-read evidence for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/actions/artifacts/{artifact_id}` jobs: response final URLs with query/fragment auth material, repository drift, or artifact-id drift fail closed before any response-body read, JSON parsing, or vault persistence, leaving drifted jobs pending/refresh failed without vault/search/relevant-memory artifacts or unsafe public leakage of raw final URLs, artifact body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub repository collaborators source-refresh now has focused final-URL no-body-read evidence for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/collaborators` jobs: response final URLs with query/fragment auth material or repository drift fail closed before any response-body read, JSON parsing, or vault persistence, leaving drifted jobs pending/refresh failed without vault/search/relevant-memory artifacts or unsafe public leakage of raw final URLs, collaborator body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub single branch source-refresh now has focused final-URL drift no-body-read evidence for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/branches/{branch}` jobs: response final URLs with query/fragment auth material, repository drift, or branch-route drift fail closed before any response-body read, JSON parsing, or vault persistence, leaving drifted jobs pending/refresh failed without vault/search/relevant-memory artifacts or unsafe public leakage of raw final URLs, branch body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub repository rulesets source-refresh now has focused final-URL drift no-body-read evidence for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/rulesets` jobs: response final URLs with userinfo, query/fragment auth material, repository drift, or route drift fail closed before any response-body read, JSON parsing, or vault persistence, leaving drifted jobs pending/refresh failed without vault/search/relevant-memory artifacts or unsafe public leakage of raw final URLs, ruleset body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub Dependabot organization selected-repositories source-refresh now has focused final-URL drift no-body-read evidence for metadata-only exact `GET https://api.github.com/orgs/{org}/dependabot/secrets/{secret_name}/repositories` jobs: response final URLs with userinfo/query/fragment auth material or organization drift fail closed before any response-body read, JSON parsing, or vault persistence, leaving drifted jobs pending/refresh failed without vault/search/relevant-memory artifacts or unsafe public leakage.
- GitHub Dependabot organization public-key source-refresh now has focused final-URL drift before-body-read + relevant-memory-empty evidence for metadata-only exact `GET https://api.github.com/orgs/{org}/dependabot/secrets/public-key` jobs: response final URLs with query/fragment auth material, organization drift, Actions-route drift, route tails/encoded tails, or userinfo fail closed before any response-body read, JSON parsing, vault persistence, search indexing, or Spaces relevant-memory output, leaving the job pending/refresh failed with no vault/search/relevant-memory artifact and no public leakage of raw final URLs, public-key body sentinels/material, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub PR commits source-refresh now has focused final-URL drift no-body-read + relevant-memory-empty hardening for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/pulls/{number}/commits` jobs: response final URLs with query/fragment auth material fail closed before any response-body read, JSON parsing, vault persistence, search indexing, or Spaces relevant-memory output, leaving the job pending/refresh failed with no vault/search/relevant-memory artifact and no public leakage of raw final URLs, PR commit body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub Pages source-refresh now has focused final-URL drift no-body-read + relevant-memory-empty hardening for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/pages` jobs: response final URLs with query/fragment auth material, raw-prompt fragments, or any non-exact canonical drift fail closed before any response-body read, JSON parsing, vault persistence, search indexing, or Spaces relevant-memory output, leaving the job pending/refresh failed with no vault/search/relevant-memory artifact and no public leakage of raw final URLs, Pages body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub Actions organization selected-actions source-refresh now has focused final-URL drift no-body-read + relevant-memory-empty evidence for metadata-only exact `GET https://api.github.com/orgs/{org}/actions/permissions/selected-actions` jobs: response final URLs with userinfo/query/fragment auth material or organization drift fail closed before any response-body read, JSON parsing, vault persistence, search indexing, or Spaces relevant-memory output, leaving the job pending/refresh failed with no vault/search/relevant-memory artifact and no public leakage of raw final URLs, hostile body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub single pull-request source-refresh now has strengthened final-URL drift no-body-read + relevant-memory-empty evidence for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/pulls/{number}` jobs: response final URLs with userinfo/query/fragment auth material, repository drift, or PR-number drift fail closed before any response-body read, JSON parsing, vault persistence, search artifact, or Spaces relevant-memory result, leaving the job pending/refresh failed with no public leakage of raw final URLs, hostile PR body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub environment deployment branch policy source-refresh now has route-specific final-URL drift no-body-read + relevant-memory-empty hardening for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies` and `GET https://api.github.com/repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{policy_id}` jobs: response final URLs with query/fragment/auth material, userinfo, repository/environment drift, or policy-id drift fail closed before any response-body read, JSON parsing, vault persistence, search indexing, or Spaces relevant-memory output, leaving the job pending/refresh failed with no vault/search/relevant-memory artifact and no public leakage of raw final URLs, deployment-branch-policy body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub repository topics source-refresh now has strengthened final-URL drift no-body-read + relevant-memory-empty evidence for metadata-only `GET https://api.github.com/repos/{owner}/{repo}/topics` jobs: response final URLs with query/fragment/auth material fail closed before any response-body read, JSON parsing, vault persistence, search artifact, or Spaces relevant-memory result, leaving the job pending/refresh failed with no public leakage of raw final URLs, topic body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub repository single-invitation source-refresh now has focused final-URL drift safety evidence for metadata-only `GET https://api.github.com/repos/{owner}/{repo}/invitations/{invitation_id}` jobs: response final URLs with query/fragment/auth material, userinfo, lookalike hosts, or invitation-id drift fail closed before any response-body read, JSON parsing, or vault persistence, with no vault/search artifact and no public leakage of raw final URLs, hostile invitation body sentinels, invitee/inviter logins, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub environment secrets source-refresh final-URL drift safety is now exact for metadata-only `GET https://api.github.com/repos/{owner}/{repo}/environments/{environment_name}/secrets` jobs: response final URLs must remain the clean canonical HTTPS `api.github.com` environment-secrets route for the same repository/environment before any response-body read, JSON parsing, vault persistence, search indexing, or Spaces relevant-memory output. Query/fragment/auth and userinfo drift fail closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, private-name rows, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub pull review comments source-refresh now has focused final-URL drift safety evidence for metadata-only `GET https://api.github.com/repos/{owner}/{repo}/pulls/{number}/comments` jobs: response final URLs with query/fragment/auth material fail closed before any response-body read, JSON parsing, vault persistence, search indexing, or Spaces relevant-memory output, with no vault/search/relevant-memory artifact and no public leakage of raw final URLs, hostile review-comment sentinels, reviewer logins, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub release reactions source-refresh now has focused final-URL drift safety evidence for metadata-only `GET https://api.github.com/repos/{owner}/{repo}/releases/{release_id}/reactions` jobs: response final URLs must remain the exact clean canonical HTTPS `api.github.com` release-reactions route for the same repository/release before any response-body read, JSON parsing, or vault persistence. Query/fragment/auth, userinfo, and release-id drift fail closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, hostile reaction body sentinels/logins, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub pull review comment reactions source-refresh now has focused final-URL drift no-body-read + relevant-memory-empty evidence for metadata-only `GET https://api.github.com/repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions` jobs: response final URLs must remain the exact clean canonical HTTPS `api.github.com` pull-review-comment-reactions route for the same repository/comment before any response-body read, JSON parsing, vault persistence, search artifact, or Spaces relevant-memory result. Comment-id/query/fragment/auth drift fails closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, hostile reaction body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub commit-comment reactions source-refresh final-URL drift safety is now exact for metadata-only `GET https://api.github.com/repos/{owner}/{repo}/comments/{comment_id}/reactions` jobs: response final URLs must remain the clean canonical HTTPS `api.github.com` commit-comment-reactions route before any response-body read, JSON parsing, vault persistence, or Spaces relevant-memory result. Query/fragment/auth drift fails closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, hostile reaction body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub issue-comment reactions source-refresh final-URL drift safety is now exact for metadata-only `GET https://api.github.com/repos/{owner}/{repo}/issues/comments/{comment_id}/reactions` jobs: response final URLs must remain the clean canonical HTTPS `api.github.com` issue-comment-reactions route before any body read, JSON parsing, vault persistence, or Spaces relevant-memory result. Query/fragment/auth drift fails closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, hostile reaction body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub issue reactions source-refresh final-URL drift safety is now exact for metadata-only `GET https://api.github.com/repos/{owner}/{repo}/issues/{number}/reactions` jobs: response final URLs must remain the clean canonical HTTPS `api.github.com` issue-reactions route for the same repository and issue before any body read, JSON parsing, vault persistence, or Spaces relevant-memory result. Query/fragment/userinfo/auth and repo/issue drift fail closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, hostile reaction body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub Actions repository single-secret source-refresh final-URL drift safety now has focused no-body-read + relevant-memory-empty evidence for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/actions/secrets/{secret_name}` jobs: drifted response final URLs with query/fragment/auth, secret-name changes, or unsafe route material fail closed with the job pending, no body read, no vault/search/relevant-memory artifact or Spaces relevant-memory output, and no public leakage of raw final URLs, secret names, raw prompts, renderer/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub workflow run jobs source-refresh final-URL drift safety now has explicit no-body-read + relevant-memory-empty evidence for metadata-only `GET https://api.github.com/repos/{owner}/{repo}/actions/runs/{run_id}/jobs` jobs: response final URLs must remain the clean canonical HTTPS `api.github.com` workflow-run jobs route for the same repository and run before any response-body read, JSON parsing, vault persistence, or Spaces relevant-memory result. Query/fragment/auth, run-id drift, cross-repository drift, route-tail drift, and secret-looking final URL markers fail closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, job rows, raw prompts, renderer/source/html/script/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub environment variables source-refresh final-URL drift safety now has explicit no-body-read + relevant-memory-empty evidence for metadata-only `GET https://api.github.com/repos/{owner}/{repo}/environments/{environment_name}/variables` jobs: response final URLs must remain the clean canonical HTTPS `api.github.com` environment-variables route for the same repository and environment before any response-body read, JSON parsing, vault persistence, or Spaces relevant-memory result. Query/fragment/userinfo/auth, port/authority drift, route-tail drift, and secret-looking final URL markers fail closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, variable rows, raw prompts, renderer/source/html/script/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub commit-statuses source-refresh final-URL drift safety is now route-specific for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/commits/{sha}/statuses` jobs: response final URLs must remain the exact clean canonical HTTPS `api.github.com` commit-statuses route for the same repository and commit before any response-body read, JSON parsing, or vault persistence. Query/fragment/auth, cross-repository drift, route-tail drift, and secret-looking final URL markers fail closed with the job pending/refresh failed, no vault/search artifact, no body read, and no public leakage of raw final URLs, status descriptions/target URLs, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub Actions organization secrets public-key source-refresh final-URL drift safety now has explicit no-body-read + relevant-memory-empty evidence for metadata-only exact `GET https://api.github.com/orgs/{org}/actions/secrets/public-key` jobs: response final URLs must remain the exact clean canonical HTTPS `api.github.com` organization secrets public-key route for the same org before any response-body read, JSON parsing, vault persistence, search indexing, or Spaces relevant-memory output. Query/fragment/auth, cross-org drift, tail/encoded-tail drift, uppercase/port authority drift, and non-string final URLs fail closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, public-key material/body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub Actions organization secrets source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/orgs/{org}/actions/secrets` jobs: a route-specific matcher now requires response final URLs to remain exact clean canonical HTTPS `api.github.com` organization-secrets routes for the same org before any response-body read, JSON parsing, vault persistence, or Spaces relevant-memory output. Query/fragment/userinfo/auth, explicit port, lookalike host, cross-org, tail/encoded-tail, and non-string final URL drift fail closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, organization-secret body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub latest-release source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/releases/latest` jobs: HTTP origins now fail before network open, and response final URLs must exactly match the sanitized canonical HTTPS `api.github.com` latest-release route before any response-body read, JSON parsing, vault persistence, or Spaces relevant-memory output. Query/fragment/userinfo/auth, repository/path/tail, lookalike-authority, and HTTP-to-HTTPS redirect drift fail closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, latest-release body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub Actions OIDC subject-claim source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/actions/oidc/customization/sub` jobs: response final URLs must match the sanitized canonical HTTPS `api.github.com` route for the same repository before any response-body read, JSON parsing, vault persistence, search indexing, or Spaces relevant-memory output. Query/fragment/userinfo/auth, repository/path/tail, lookalike-authority, and non-string final URL drift fail closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, OIDC body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub deployments source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/deployments` and `GET https://api.github.com/repos/{owner}/{repo}/deployments/{deployment_id}` jobs: response final URLs must exactly match the sanitized canonical HTTPS `api.github.com` deployments route for the same repository and deployment id, where applicable, before any response-body read, JSON parsing, or vault persistence. Query/fragment/userinfo/auth, repository/id/path/tail drift, lookalike-authority, HTTP-scheme drift, and non-string final URLs fail closed with the job pending/refresh failed, no vault/search artifact, no body read, and no public leakage of raw final URLs, deployment body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub deployment-statuses source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/deployments/{deployment_id}/statuses` jobs: response final URLs must remain the exact clean canonical HTTPS `api.github.com` deployment-statuses route for the same repository and deployment before any response-body read, JSON parsing, or vault persistence. Query/fragment/userinfo/auth, explicit port, lookalike host, cross-repository/deployment drift, path-tail/route drift, HTTP scheme, and non-string final URLs fail closed with the job pending/refresh failed, no vault/search artifact, no body read, and no public leakage of raw final URLs, status body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub issue-list source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/issues` jobs: response final URLs must remain clean canonical HTTPS `api.github.com` issue-list routes for the same repo before body reads; query/fragment/userinfo/port/host/repo/path/scheme/non-string drift fails closed with no vault/search/relevant-memory artifact, no body read, and no unsafe public leakage.
- GitHub commit-comments source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/commits/{sha}/comments` jobs: response final URLs must exactly match the sanitized canonical route before body reads, vault persistence, search indexing, or Spaces relevant-memory output; query/fragment drift fails closed with no vault/search/relevant-memory artifact, no body read, and no leakage of raw final URLs, comment bodies, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub milestones source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/milestones` jobs: response final URLs must exactly match the sanitized clean HTTPS `api.github.com` milestones route for the same repo before body reads; route/query/fragment/userinfo/port/scheme/non-string drift fails closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, milestone rows, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub release-assets source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/releases/{release_id}/assets` jobs: response final URLs must be clean canonical HTTPS `api.github.com` routes with no query/fragment/userinfo/port and the same owner/repo/release id before any body read, JSON parsing, vault persistence, or Spaces relevant-memory output. Same-route auth/query drift, route/release drift, and noncanonical finals leave jobs pending/refresh failed with no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, hostile body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub PR files source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/pulls/{number}/files` jobs: response final URLs must be string HTTPS `api.github.com` canonical routes with no userinfo/query/fragment and the same owner/repo/PR number before body read; drift leaves jobs pending/refresh failed with no vault/search/relevant-memory artifact, no body read, and no unsafe public leakage.
- GitHub workflow run approvals source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/actions/runs/{run_id}/approvals` jobs: response final URLs must be string clean canonical HTTPS `api.github.com` routes with the same repo and run id, no userinfo/port/query/fragment, before body read; drift leaves jobs pending/refresh failed with no vault/search/relevant-memory artifact, no body read, and no unsafe public leakage.
- GitHub workflow run pending-deployments source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments` jobs: a route-specific matcher now requires the response final URL to exactly match the sanitized canonical HTTPS `api.github.com` route for the same repo and run id before any response-body read. Query/fragment/userinfo/auth, repository/run/path-tail drift, lookalike hosts, and non-string finals fail closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, pending-deployment rows, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub workflow run attempt jobs source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt}/jobs` jobs: response final URLs must exactly match the clean canonical route before any response-body read; query/fragment/userinfo auth, repository/run/attempt, and route-tail drift leave jobs pending/refresh failed with no vault/search/relevant-memory artifact, no body read, and no unsafe public leakage of raw final URLs, hostile job body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub workflow timing source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/actions/workflows/{workflow_id}/timing` jobs: response final URLs must exactly match the sanitized canonical API route before body read, with query/fragment/userinfo/auth, repo/workflow/tail drift leaving jobs pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no unsafe public leakage.
- GitHub Actions organization runners source-refresh final-URL protection is now centralized and covered by focused no-body-read + relevant-memory-empty regressions for exact `GET https://api.github.com/orgs/{org}/actions/runners` jobs: response final URLs must exactly match the sanitized canonical HTTPS `api.github.com` organization runners route before any body read, JSON parsing, vault persistence, search indexing, or Spaces relevant-memory output. Query/fragment/userinfo/auth, organization/path/tail, and lookalike-host drift leave jobs pending/refresh failed with no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, runner body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, registration-token markers, tokens, or secret-looking values.
- GitHub Actions organization runner-downloads source-refresh now has explicit before-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/orgs/{org}/actions/runners/downloads` jobs: response final-URL query/fragment/userinfo/auth, organization/path/tail, and lookalike-host drift fails closed before body reads, leaving jobs pending/refresh failed with no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, runner-download body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub contributor-stats source-refresh now rejects response final-URL query/fragment/userinfo/auth drift before body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/stats/contributors` jobs, requiring exact sanitized canonical route parity and leaving drifted jobs pending/refresh failed with no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, contributor body sentinels/logins, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub code-scanning default-setup source-refresh now rejects response final-URL query/fragment/userinfo/auth, repo/path, or host drift before body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/code-scanning/default-setup` jobs, requiring exact clean canonical route parity and leaving drifted jobs pending/refresh failed with no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub code-scanning analyses source-refresh final-URL drift safety is complete for metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/code-scanning/analyses` jobs: query/fragment auth, userinfo, lookalike host, different-repo, tail-route, and non-string response final URLs fail closed before body reads, leaving jobs pending/refresh failed with no vault/search/relevant-memory artifact and no public leakage of raw final URLs, hostile body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub code-scanning alerts source-refresh final-URL drift hardening is complete: metadata-only exact `GET https://api.github.com/repos/{owner}/{repo}/code-scanning/alerts` now rejects final URLs with query/fragment/userinfo/port/lookalike/cross-repo/tail drift before response-body read; no vault/search/relevant-memory artifact; no raw final URL/prompt/API-auth/token leakage.
- GitHub commit-activity source-refresh now rejects response final-URL query/fragment/userinfo/auth drift before body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/stats/commit_activity` jobs, requiring exact sanitized canonical HTTPS `api.github.com` route parity and leaving drifted jobs pending/refresh failed with no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, commit-activity body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub issue-events source-refresh now rejects response final-URL query/fragment/userinfo/auth drift before body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/issues/{positive_number}/events` jobs, requiring exact sanitized canonical HTTPS `api.github.com` route parity and leaving drifted jobs pending/refresh failed with no vault/search artifact, no body read, and no public leakage of raw final URLs, issue-event row/body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub branch-protection source-refresh now has strengthened response final-URL no-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/branches/{branch}/protection` jobs: query/fragment auth, userinfo credentials, repository drift, or branch-protection route-tail drift fails closed before any response-body read, JSON parsing, vault persistence, search artifact, or Spaces relevant-memory result, with no public leakage of raw final URLs, hostile body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, or secret-looking values.
- GitHub Dependabot alerts list source-refresh now rejects response final-URL query/fragment/userinfo/auth drift before body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/dependabot/alerts` jobs, requiring exact sanitized canonical route parity and leaving drifted jobs pending/refresh failed with no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, alert row/body sentinels, advisory/vulnerability details, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub workflow-list source-refresh now has strengthened response final-URL no-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/actions/workflows` jobs: query/fragment/userinfo/auth, repository/path, or other noncanonical drift fails closed before any response-body read, JSON parsing, vault persistence, search artifact, or Spaces relevant-memory result, with no public leakage of raw final URLs, workflow row/body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub check-runs source-refresh now has strengthened no-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/commits/{40-hex-sha}/check-runs` jobs: response final URLs with query/fragment/userinfo/auth, repository/SHA/path drift, or other noncanonical finals fail closed before any response-body read, JSON parsing, vault persistence, search artifact, or Spaces relevant-memory result, requiring exact sanitized canonical HTTPS `api.github.com` route parity and leaving drifted jobs pending with no public leakage of raw final URLs, check-run row/body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub Actions runner-downloads source-refresh now has explicit before-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/actions/runners/downloads` jobs: response final URLs must match the clean HTTPS `api.github.com` canonical route, so query/fragment/userinfo/auth, repository/tail, and lookalike-host drift leave jobs pending with no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, runner-download body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub participation-stats source-refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/stats/participation` jobs, requiring exact sanitized canonical route parity and leaving drifted jobs pending with no vault/search/relevant-memory artifact or public leakage of raw final URLs, participation arrays/body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub Actions workflow permissions source-refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/actions/permissions/workflow` jobs: response final URLs with query/fragment/userinfo/auth, repository/path/host/scheme/port drift, or non-string values fail closed before any response-body read, JSON parsing, vault persistence, search artifact, or Spaces relevant-memory result, requiring exact sanitized canonical route parity and leaving drifted jobs pending with no public leakage of raw final URLs, workflow-permission policy body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub issue timeline source-refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/issues/{positive_number}/timeline` jobs: response final URLs with query/fragment/auth drift fail closed before response-body reads, JSON parsing, vault persistence, search artifacts, or Spaces relevant-memory results, requiring exact sanitized canonical route parity and leaving drifted jobs pending with no public leakage of raw final URLs, timeline rows, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub PR requested-reviewers source-refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/pulls/{number}/requested_reviewers` jobs: query/fragment/userinfo auth, repository, PR-number, or path drift fails closed before response-body reads, JSON parsing, vault persistence, search artifacts, or Spaces relevant-memory results, with no public leakage of raw final URLs, reviewer/team sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub issue-labels source-refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/issues/{positive_number}/labels` jobs: query/fragment/userinfo/auth, repo/issue/path/host/scheme/port drift, and non-string final URLs fail closed before response-body reads, requiring exact sanitized canonical route parity and leaving drifted jobs pending with no vault/search/relevant-memory artifact or public leakage of raw final URLs, hostile label fields, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub issue-comments source-refresh now rejects response final-URL query/fragment/auth drift before response-body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/issues/{number}/comments` jobs, requiring exact sanitized canonical route parity and leaving drifted jobs pending with no vault/search artifact or public leakage of raw final URLs, raw prompts, comment bodies, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub repository autolinks source-refresh now rejects response final-URL query/fragment/auth drift before response-body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/autolinks` jobs, requiring exact sanitized canonical route parity and leaving drifted jobs pending with no vault/search/relevant-memory artifact or Spaces relevant-memory output, and no public leakage of raw final URLs, raw prompts, `url_template`, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub repository security-advisories source-refresh now rejects response final-URL query/fragment/userinfo/auth drift before response-body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/security-advisories` jobs, requiring exact sanitized canonical route parity and leaving drifted jobs pending with no vault/search/relevant-memory artifact or Spaces relevant-memory output, and no public leakage of advisory rows, raw final URLs, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub Secret Scanning alert-list, single-alert, and alert-locations source-refresh now require the response final URL to remain the exact metadata-only `hide_secret=true` fetch route before body reads; final URL query/fragment/auth drift or safety-query stripping leaves jobs pending with no vault/search artifact or public leakage of raw final URLs, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values, while legacy exact alert-list sources preserve safe public labels plus hidden clean fetch origins for due requeues.
- GitHub repository invitations list source-refresh now rejects response final-URL query/fragment/auth drift before body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/invitations` jobs, requiring exact clean canonical route parity, exact `api.github.com` authority, and same repo path while preserving single-invitation behavior; drift leaves jobs pending with no vault/search/relevant-memory artifact or public leakage of raw final URLs, invitee sentinels, access tokens, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub private vulnerability reporting source-refresh now rejects response final-URL query/fragment/userinfo/auth, repository, route, scheme, port, or host drift before response-body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/private-vulnerability-reporting` jobs, requiring exact sanitized canonical route parity and leaving drifted jobs pending with no vault/search/relevant-memory artifact or Spaces relevant-memory output, and no public leakage of raw final URLs, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub CODEOWNERS errors source-refresh now rejects response final-URL query/fragment/userinfo/auth, repository, route, or host drift before response-body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/codeowners/errors` jobs, requiring exact canonical API route parity and leaving drifted jobs pending with no vault/search/relevant-memory artifact or Spaces relevant-memory output, and no public leakage of raw final URLs, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub repository community-profile source-refresh now has strengthened response final-URL no-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/community/profile` jobs, requiring exact sanitized canonical route parity and leaving drifted jobs pending with no vault/search/relevant-memory artifact or public leakage of raw final URLs, prompts, renderer/source/html/script/data/API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub repository interaction-limits source-refresh now rejects final-URL query/fragment/userinfo/auth, repository, or lookalike-authority drift before response-body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/interaction-limits` jobs, requiring exact clean canonical route parity and leaving drifted jobs pending with no vault/search/relevant-memory artifact or public leakage of raw final URLs, prompts, API-auth fields, credentials, access-token markers, tokens, or secret-looking values.
- GitHub Actions repository secrets public-key source-refresh now has explicit final-URL before-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/actions/secrets/public-key` jobs, requiring exact clean canonical route parity and rejecting final-URL query/fragment/auth drift before response-body reads, JSON parsing, vault persistence, search indexing, or Spaces relevant-memory output. Drift leaves jobs pending with no vault/search/relevant-memory artifact and no public leakage of raw final URLs, raw prompts, renderer/source/html/script/data/API-auth fields, access-token markers, key material, credentials, tokens, or secret-looking values.
- GitHub repository custom-properties source-refresh now rejects final-URL query/fragment/userinfo/auth drift before response-body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/properties/values` jobs, requiring exact sanitized canonical route parity and leaving drifted jobs pending with no vault/search artifact or public leakage of property rows, raw final URLs, raw prompts, raw custom-property values, API-auth fields, credentials, tokens, access-token markers, or secret-looking values.
- GitHub Actions repository permissions final-URL drift hardening source-refresh now has explicit before-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/actions/permissions` metadata-only jobs: final-URL query/fragment/userinfo/auth drift fails closed before response-body reads, JSON parsing, vault persistence, search indexing, or Spaces relevant-memory output, requiring exact sanitized canonical route parity and leaving drifted jobs pending with no vault/search/relevant-memory artifact or public leakage of raw final URLs, raw prompts, values, API-auth fields, credentials, tokens/access-token markers, or secret-looking values.
- GitHub Actions repository variables source-refresh now rejects final-URL query/fragment/userinfo/auth drift before response-body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/actions/variables` jobs, requiring exact sanitized canonical route parity and leaving drifted jobs pending with no vault/search artifact or public leakage of variable rows, raw final URLs, raw prompts, values, API-auth fields, credentials, tokens, access-token markers, or secret-looking values.
- GitHub Actions organization variables source-refresh now rejects final-URL query/fragment/userinfo/auth, org, tail-route, or lookalike-authority drift before response-body reads for exact `GET https://api.github.com/orgs/{org}/actions/variables` jobs, preserving hidden clean fetch reconstruction and metadata-only public labels while leaving drifted jobs pending with no vault/search artifact or public leakage of variable values, raw final URLs, prompts, renderer/source/html/script/data/API-auth, credentials, tokens, or secret-looking fixtures.
- GitHub Actions organization permissions final-URL drift hardening source-refresh now requires exact response final-URL parity with the sanitized canonical `GET https://api.github.com/orgs/{org}/actions/permissions` route before any body read, JSON parsing, vault persistence, search indexing, or Spaces relevant-memory output; query/fragment/userinfo/auth, org drift, host/scheme/port/tail drift, and other noncanonical finals fail closed with the job pending/refresh failed, no vault/search/relevant-memory artifact, no body read, and no public leakage of raw final URLs, org-permission policy body sentinels, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, access-token markers, or secret-looking values.
- GitHub repository Actions artifacts source-refresh now rejects final-URL query/fragment/userinfo/auth or route drift before response-body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/actions/artifacts` jobs, requiring exact sanitized canonical route parity and leaving drifted jobs pending with no vault/search artifact or public leakage of artifact rows, raw final URLs, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, access-token markers, or secret-looking values.
- GitHub Actions repository self-hosted runners source-refresh now has explicit before-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/actions/runners` jobs: final-URL query/fragment/userinfo/auth drift fails closed before response-body reads, vault persistence, search indexing, or Spaces relevant-memory output, requiring exact sanitized canonical route parity and leaving drifted jobs pending with no vault/search/relevant-memory artifact or public leakage of raw final URLs, runner rows, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, access-token markers, or secret-looking values.
- GitHub repository forks source-refresh now rejects final-URL query/fragment/userinfo/auth, repository, lookalike-authority, non-HTTPS, or tail drift before response-body reads for exact `GET https://api.github.com/repos/{owner}/{repo}/forks` jobs, leaving drifted jobs pending with no vault/search/relevant-memory artifact or public leakage of raw final URLs, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values in result/jobs/catalog/search/relevant-memory output.
- GitHub traffic views/clones source-refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/traffic/{views|clones}` jobs: query/fragment/userinfo/auth, repository, lookalike-authority, or tail drift fails closed before any response-body read, leaving drifted jobs pending with no vault/search/relevant-memory artifact and no public leakage of raw final URLs, traffic rows, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub traffic popular paths source-refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/traffic/popular/paths` jobs: query/fragment/userinfo/auth drift fails closed before any response-body read, leaving drifted jobs pending with no vault/search/relevant-memory artifact and no public leakage of popular path rows, raw final URLs, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, access-token markers, or secret-looking values in result/jobs/catalog/search/relevant-memory output.
- GitHub traffic popular referrers source-refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/traffic/popular/referrers` jobs: query/fragment/userinfo/auth, repository, host, route, non-HTTPS, or tail drift fails closed before any response-body read, leaving drifted jobs pending with no vault/search/relevant-memory artifact and no public leakage of referrer rows, raw final URLs, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, access-token markers, or secret-looking values in result/jobs/catalog/search/relevant-memory output.
- GitHub check-suites source-refresh now has strengthened no-body-read + relevant-memory-empty final-URL drift evidence for exact `GET https://api.github.com/repos/{owner}/{repo}/commits/{sha}/check-suites` refreshes. Query/fragment/auth, userinfo, repository/SHA drift, raw prompt fragments, renderer/source/html/script/API-auth markers, check-suite app/body sentinels, credentials, tokens, and secret-looking fixtures fail closed before response-body reads with no vault/search/relevant-memory artifact and no public leakage in result/job/catalog/search/relevant-memory output.
- GitHub punch-card stats source-refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence: exact `GET https://api.github.com/repos/{owner}/{repo}/stats/punch_card` jobs require exact sanitized canonical API route parity, leaving query/fragment/userinfo/auth, other-repository, lookalike-authority, or tail-path drift pending/refresh-failed before response-body reads with no vault/search/relevant-memory artifact and no public leakage of raw final URLs, hostile body sentinels, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values in result/jobs/catalog/search/relevant-memory output.
- GitHub code-frequency stats source-refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence: exact `GET https://api.github.com/repos/{owner}/{repo}/stats/code_frequency` jobs require exact sanitized canonical API route parity, leaving query/fragment/userinfo/auth, other-repository, or tail-path drift pending before response-body reads with no vault/search/relevant-memory artifact and no public leakage of raw final URLs, hostile line-change body sentinels, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values in result/jobs/catalog/search/relevant-memory output.
- GitHub repository Events source-refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence: exact `GET https://api.github.com/repos/{owner}/{repo}/events` jobs require exact sanitized canonical API route parity, so query/fragment/userinfo/auth drift fails closed before response-body reads with no vault/search/relevant-memory artifact and no public leakage of event actors, hostile payload/body sentinels, raw final URLs, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub repository stargazers source-refresh now has tightened final-URL no-body-read + relevant-memory-empty evidence: exact `GET https://api.github.com/repos/{owner}/{repo}/stargazers` jobs require exact sanitized canonical API route parity, so query/fragment/userinfo/auth drift fails closed before response-body reads with no vault/search/relevant-memory artifact and no public leakage of stargazer logins, hostile body sentinels, raw final URLs, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub repository subscribers/watchers source-refresh now has strengthened final-URL no-body-read + relevant-memory-empty evidence: exact `GET https://api.github.com/repos/{owner}/{repo}/subscribers` jobs reject query/fragment/userinfo/auth, repository, lookalike-authority, non-HTTPS, or tail final-URL drift before response-body reads, leaving drifted jobs pending with no vault/search/relevant-memory artifact and no public leakage of subscriber logins, hostile body sentinels, raw final URLs, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub repository assignees source-refresh now has explicit final-URL no-body-read + relevant-memory-empty evidence: exact `GET https://api.github.com/repos/{owner}/{repo}/assignees` jobs reject final-URL query/fragment/userinfo/auth, repository, lookalike-authority, non-HTTPS, or tail drift before response-body reads, leaving drifted jobs pending with no vault/search/relevant-memory artifact and no public leakage of assignee logins, hostile body sentinels, raw final URLs, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, access-token markers, or secret-looking values.
- GitHub repository teams source-refresh now has explicit no-body-read + relevant-memory-empty evidence: exact `GET https://api.github.com/repos/{owner}/{repo}/teams` jobs reject final-URL query/fragment/userinfo/auth, repository, host/scheme, or tail drift before response-body reads, leaving drifted jobs pending with no vault/search/relevant-memory artifact or public leakage of team names/slugs, raw final URLs, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, access-token markers, or secret-looking values.
- GitHub repository webhooks source-refresh now has explicit final-URL no-body-read and relevant-memory-empty evidence: exact `GET https://api.github.com/repos/{owner}/{repo}/hooks` jobs require the response final URL to exactly match the sanitized canonical Webhooks route before body reads, JSON parsing, vault persistence, search artifacts, or Spaces relevant-memory results, so drifted webhook payloads fail closed with no public leakage of callback/config URLs, raw final URLs, prompts, scripts, credentials, tokens, or secret-looking values.
- GitHub repository Contents source-refresh now rejects final-URL route drift: exact `GET https://api.github.com/repos/{owner}/{repo}/contents[/path...]` jobs require HTTPS and exact sanitized canonical Contents route parity before JSON parsing or vault persistence, so drifted/different-repository or different-path payloads fail closed with no vault artifact and no public leakage of rejected content names, raw final URLs, query/fragment/userinfo auth, raw prompts, file bodies/content/encoding/download/html/git/_links metadata, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub repository README source-refresh now rejects final-URL route drift: exact `GET https://api.github.com/repos/{owner}/{repo}/readme` jobs require the response final URL to remain on the same sanitized canonical API README route before body parsing/vault persistence; drifted README responses fail closed with no vault artifact, metadata-only public evidence, and no public leakage of raw final URLs, query/fragment/userinfo auth, raw prompts, README content/body, content/encoding/download/html/git/_links metadata, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub repository license source-refresh now rejects final-URL route drift: exact `GET https://api.github.com/repos/{owner}/{repo}/license` jobs require HTTPS and the response final URL to remain on the same sanitized canonical `api.github.com` repository License route before body parsing or vault persistence, so redirected/noncanonical license payloads cannot leak raw final URLs, query/fragment/userinfo auth, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub repository Languages source-refresh now rejects final-URL route drift before response-body reads: exact `GET https://api.github.com/repos/{owner}/{repo}/languages` jobs require the response final URL to remain on the same sanitized `api.github.com` repository Languages route before any response-body read, JSON parsing, or vault persistence, so redirected/different-repository language payloads cannot produce vault/search/relevant-memory artifacts or leak raw route, language rows, query/fragment auth, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub repository branch-list source-refresh now rejects final-URL route drift before response-body reads: exact `GET https://api.github.com/repos/{owner}/{repo}/branches` jobs require HTTPS and exact sanitized canonical branch-list route parity before any response-body read, JSON parsing, vault persistence, search artifact, or Spaces relevant-memory result, so drifted/different-repository branch payloads fail closed with no public leakage of rejected branch names, raw final URLs, query/fragment/userinfo auth, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub repository tags source-refresh now rejects final-URL route drift before response-body reads: exact `GET https://api.github.com/repos/{owner}/{repo}/tags` jobs require HTTPS and the response final URL to exactly match the sanitized canonical tags route before any response-body read, JSON parsing, or vault persistence, so redirected/different-repository tag payloads fail closed without vault/search/relevant-memory artifacts or leakage of raw final URLs, rejected tag names, query/fragment/userinfo auth, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub repository single-invitation source-refresh now supports exact `GET https://api.github.com/repos/{owner}/{repo}/invitations/{invitation_id}` JSON objects with metadata-only public labels such as `github repository invitation capy/spaces #701`, hidden clean-route fetches, bounded invitation metadata, exact id parity, and fail-closed rejection for list/feed/text fallback, route abuse, dangerous nested aliases, non-exact URL-ish metadata keys, raw API/auth/prompt/renderer/source/html/script/data/body fields, credentials, tokens, and secret-looking values.
- GitHub dependency graph SBOM source-refresh now supports exact `GET https://api.github.com/repos/{owner}/{repo}/dependency-graph/sbom` JSON objects with metadata-only public labels such as `github dependency graph sbom capy/spaces`, hidden clean-route fetches, bounded package counts/previews, and fail-closed route/content/payload redaction.
- GitHub Pages latest-build source-refresh now supports exact `GET https://api.github.com/repos/{owner}/{repo}/pages/builds/latest` JSON objects with metadata-only public labels such as `github pages latest build capy/spaces`; workers fetch only the hidden clean route, persist only safe repo/build id/status/SHA-prefix/pusher/timestamps/duration/error-presence metadata, and focused coverage now proves final-URL drift to concrete build ids or query/fragment auth material fails closed before any body read with no vault/search/relevant-memory artifact or unsafe public leakage of raw URL/body/prompt/renderer/source/html/script/API-auth/credential/token fixtures.
- GitHub Pages deployment-status source-refresh now supports exact `GET https://api.github.com/repos/{owner}/{repo}/pages/deployments/{pages_deployment_id}` JSON objects with metadata-only public labels such as `github pages deployment capy/spaces 123` or `github pages deployment capy/spaces abc123...`; workers fetch only the hidden clean route, persist only repo, safe deployment id/SHA prefix, allow-listed status, and safe timestamps, and focused coverage now proves final-URL query/fragment auth drift fails closed before body reads with no vault/search/relevant-memory artifact or raw URL/auth/prompt/renderer/source/html/script/data/body/secret leakage.
- GitHub Dependabot organization single private-name source-refresh now supports exact `GET https://api.github.com/orgs/{org}/dependabot/secrets/{secret_name}` JSON objects with metadata-only public labels such as `github dependabot organization private name capy CAPY_MODE`; workers reconstruct only the hidden clean route from safe org/private-name metadata, persist only org, safe private-name identifier, allow-listed visibility, selected-repository count, and safe timestamps, and fail closed for token-shaped source IDs, list/public-key/repositories adjacent routes, text/JSON Feed fallback, final-URL drift, raw API/auth/query/fragment/userinfo markers, raw prompts, renderer/source/html/script/data/body/API-auth fields, key material, credentials, tokens, and secret-looking values.
- GitHub Dependabot organization private-name selected-repositories source-refresh now supports exact `GET https://api.github.com/orgs/{org}/dependabot/secrets/{secret_name}/repositories` JSON objects with metadata-only public labels such as `github dependabot organization private name repositories capy CAPY_MODE`; workers reconstruct only the hidden clean route from safe org/private-name metadata, persist only org, safe private-name identifier, repository count, and bounded safe repository id/name/full-name/private previews, and fail closed for raw route/auth/prompt/renderer/source/html/script/data/body/API-auth fields, selected-repositories URL/key-material markers, non-empty temp clone tokens, unsafe legacy source IDs, queue public-output leakage, adjacent routes, text/JSON Feed fallback, row-count drift, oversized rows, and final-URL drift.
- GitHub Dependabot organization private-name source-refresh now supports exact `GET https://api.github.com/orgs/{org}/dependabot/secrets` JSON objects with metadata-only public labels such as `github dependabot organization private names capy`; workers reconstruct only the hidden clean route from safe org metadata/public labels, persist only org, private-name count, bounded safe names, allow-listed visibility, selected-repository counts, and safe timestamps, and fail closed for raw-route abuse, JSON Feed/text fallback, count drift, oversized/malformed rows, adjacent Dependabot routes, and key/value/auth/prompt/renderer fields.
- GitHub Actions organization selected-actions source-refresh now supports exact `GET https://api.github.com/orgs/{org}/actions/permissions/selected-actions` JSON objects with metadata-only public labels such as `github actions organization selected actions capy`; workers reconstruct only the hidden clean canonical route, persist only safe org, `github_owned_allowed`, `verified_allowed`, bounded safe `patterns_allowed`, and pattern counts, and fail closed for text/JSON Feed fallback, route abuse, adjacent repo/org permissions/workflow routes, final URL drift, raw API URLs, `selected_actions_url`, query/fragment/userinfo/auth, raw prompts, renderer/source/html/script/data/body/API-auth fields, credentials, tokens, and secret-looking values.
- GitHub Actions organization single-secret/private-name source-refresh now supports exact `GET https://api.github.com/orgs/{org}/actions/secrets/{secret_name}` JSON objects with metadata-only public labels such as `github actions organization private name capy CAPY_MODE`; workers reconstruct only the clean fetch route from safe org/private-name metadata, persist only org, safe private-name identifier, allow-listed visibility, selected-repository count, and safe timestamps, and fail closed for list/public-key/selected-repositories/repo-scoped adjacent routes, query/fragment/userinfo/port/lookalike/uppercase authorities, encoded/extra tails, final URL drift, non-JSON/text/JSON Feed fallback payloads, raw API URLs, raw prompts, renderer/source/html/script/data/body/API-auth fields, encrypted/key material, credentials, tokens, and secret-looking values.

- GitHub Actions organization variables source-refresh now supports exact `GET https://api.github.com/orgs/{org}/actions/variables` JSON objects with metadata-only public labels such as `github actions organization variables capy`; workers reconstruct only the clean fetch route, persist bounded org/variable count plus safe variable names, allow-listed visibility, selected repository count/id/name metadata when safe, and safe timestamps, while documented variable `value` and `selected_repositories_url` values stay omitted and text/JSON Feed bypasses, lookalike hosts, malformed routes, dirty source ids/display names, raw API/auth/prompt/renderer/source/html/script/data/body/secret leakage fail closed.
- GitHub Actions organization permissions source-refresh now supports exact `GET https://api.github.com/orgs/{org}/actions/permissions` JSON objects with metadata-only public labels such as `github actions organization permissions capy`; workers reconstruct only the clean fetch route, persist only safe org plus allow-listed `enabled_repositories`/`allowed_actions` policy metadata, and fail closed for route abuse, JSON Feed/text fallback, final URL drift, and selected-actions/workflow/repo-scoped adjacent routes.
- GitHub Actions organization secrets source-refresh now supports exact `GET https://api.github.com/orgs/{org}/actions/secrets` JSON objects with metadata-only public labels such as `github actions organization secrets capy`; workers reconstruct only the clean fetch route, persist bounded org/private-name count plus safe secret names, allow-listed visibility, selected repository counts, and safe timestamps, while `selected_repositories_url`, encrypted values, key material, raw API/auth/query/fragment/userinfo markers, raw prompts, renderer/source/html/script/data/body fields, credentials, tokens, and secret-looking fixture values stay omitted or fail closed before public receipts, catalog/jobs, search, relevant-memory, or vault output.
- GitHub Actions organization self-hosted runners source-refresh now supports exact `GET https://api.github.com/orgs/{org}/actions/runners` JSON objects with metadata-only public labels such as `github actions organization runners capy`; workers reconstruct only the clean fetch route, persist bounded safe org/runner count plus runner id/name/status/busy/os/architecture/label names, and fail closed for route abuse, JSON Feed/text fallback, count drift, unsafe row fields/values, oversized rows, final-URL drift, and raw API/auth/prompt/renderer/source/script/secret leakage.
- GitHub Actions organization runner-groups source-refresh now supports exact `GET https://api.github.com/orgs/{org}/actions/runner-groups` JSON objects with metadata-only public labels such as `github actions organization runner groups capy`; workers reconstruct only the clean fetch route, persist bounded org/group count plus safe runner-group id/name/visibility/booleans/selected workflows, and fail closed for adjacent-route stealing, route abuse, JSON Feed/text fallback, count drift, unsafe fields/values, final-URL drift, and raw API/auth/prompt/renderer/source/html/script/data/body/secret leakage.
- GitHub Actions organization runner-group runners source-refresh now supports exact `GET https://api.github.com/orgs/{org}/actions/runner-groups/{runner_group_id}/runners` JSON objects with metadata-only public labels such as `github actions organization runner group runners capy group 201`; workers reconstruct only the clean fetch route, persist bounded org/group id/runner count plus safe runner id/name/status/busy/os/architecture/labels, keep adjacent org runner-groups/repositories/runners/downloads and repo-scoped runner-group routes distinct, and fail closed for route abuse, JSON Feed/text fallback, count drift, unsafe top-level/runner/label fields, final-URL drift, and raw API/auth/prompt/renderer/source/html/script/data/body/secret leakage.
- GitHub Actions org runner-group selected repositories source-refresh now rejects final-URL drift before response-body reads: exact `GET https://api.github.com/orgs/{org}/actions/runner-groups/{runner_group_id}/repositories` jobs require exact clean canonical route parity and fail closed without vault/search leakage for query/fragment/userinfo/auth, different org/group/tail, lookalike authority, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking values.
- GitHub Actions org runner-group selected repositories source-refresh now supports exact `GET https://api.github.com/orgs/{org}/actions/runner-groups/{runner_group_id}/repositories` JSON pages with metadata-only public labels such as `github actions runner group repositories capy group 201`; workers reconstruct only the clean fetch route, project realistic repository rows to safe id/name/full-name/private summaries, ignore documented URL/template metadata, enforce org/name consistency, and fail closed without raw API/auth/query/fragment/userinfo, raw prompts, renderer/source/html/script/data/body fields, non-empty temp clone tokens, credentials, tokens, or secret-looking values in receipts, jobs, catalog, search, relevant-memory hits, or vault output.
- GitHub Actions single runner-group source-refresh now supports exact `GET https://api.github.com/repos/{owner}/{repo}/actions/runner-groups/{runner_group_id}` JSON objects with metadata-only public labels such as `github actions runner group capy/spaces group 201`; workers reconstruct only the clean fetch route, accept only safe runner-group fields with route id parity, persist metadata-only summaries, and fail closed without raw API/auth/prompt/renderer/source/script/secret leakage for JSON Feed/text/html/source/data bypasses, unsafe fields/visibility/workflows, id/final-URL drift, or route abuse.
- GitHub Actions runner-group runners source-refresh now supports exact `GET https://api.github.com/repos/{owner}/{repo}/actions/runner-groups/{runner_group_id}/runners` JSON objects with public labels such as `github actions runner group runners capy/spaces group 201`; workers reconstruct the clean fetch route, persist only metadata-only repo/group id/runner count plus bounded safe runner id/name/status/busy/os/architecture/labels, and fail closed for route abuse, JSON Feed/text fallback, malformed payloads, count drift, unsafe fields, raw API routes, auth/query/fragment/userinfo, prompts, renderer/source/html/script/data fields, tokens, and secret-looking values.
- GitHub Actions runner labels source-refresh now supports exact `GET https://api.github.com/repos/{owner}/{repo}/actions/runners/{runner_id}/labels` JSON objects with hidden clean fetch reconstruction and public labels such as `github actions runner labels capy/spaces runner 101`; persisted output is metadata-only (repo, runner id, label count, bounded safe names/types), while route abuse, JSON Feed/text fallbacks, unsafe rows/fields, count drift, >100 labels, final URL drift, auth/query/fragment markers, raw prompts, renderer/source/html/script/API-auth fields, tokens, and secret-looking values fail closed without public leakage.
- GitHub Actions organization runner labels source-refresh now supports exact `GET https://api.github.com/orgs/{org}/actions/runners/{runner_id}/labels` JSON objects with hidden clean fetch reconstruction and public labels such as `github actions organization runner labels capy runner 101`; persisted output is metadata-only (org, runner id, label count, bounded safe names/types), while adjacent org/repo runner routes, route abuse, JSON Feed/text fallbacks, unsafe rows/fields, count drift, >100 labels, response final-URL drift before any body read, auth/query/fragment/userinfo markers, raw prompts, renderer/source/html/script/data/API-auth fields, tokens, and secret-looking values fail closed with no vault/search/relevant-memory artifact and no public leakage in receipts, jobs, catalog, search, or vault output.
- GitHub workflow-runs source-refresh now rejects exact JSON objects whose `workflow_runs` arrays exceed 100 rows for both `GET https://api.github.com/repos/{owner}/{repo}/actions/runs` and `GET https://api.github.com/repos/{owner}/{repo}/actions/workflows/{workflow_id}/runs`. Bounded/empty workflow-run metadata remains accepted and metadata-only, while oversized payloads fail closed with no vault artifact and no public leakage of rejected run rows, API URLs, auth/query/fragment markers, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, `logs_url`, `jobs_url`, or secret-looking fixtures; unscoped/scoped public labels continue to preserve hidden clean fetch origins.

- GitHub Actions single artifact source-refresh now supports exact `GET https://api.github.com/repos/{owner}/{repo}/actions/artifacts/{artifact_id}` JSON objects with hidden clean canonical fetch origins and public labels such as `github actions artifact capy/spaces 201`; persisted summaries are metadata-only (repo, id, safe name, size, expired flag, safe timestamps), while id drift, JSON Feed fallbacks, unsafe names, malformed/lookalike/port/uppercase/encoded-tail routes, raw API URLs, query/fragment auth, prompts, renderer/source/html/script/data/body fields, credentials, tokens, and secret-looking fixtures fail closed or remain omitted from public output.
- GitHub Actions repository and workflow-run artifact source-refresh now rejects exact `artifacts` arrays above 100 rows for `https://api.github.com/repos/{owner}/{repo}/actions/artifacts` and `https://api.github.com/repos/{owner}/{repo}/actions/runs/{run_id}/artifacts`. Bounded artifact metadata remains metadata-only, while oversized payloads fail closed with no vault artifact and no public leakage of rejected artifact rows, auth/query/fragment markers, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.
- GitHub repository security-advisories source-refresh now rejects exact `https://api.github.com/repos/{owner}/{repo}/security-advisories` JSON arrays above 100 rows. Bounded advisory metadata remains metadata-only, while oversized payloads fail closed with no vault artifact and no public leakage of raw advisory/security fixture rows, GHSA/CVE/sentinel details, API URLs/auth markers, raw prompts, renderer/source/html/script/API-auth fields, vulnerabilities, descriptions, private flags, credentials, tokens, or secret-looking fixtures.
- GitHub Dependabot alerts list source-refresh now rejects exact `https://api.github.com/repos/{owner}/{repo}/dependabot/alerts` JSON arrays above 100 rows. Bounded alert metadata remains metadata-only, while oversized payloads fail closed with no vault artifact and no public leakage of raw alert rows, API URLs/auth markers, raw prompts, renderer/source/html/script/API-auth fields, advisory/vulnerability details, CVE/GHSA ids, version ranges, credentials, tokens, or secret-looking fixtures.
- GitHub code scanning alerts list source-refresh now rejects exact `https://api.github.com/repos/{owner}/{repo}/code-scanning/alerts` JSON arrays above 100 rows. Bounded alert metadata remains metadata-only, while oversized payloads fail closed with no vault artifact and no public leakage of rejected alert metadata, raw API routes, auth/query/fragment markers, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, or secret-looking fixtures.
- GitHub repository contents source-refresh now rejects canonical `https://api.github.com/repos/{owner}/{repo}/contents[/path]` directory-list JSON arrays above 100 rows. Bounded contents metadata and single-object payloads remain metadata-only, while oversized list payloads fail closed with no vault artifact and no public leakage of raw/rejected rows, item count 101, API URLs/auth markers, raw prompts, renderer/source/data/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.
- Source registration receipts now expose fixed server-generated metadata-only trust labels (`metadata_only`, `public_output`, advisory/no-authority fields, and gate-bypass false) before source-refresh jobs are queued, while omitting caller-forged trust labels, raw prompt, renderer/source/html/script/data/API-auth, credential, userinfo, query/fragment auth, and secret-like registration fields.
- Source-refresh queue job receipts now expose fixed per-job metadata-only trust labels (`metadata_only`, `public_output`, advisory/no-authority fields, and gate-bypass false) and the product-home Source refresh queue renders concise `metadata-only` / `advisory only` labels without trusting caller payloads or leaking raw origin, prompt, renderer/source/html/script/API-auth, credential, or secret-like job fields.
- Source catalog responses now include a top-level server-generated Memory Tree advisory/no-authority envelope from `source_catalog()` / `GET /api/capy-memory/source/catalog`, so connector/source freshness inspection is explicitly metadata-only advisory context and cannot let caller/source/job-forged `trusted_system_memory`, context-authority, bypass flags, raw prompts, renderer/source/html/script/API-auth fields, credentials, or secret-looking fixtures understate required gates.
- Source-refresh job-list responses now also include a top-level server-generated Memory Tree advisory/no-authority envelope from `list_source_refresh_jobs()` / `GET /api/capy-memory/source/jobs`, so queue inspection itself is explicitly metadata-only advisory context and cannot let caller/job-forged `trusted_system_memory`, context-authority, bypass flags, raw prompts, renderer/source/html/script/API-auth fields, credentials, or secret-looking fixtures understate required gates.
- GitHub environments source-refresh now rejects exact `/repos/{owner}/{repo}/environments` payloads whose `environments` array contains more than 100 rows. Bounded environment metadata remains accepted, while oversized payloads fail closed with no vault artifact and no public leakage of rejected environment names, URLs, protection rules, deployment branch policies, raw prompts, renderer/html/script/API-auth fields, credentials, or secret-looking fixtures.
- GitHub fork-list source-refresh now rejects exact `https://api.github.com/repos/{owner}/{repo}/forks` JSON arrays above 100 rows. Bounded fork metadata remains accepted, while oversized payloads fail closed with no vault artifact and no public leakage of rejected fork row metadata, URL/auth markers, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.
- GitHub stargazers source-refresh now rejects exact `https://api.github.com/repos/{owner}/{repo}/stargazers` JSON arrays above 100 rows. Bounded stargazer metadata remains accepted, while oversized payloads fail closed with no vault artifact and no public leakage of rejected logins/counts, URL/auth markers, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.
- GitHub subscribers/watchers source-refresh now rejects exact `https://api.github.com/repos/{owner}/{repo}/subscribers` JSON arrays above 100 rows. Bounded and empty subscriber metadata remain accepted, while oversized payloads fail closed with no vault artifact and no public leakage of rejected subscriber logins/counts, URL/auth markers, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.
- GitHub assignees source-refresh now rejects exact `https://api.github.com/repos/{owner}/{repo}/assignees` JSON arrays above 100 rows. Bounded assignee metadata remains accepted, while oversized payloads fail closed with no vault artifact and no public leakage of rejected assignee logins/counts, URL/auth markers, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.
- GitHub collaborators source-refresh now rejects exact `https://api.github.com/repos/{owner}/{repo}/collaborators` JSON arrays above 100 rows. Bounded collaborator metadata remains accepted and reconstructed only from safe repository path, count, login/id, optional role, and site-admin flag while oversized payloads fail closed with no vault artifact or public leakage of rejected collaborator logins/counts, URL/auth markers, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.
- GitHub repository rulesets source-refresh now rejects exact `https://api.github.com/repos/{owner}/{repo}/rulesets` JSON arrays above 100 rows, failing closed with no vault artifact and no public leakage of raw/rejected ruleset metadata, URLs/auth markers, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, bypass actor/rule condition details, or secret-looking fixtures.
- GitHub milestone-list source-refresh now rejects exact `/repos/{owner}/{repo}/milestones` JSON arrays above 100 rows, failing closed with no vault artifact and no raw/rejected milestone metadata leak.
- GitHub commit-list source-refresh now rejects exact `/repos/{owner}/{repo}/commits` JSON arrays above 100 rows; oversized payloads fail closed with no vault artifact and no public leakage of raw/rejected commit metadata, URL/auth markers, raw prompts, renderer/source/script/API-auth fields, credentials, or secret-looking fixtures.
- GitHub repository tags source-refresh now rejects exact `GET https://api.github.com/repos/{owner}/{repo}/tags` JSON arrays above 100 rows; empty and bounded tag metadata remains accepted, while oversized payloads fail closed with no vault artifact and no public leakage of raw/rejected tag metadata, query/fragment auth markers, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.
- GitHub repository labels and issue-labels source-refresh now reject exact `GET https://api.github.com/repos/{owner}/{repo}/labels` and `/repos/{owner}/{repo}/issues/{number}/labels` JSON arrays above 100 rows; oversized payloads fail closed with no vault artifact and no public leakage of raw label row metadata, URL/auth markers, prompts, renderer/source/script/API-auth fields, credentials, or secret-looking fixtures.
- GitHub branch-list source-refresh now rejects exact `/repos/{owner}/{repo}/branches` JSON arrays above 100 rows, preserving bounded metadata-only branch refreshes while oversized payloads fail closed without vault artifacts, auth/query/fragment markers, raw prompts, renderer/source/html/script/API-auth fields, credentials, or rejected branch metadata in public output.
- GitHub commit-associated PR source-refresh now rejects exact `/repos/{owner}/{repo}/commits/{40-hex-sha}/pulls` JSON arrays above 100 rows, preserving metadata-only bounded refreshes while oversized payloads fail closed without vault artifacts or raw PR bodies, URLs/API-auth markers, raw prompts, renderer/source/html/script fields, credentials, or rejected row metadata in public output.
- GitHub issue comments, pull review comments, and commit comments source-refresh now fail closed when exact comment-list JSON arrays exceed 100 rows, leaving oversized refreshes pending without vault artifacts while public result/job/search output omits raw comment bodies, URLs/auth markers, prompts, renderer/source/html/script/API-auth fields, credentials, and rejected row metadata.
- GitHub pull-review source-refresh now rejects exact `GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews` JSON arrays above 100 review rows; oversized review payloads fail closed with no vault artifact and no public leakage of rejected review metadata, URLs/auth markers, prompts, renderer/source/script/API-auth fields, credentials, or secret-looking values.
- GitHub issue reactions source-refresh now rejects exact `/repos/{owner}/{repo}/issues/{number}/reactions` JSON arrays over 100 rows; oversized payloads fail closed with no vault artifact and no public leakage of rejected reaction metadata, URL/auth markers, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.
- GitHub issue-comment reactions source-refresh now rejects exact `/repos/{owner}/{repo}/issues/comments/{comment_id}/reactions` JSON arrays over 100 rows; oversized payloads fail closed with no vault artifact and no public leakage of rejected reactor metadata, URL/auth markers, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.
- GitHub pull review comment reactions source-refresh now rejects exact `https://api.github.com/repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions` JSON arrays over 100 rows; oversized payloads fail closed with no vault artifact and no public leakage of rejected reactor metadata, URL/auth markers, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.
- GitHub workflow-list source-refresh now fails closed when exact `/repos/{owner}/{repo}/actions/workflows` JSON payloads contain more than 100 `workflows` rows, leaving oversized refreshes pending without vault artifacts while public result/job/search output omits raw workflow URLs, auth/query/fragment markers, prompts, renderer/source/html/script/API-auth fields, credentials, secret-looking fixtures, and rejected row metadata.
- GitHub check-runs source-refresh now fails closed when exact `/repos/{owner}/{repo}/commits/{sha}/check-runs` JSON payloads contain more than 100 `check_runs`, leaving oversized refreshes pending without vault artifacts while public result/job/search output omits raw output, details/html/pull-request URLs, prompts, renderer/script/API-auth fields, credentials, and secret-looking fixtures.
- GitHub check-suites source-refresh now fails closed when exact `/repos/{owner}/{repo}/commits/{sha}/check-suites` JSON payloads contain more than 100 `check_suites` rows, leaving oversized refreshes pending without vault artifacts while public result/job/search/catalog output omits raw suite row fields, URLs, auth, prompts, renderer/source/html/script/API-auth fields, credentials, and secrets.
- GitHub repository-events source-refresh now fails closed when exact `/repos/{owner}/{repo}/events` JSON arrays contain more than 100 rows, leaving oversized refreshes pending without vault artifacts while public result/job/search output omits raw event row fields, payload/body, API URLs/query/fragment, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, and secret-looking fixtures.
- GitHub issue-events source-refresh now rejects exact `/repos/{owner}/{repo}/issues/{number}/events` arrays above 100 rows, no vault artifact, no public leakage of raw event row metadata, URL/auth markers, raw prompts, renderer/source/script/API-auth fields, credentials, or secret-looking fixtures.
- GitHub issue timeline source-refresh now rejects exact `https://api.github.com/repos/{owner}/{repo}/issues/{number}/timeline` JSON arrays above 100 rows, failing closed with no vault artifact and no public leakage of raw/rejected timeline metadata, URL/auth markers, prompts, renderer/source/script fields, credentials, or secrets.
- GitHub check-run annotations source-refresh now fails closed on unbounded annotation arrays over 100 rows, leaving oversized refreshes pending without writing vault artifacts while public receipts/job listings continue to omit raw annotation messages, paths, raw details, API routes, query/fragment auth, raw prompts, and secret-looking fixtures.
- GitHub release-assets source-refresh now fails closed on exact `/repos/{owner}/{repo}/releases/{release_id}/assets` JSON arrays over 100 rows, leaving oversized refreshes pending without writing vault artifacts while public receipts/job listings/search continue to omit raw asset row metadata, download/API URLs, query/fragment auth, raw prompts, renderer/source/html/script/API-auth fields, credentials, and secret-looking fixtures.
- GitHub release-reactions source-refresh now fails closed on exact `https://api.github.com/repos/{owner}/{repo}/releases/{release_id}/reactions` JSON arrays over 100 rows, leaving oversized refreshes pending without a vault artifact while public result/job/search output omits raw reaction row metadata, URLs, auth markers, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, and secret-looking fixtures.
- GitHub release-list source-refresh now fails closed on exact `/repos/{owner}/{repo}/releases` JSON arrays over 100 rows, leaving oversized refreshes pending without a vault artifact while public result/job/search output omits raw release bodies/URLs/assets, API-auth markers, raw prompts, renderer/source/html/script fields, credentials, secrets, and rejected row metadata.
- GitHub issue-list source-refresh now fails closed on exact `/repos/{owner}/{repo}/issues` JSON arrays over 100 rows, leaving oversized refreshes pending without writing vault artifacts while public receipts/job listings/search continue to omit raw issue bodies, URLs, auth markers, prompts, renderer/source/html/script/API-auth fields, credentials, and secret-looking fixtures.
- GitHub PR-list source-refresh now fails closed on exact `/repos/{owner}/{repo}/pulls` JSON arrays over 100 rows, leaving oversized refreshes pending without writing vault artifacts while public receipts/job listings/search continue to omit raw PR bodies, URLs, head/base refs, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, and secret-looking fixtures.
- GitHub PR commit-list source-refresh now fails closed on exact `/repos/{owner}/{repo}/pulls/{number}/commits` JSON arrays over 100 rows, leaving oversized refreshes pending without vault artifacts while public output omits raw/rejected commit metadata, URLs/auth markers, prompts, renderer/source/html/script/API-auth fields, credentials, tokens, and secrets.
- GitHub PR requested-reviewers source-refresh now fails closed when either `users` or `teams` exceeds 100 rows, leaving oversized refreshes pending without vault artifacts or raw reviewer/team metadata leakage.
- GitHub repository teams source-refresh now fails closed on exact `https://api.github.com/repos/{owner}/{repo}/teams` JSON arrays over 100 rows, leaving oversized refreshes pending without vault artifacts or search hits while public result/job/search output omits rejected team names/slugs/ids, URL/auth markers, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, tokens, and secret-looking fixtures.
- GitHub deployments list source-refresh now fails closed on unbounded `/repos/{owner}/{repo}/deployments` arrays over 100 rows, leaving oversized refreshes pending without writing vault artifacts while public receipts/job listings/search continue to omit raw deployment row details, descriptions, payloads, URLs, prompts, renderer/source/html/script/data/API-auth fields, credentials, and secret-looking fixtures.
- GitHub deployment-statuses source-refresh now fails closed on unbounded `/repos/{owner}/{repo}/deployments/{deployment_id}/statuses` arrays over 100 rows, leaving oversized refreshes pending without writing vault artifacts while public receipts/job listings/search continue to omit raw status row metadata, descriptions, target/log/deployment URLs, payloads, raw prompts, renderer/script/API-auth fields, credentials, and secret-looking fixtures.
- GitHub workflow-run pending-deployments source-refresh now fails closed when exact `/repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments` JSON arrays exceed 100 rows, leaving oversized refreshes pending without a vault artifact while public result/job/search output omits raw deployment/environment/reviewer row metadata, URLs, prompts, renderer/source/script/API-auth fields, credentials, tokens, and secret-looking fixtures.
- GitHub environment deployment branch-policies source-refresh now fails closed when exact `/repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies` JSON payloads contain more than 100 `branch_policies` rows, leaving oversized refreshes pending without vault artifacts while public catalog/job/search/result output omits rejected policy names/ids, URLs/auth markers, prompts, renderer/source/html/script/API-auth fields, credentials, and secret-looking fixtures.
- XML sitemap URLSet source-refresh metadata is implemented for allowlisted HTTPS sitemap origins: Capy persists only safe URL counts, bounded host/path-only entry previews, optional safe `lastmod` dates, and sanitized origin provenance while omitting credentials, query/fragment markers, auth-like query parameters, raw XML bodies, renderer/source/html/script/API-auth fields, raw prompts, and secret-looking fixture values; sitemap payloads with zero safe `<loc>` entries fail closed without writing vault artifacts.
- GitHub code-scanning alerts list source-refresh public-origin hardening now preserves a hidden clean canonical API fetch origin for exact `/repos/{owner}/{repo}/code-scanning/alerts` registrations while public receipts/catalog/jobs/search/vault output use only labels such as `github security scanning alerts capy/spaces`; dirty query/fragment auth and raw-prompt markers are stripped before queueing, runs fetch the clean API route with `Accept: application/json`, and malformed/lookalike/userinfo/port/uppercase/encoded/extra-tail routes continue to fail closed without leaking raw API routes, renderer/source/html/script/data/API-auth fields, tokens, or secret-looking fixtures.
- GitHub repository security-advisories source-refresh public-output hardening now keeps receipts, source catalog, and job-list surfaces on labels such as `github security advisories capy/spaces` while preserving hidden clean canonical fetch origins for exact `/repos/{owner}/{repo}/security-advisories` JSON refreshes; dirty query/fragment auth/raw-prompt/API-key markers and advisory body/detail fields stay out of public control-plane output, and due requeue preserves the hidden fetch origin from safe metadata.
- GitHub single Dependabot alert source-refresh metadata is implemented for exact `/repos/{owner}/{repo}/dependabot/alerts/{alert_number}` JSON object payloads. Dirty registrations and due requeues preserve a hidden clean canonical API fetch origin while public receipts use labels such as `github dependabot alert capy/spaces #17`; persisted summaries include only alert number, allow-listed state, dependency package ecosystem/name/manifest path, and allow-listed severity. Ignored advisory/vulnerability details, CVE/GHSA ids, version ranges, and raw URLs from realistic GitHub alert objects are omitted from persisted/search/vault output, while JSON Feed/generic JSON bypasses, payload number mismatches, route-bearing URL mismatches, malformed/lookalike/userinfo/port/uppercase/encoded/extra-tail origins, hostile prompt/script/secret/API-auth markers, renderer/source/html/script/data fields, tokens, and secret-looking fixtures fail closed. Legacy due-refresh jobs with unsafe raw Dependabot alert candidates in payload or source rows now fail closed to local `capy-memory://...` labels instead of reconstructing hidden fetch origins from nearby public labels.
- GitHub Dependabot alerts list source-refresh registration now hardens exact `/repos/{owner}/{repo}/dependabot/alerts` JSON list sources: dirty registrations publish only labels such as `github dependabot alerts capy/spaces`, preserve a hidden clean canonical fetch origin plus `github_dependabot_alerts` kind in the job payload, fetch with `Accept: application/json`, and keep raw API URLs/query/fragment auth markers, advisory bodies/details, CVE/GHSA ids, renderer/source/html/script fields, raw prompts, and secret-looking fixture values out of public receipt/search/catalog/result/vault output.
- GitHub code-scanning default-setup source-refresh metadata is implemented for exact `/repos/{owner}/{repo}/code-scanning/default-setup` JSON object payloads. Dirty registrations and due requeues preserve a hidden clean canonical API fetch origin while public receipts use labels such as `github code scanning default setup capy/spaces`; persisted summaries include only safe repository path, setup state/status, bounded languages, query suite, schedule, runner type/label, threat model, and safe updated timestamp, and fail closed for JSON Feed/text fallback, unsafe ignored fields, malformed/lookalike/userinfo/port/uppercase/encoded/extra-tail routes, and final-URL drift without exposing raw URLs, auth/query/fragment, prompts, scripts, API-auth markers, tokens, or secret-like values.
- GitHub code-scanning analyses source-refresh metadata is implemented for exact `/repos/{owner}/{repo}/code-scanning/analyses` JSON list payloads. Dirty registrations preserve a hidden clean canonical API fetch origin while public receipts use labels such as `github code scanning analyses capy/spaces`; persisted summaries include only bounded analysis count plus up to five safe id/ref/category/analysis-key/commit-prefix/result-count/rule-count/tool/timestamp/deletable fields and fail closed for JSON Feed/text fallbacks, malformed/lookalike/userinfo/port/encoded/extra-tail routes, unsafe nested keys/values, raw URLs, SARIF IDs, API-auth fields, prompts, tokens, and secret-like markers.
- GitHub code-scanning single-analysis source-refresh metadata is implemented for exact `/repos/{owner}/{repo}/code-scanning/analyses/{analysis_id}` JSON object payloads. Dirty registrations and due requeues preserve a hidden clean canonical API fetch origin while public receipts use labels such as `github code scanning analysis capy/spaces #101`; persisted summaries include only safe analysis id, ref, category, analysis key, commit prefix, result/rule counts, tool name/version, timestamp, and deletable metadata, while validation-only URL/SARIF/environment/error/warning/tool GUID fields are checked but never rendered. Query/fragment fetch origins plus response final-URL auth/query/fragment, repository drift, analysis-id drift, and tail-route drift now fail closed before any body read; text/JSON Feed fallbacks, mismatched ids/URLs, non-empty errors/warnings, unsafe environment or nested tool fields, userinfo/ports/lookalikes, encoded/extra-tail routes, prompts, renderer/source/html/script/data/API-auth fields, tokens, and secret-looking fixtures fail closed before fetch, catalog, search, or vault output.
- GitHub code-scanning alert-instances source-refresh metadata is implemented for exact `/repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/instances` JSON list payloads. Dirty registrations and due requeues preserve a hidden clean canonical API fetch origin while public receipts use labels such as `github code scanning alert instances capy/spaces #17`; persisted summaries include only bounded instance count plus safe ref/state/category/analysis/line/commit-prefix metadata and fail close for text/JSON Feed fallbacks, malformed or unsafe rows, message/path/source/body/data/html/script/API-auth fields, query/fragment/userinfo/port/lookalike/encoded/extra-tail origins, raw prompts, tokens, and secret-looking fixtures before fetch, catalog, search, or vault output.
- GitHub single code-scanning alert source-refresh metadata is implemented for exact `/repos/{owner}/{repo}/code-scanning/alerts/{alert_number}` JSON object payloads. Registrations and due requeues preserve a hidden canonical API fetch origin while public receipts use labels such as `github code scanning alert capy/spaces #17`; the parser requires payload number and route-bearing `url`/`html_url`/`instances_url` fields to match the requested repo/alert exactly, persists only bounded alert state/rule/severity/tool/timestamp metadata, and fail-closes text/feed fallbacks, unsafe ignored fields, mismatched payload routes, final-URL drift, userinfo/ports/lookalikes, encoded/extra-tail origins, raw prompts, renderer/source/html/script/data/API-auth fields, tokens, and secret-looking fixtures before catalog/search/vault output.
- GitHub single secret-scanning alert source-refresh metadata is implemented for exact `/repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}` JSON object payloads. Registrations persist public labels such as `github secret scanning alert {owner}/{repo} #{alert_number}` while live fetches use the hidden clean API origin plus `hide_secret=true`; summaries persist only bounded alert metadata (number/state/type/provider/validity/leak flags/location/assignee/timestamps/push-protection booleans) and fail closed before fetch or vault output for feed/text fallbacks, non-empty `secret`, raw URLs/comments, API-auth fields, raw prompts, scripts, query/fragment/userinfo/port/encoded/extra-tail origins, and invalid alert numbers.
- GitHub secret-scanning alert-locations source-refresh metadata is implemented as a metadata-only GitHub refresher for exact `/repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}/locations` JSON list payloads. It fetches only the clean canonical API route with `hide_secret=true`, publishes labels such as `github secret scanning alert locations {owner}/{repo} #{alert_number}`, persists only bounded location type/path/line metadata, accepts documented safe body/review/discussion URL fields without rendering raw URLs/fragments, and fails closed before fetch or vault output for JSON Feed/text fallbacks, raw queued query/fragment/userinfo/port/encoded/extra-tail origins, raw body/html fields, renderer/source/data/API-auth markers, raw prompts, scripts, tokens, and secret-looking fixtures.
- GitHub secret-scanning scan-history source-refresh metadata is implemented as a metadata-only GitHub refresher for exact `/repos/{owner}/{repo}/secret-scanning/scan-history` JSON object payloads. It fetches only the clean canonical API route without alert-specific `hide_secret=true`, publishes labels such as `github secret scanning scan history {owner}/{repo}`, persists only bounded scan-category counts plus whitelisted scan type/status/timestamp and safe custom-pattern scope/name metadata, restores the hidden canonical fetch origin from safe public labels during due requeue, and fails closed before fetch or vault output for JSON Feed/text fallbacks, missing or unknown scan types, malformed rows, raw API URLs, API-auth markers, raw prompts, renderer/source/html/script/data fields, token/secret-like pattern names, query/fragment/userinfo/port/encoded/extra-tail origins, and lookalike hosts.
- GitHub repository autolinks, deploy keys, and private vulnerability reporting source-refresh metadata are implemented as metadata-only GitHub refreshers with public labels (`github autolinks {owner}/{repo}`, `github deploy keys {owner}/{repo}`, and `github private vulnerability reporting {owner}/{repo}`); persisted metadata is limited to autolink count/ids/key prefixes/alphanumeric flags, deploy key count/ids/safe titles/read-only and verified flags/safe created timestamps, and the private vulnerability reporting enabled boolean. Deploy-key response final URLs now must exactly match the sanitized canonical `/repos/{owner}/{repo}/keys` API route before body reads; URL templates/raw URLs, key material, API-auth/query/fragment/userinfo/credential drift, raw prompts, renderer/source/html/script/data fields, tokens, secret-like fixtures, text/feed bypasses, malformed tails, ports, and lookalikes are omitted or fail closed before catalog/job/search/vault output.
- GitHub workflow-scoped workflow-runs source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/actions/workflows/{workflow_id}/runs` API payloads, reconstructing summaries only from the safe repository path, workflow id, run count, and bounded run ids/names/statuses/conclusions/events/run numbers/attempts/branches/SHA prefixes/timestamps behind a metadata-only public origin label (`github workflow runs {owner}/{repo} workflow {workflow_id}`) while omitting logs/jobs/html URLs, workflow YAML/path/body fields, commit payloads, API-auth fields, raw prompts, renderer/source/html/script/data fields, tokens, and secret-like markers; JSON Feed/text fallbacks, malformed or mismatched rows, missing/mismatched scoped workflow ids, lookalike/userinfo/port authorities, case-mismatched routes, suffixed or encoded runs tails, final-URL drift, and public raw API origin persistence fail closed before fetch, queueing, catalog, search, or vault output.
- GitHub commit-associated pull-request source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/commits/{40-hex-sha}/pulls` API payloads, reconstructing summaries only from the safe repository path, commit SHA prefix, pull-request count, bounded PR numbers/titles/states/draft flags/authors/timestamps, and a metadata-only public origin label (`github commit pull requests {owner}/{repo} {sha12}`) while omitting PR bodies, URLs, head/base refs, API-auth fields, raw prompts, renderer/source/html/script/data fields, tokens, and secret-like markers; JSON Feed/text fallbacks, malformed rows, userinfo/port/lookalike authorities, suffixed or encoded pulls tails, response final-URL drift before body read, and legacy raw source/job/catalog origins fail closed or scrub to safe labels before fetch, queueing, catalog, search, relevant-memory, or vault output.
- GitHub repository topics source-refresh registration now stores and renders metadata-only public origins (`github repository topics {owner}/{repo}`) while preserving a hidden canonical fetch origin for exact `/repos/{owner}/{repo}/topics` JSON requests; uppercase/whitespace-wrapped uppercase hosts, userinfo/ports/lookalike authorities, extra or encoded topics tails, and redirect/final-URL drift fail closed before fetch or persistence, no vault artifact is written for rejected routes, and raw topics API URLs, query/fragment auth markers, raw prompts, renderer/source/html/script/data/API-auth fields, bearer markers, and secret-like fixtures stay out of receipts/catalog/jobs/search/vault output while repositories merely named `topics` remain valid non-topics routes.
- Legacy GitHub repository invitations public-origin hardening now fail-closes stale source/job rows with uppercase or whitespace-wrapped uppercase `API.GITHUB.COM` repository-invitations origins to local `capy-memory://...` labels in catalog/job public surfaces, keeps `_refresh_open` uncalled, writes no vault artifact, and prevents raw invitations API routes, auth query markers, raw/system prompt fragments, renderer/source/html/script/data/body/API-auth fields, bearer markers, and secret-like fixtures from public output while exact lowercase invitations ingestion and valid GitHub contents paths containing `invitations` remain preserved.
- Legacy GitHub collaborators public-origin hardening now fail-closes stale source/job rows with uppercase or whitespace-wrapped uppercase `API.GITHUB.COM` collaborator origins to local `capy-memory://...` labels in catalog/job public surfaces, keeps `_refresh_open` uncalled for those variants, writes no vault artifact, and prevents raw GitHub collaborator routes, auth query markers, raw-prompt fragments, hostile prompt text, API-key markers, scripts, and secret-like fixtures from public output while exact lowercase collaborators ingestion remains preserved.
- Legacy GitHub milestones public-origin hardening now fail-closes stale source/job rows with uppercase or whitespace-wrapped uppercase `API.GITHUB.COM` milestone origins to local `capy-memory://...` labels in catalog/job public surfaces, keeps `_refresh_open` uncalled for those variants, writes no vault artifact, and prevents raw GitHub milestone routes, auth query markers, raw-prompt fragments, hostile prompt text, API-key markers, scripts, and secret-like fixtures from public/search output while exact lowercase milestones ingestion, external milestone-shaped URLs, and GitHub contents paths with milestone filenames remain preserved.
- GitHub assignees source-refresh registration and fetching now fail close on uppercase or whitespace-wrapped uppercase `API.GITHUB.COM` raw origins before safe-origin normalization can produce a canonical lowercase fetch URL; public registration/job/catalog receipts use local `capy-memory://...` labels, `_refresh_open` remains uncalled for those variants, no vault file is written, and assignee fixture logins, auth markers, raw prompts, and secret-like values stay out of serialized receipts while exact lowercase assignees behavior is preserved.
- GitHub subscribers/watchers source-refresh registration and fetching now fail close on uppercase or whitespace-wrapped uppercase `API.GITHUB.COM` raw origins before normalization can produce a canonical lowercase fetch URL; public registration/job receipts use local `capy-memory://...` labels, `_refresh_open` remains uncalled for those variants, no vault file is written, and subscriber fixture logins, auth markers, raw prompts, and secret-like values stay out of serialized receipts while exact lowercase subscribers behavior is preserved.
- GitHub CODEOWNERS errors source-refresh metadata now has a strict JSON-only parser for exact `https://api.github.com/repos/{owner}/{repo}/codeowners/errors` payloads, reconstructing summaries only from safe repository path, total error count, and up to five line/optional-column/kind rows while omitting raw CODEOWNERS lines, suggestions, messages, URLs, API-auth fields, raw prompts, renderer/source/html/script/data fields, tokens, and secret-like fixture values; JSON Feed/XML/text fallbacks, lookalike/userinfo/port authorities, malformed tails, encoded suffixes, and redirect/final-URL drift fail closed before unsafe fetch or vault persistence.
- Legacy GitHub CODEOWNERS errors source/job rows now render public `source_catalog` and `source.refresh` job-list origins as `github codeowners errors {owner}/{repo}` for strict canonical routes, while lookalike/userinfo/malformed and non-string route payloads fail closed to `capy-memory://...` labels so stale queued metadata cannot leak raw GitHub API URLs, paths, query/fragment auth markers, raw prompts, or secret-looking values into product freshness cards.
- GitHub Actions runner-groups source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/actions/runner-groups` API payloads, reconstructing summaries only from safe repository path, bounded runner-group count, ids, names, allow-listed visibility, boolean policy flags, and safe selected workflow paths while omitting raw URLs, API-auth fields, raw prompts, scripts, renderer/source/data/html fields, tokens, and secret-like fixture values; JSON Feed/text fallback, over-limit or malformed rows, unsafe names/workflows, non-boolean policy fields, lookalike hosts, userinfo/ports, malformed tails, encoded suffixes, and legacy fallback public-output leaks fail closed or scrub to neutral metadata-only labels before fetch, search, catalog, jobs, queue, or persistence output.
- GitHub Actions runner-downloads source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/actions/runners/downloads` API payloads, reconstructing summaries only from the safe repository path, bounded runner application count, operating system, architecture, and filename while omitting raw download URLs, API-auth fields, raw prompts, scripts, renderer/source/data/html fields, tokens, and secret-like fixture values; JSON Feed/text fallback, unsafe payload fields, lookalike hosts, userinfo, explicit ports, uppercase authorities, padded/unsafe path segments, suffixes, encoded suffixes, and extra tails fail closed before unsafe fetch or persistence, and the broader `/actions/runners` parser no longer swallows the downloads subroute.
- GitHub Actions organization runner-downloads source-refresh metadata now has a narrow JSON-only parser for exact `GET https://api.github.com/orgs/{org}/actions/runners/downloads` payloads, reconstructing metadata-only summaries only from the safe public label (`github actions organization runner downloads {org}`, for example `github actions organization runner downloads capy`), bounded runner application count, operating system, architecture, and filename while omitting raw API/download URLs, API-auth fields, prompts/raw prompts, scripts, renderer/source/data/html fields, tokens, credentials, and secret-like values from public source/catalog/jobs/search/vault output; lookalike hosts, userinfo, explicit ports, uppercase authority drift, unsafe or padded org segments, suffixes/extra tails, encoded suffixes, JSON Feed/text fallback payloads, unsafe payload fields, malformed rows/count drift, final-URL drift, and legacy raw public-output leaks fail closed before unsafe fetch or persistence.
- GitHub Actions self-hosted runners source-refresh metadata is already implemented for exact `/repos/{owner}/{repo}/actions/runners` API payloads, with public labels such as `github actions runners {owner}/{repo}` and metadata-only persisted/rendered summaries limited to runner count plus bounded runner id, name, status, busy flag, OS, architecture, and safe label names. Raw API URLs/routes, query/fragment/userinfo/ports, registration tokens, API-auth fields, prompts, renderer/source/data/html/script fields, credentials, and secret-like fixtures remain omitted from public output or fail closed before fetch/persistence.
- GitHub Actions runner-downloads route hardening now makes the strict matcher reject malformed, lookalike-host, userinfo, explicit-port, suffixed, tail, and encoded near-miss routes while documenting that the exact downloads route and public-origin/fetch-origin round trip still pass.
- GitHub Actions workflow-run pending-deployments source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments` API payloads, reconstructing summaries only from safe repository path, run id, pending deployment count, bounded environment ids/names, wait timer/status/timestamps, and safe reviewer actor/team/app labels while omitting raw URLs, deployment/environment bodies, comments, API-auth fields, raw prompts, scripts, renderer/source/data/html fields, tokens, and secret-like fixture values; JSON Feed/text fallback, abusive authorities, padded run ids, encoded suffixes, extra tails, and redirect route drift fail closed before unsafe fetch or persistence.
- GitHub Actions workflow-run artifacts source-refresh metadata now has strict JSON-only coverage for exact `/repos/{owner}/{repo}/actions/runs/{run_id}/artifacts` payloads. It fetches only the canonical `api.github.com` HTTPS route with query/fragment stripped, persists bounded artifact count plus safe ids/names/sizes/expired/timestamps, and fail-closes JSON Feed/text fallbacks, userinfo/ports/lookalikes, padded/nonpositive run ids, encoded/extra tails, unsafe names, and redirect/final-URL drift before vault/search/catalog/job output.
- GitHub Actions workflow-run approvals source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/actions/runs/{run_id}/approvals` API payloads, reconstructing summaries only from safe repository path, run id, approval count, allow-listed approval state, actor login, environment names, and safe timestamps while omitting approval comments, URLs, API-auth query/fragment markers, raw prompts, scripts, renderer/source fields, and secret-like fixture values; JSON Feed/text fallback, redirect/final-URL mismatches, lookalike hosts, userinfo/port authorities, padded run ids, encoded suffixes, and extra tails fail closed before fetch or persistence.
- GitHub interaction-limits source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/interaction-limits` API payloads, reconstructing summaries only from safe repository path, allow-listed limit/origin values, and safe expiry timestamp while ignoring raw URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; query/fragment origins sanitize to a durable public label plus exact safe fetch route, requeued jobs preserve that fetch route, and text fallback, lookalike-host, userinfo, malformed-tail, and encoded-suffix route bypasses fail closed before unsafe fetch or persistence.
- GitHub issue and pull-request comments source-refresh metadata now fail-closes malformed, missing-number, encoded/double-encoded, and lookalike-host comments-route variants before fetch and downgrades non-canonical uppercase `api.github.com` authorities to local `capy-memory://` origins before job persistence, while the exact `/repos/{owner}/{repo}/{issues|pulls}/{number}/comments` parser continues to expose only metadata-only comment counts, ids, commenters, and timestamps without raw comment bodies, query/fragment auth markers, raw prompts, renderer/source/html/script fields, or secret-like fixture values.
- GitHub commit-comments source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/commits/{sha}/comments` API payloads, reconstructing summaries only from safe repository path, commit SHA prefix, comment count, bounded commenter logins, comment ids, and safe timestamps while omitting raw comment bodies, diff hunks, file paths, URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; query/fragment origins sanitize before fetch, while JSON Feed-shaped payloads fail closed before persistence/search output.
- GitHub commit-comment reactions source-refresh metadata now has a narrow JSON-only parser for exact HTTPS `/repos/{owner}/{repo}/comments/{comment_id}/reactions` API payloads, reconstructing summaries only from safe repository path, comment id, reaction count, allow-listed reaction content counts, bounded reactor logins, reaction ids, and safe timestamps while omitting raw bodies, URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; malformed/encoded route variants, non-canonical raw authorities, and JSON Feed-shaped payloads fail closed before unsafe fetch or persistence/search output.
- GitHub release reactions source-refresh metadata now has a narrow JSON-only parser for exact HTTPS `/repos/{owner}/{repo}/releases/{release_id}/reactions` API payloads, reconstructing summaries only from safe repository path, release id, reaction count, allow-listed reaction content counts, bounded reactor logins, reaction ids, and safe timestamps while omitting raw bodies, URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; malformed/encoded route variants, non-canonical authorities, and JSON Feed-shaped payloads fail closed before unsafe fetch or persistence/search output.
- GitHub code-frequency source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/stats/code_frequency` API payloads, reconstructing summaries only from the safe repository path, week count, total additions, total deletions, net changed lines, and active-week count while omitting raw URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; userinfo/query/fragment origins sanitize before fetch, while feed-shaped payloads, malformed rows, positive deletion counts, encoded route tricks, and route-shaped text fallbacks fail closed before persistence/search output.
- GitHub punch-card source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/stats/punch_card` API payloads, reconstructing summaries only from the safe repository path, slot count, total commits, active-slot count, and busiest day/hour slot while omitting raw URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; userinfo/query/fragment origins sanitize before fetch, while feed-shaped payloads, malformed rows, invalid day/hour ranges, encoded route tricks, and route-shaped text fallbacks fail closed before fetch or persistence/search output.
- GitHub repository webhooks source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/hooks` API payloads, reconstructing summaries only from the safe repository path, hook count, bounded hook ids/names, active flags, event names, safe timestamps, and last response codes while omitting webhook callback URLs, delivery/test/ping URLs, config/auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; query/fragment/userinfo origins sanitize before fetch, while secret config rows and lookalike-host route-shaped URLs fail closed before persistence/search output.
- GitHub Actions workflow-permissions source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/actions/permissions/workflow` API payloads, reconstructing summaries only from the safe repository path, default workflow permission level, and pull-request-review approval boolean while omitting URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; text fallback payloads and lookalike-host route-shaped URLs fail closed before persistence/search output.
- GitHub Actions workflow-access source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/actions/permissions/access` API payloads, reconstructing summaries only from the safe repository path and allow-listed external workflow access level (`none`, `user`, or `organization`) while omitting raw URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, userinfo, and secret-like markers; jobs persist only `source_refresh_kind` plus `repo_path` and reconstruct the fetch URL at run time, while JSON Feed bypasses, text fallbacks, malformed route tails, cross-repository final-URL drift, and query/fragment auth final-URL drift fail closed before any response-body read, unsafe fetch continuation, or persistence.
- GitHub Actions repository permissions source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/actions/permissions` API payloads, persisting only the safe repository path, `enabled` boolean, and `allowed_actions` enum (`all`, `local_only`, `selected`) while omitting raw URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, tokens, and secrets; text fallback, JSON Feed bypasses, malformed routes, selected-actions lookalikes, encoded subroutes, lookalike hosts, userinfo, and explicit ports fail closed before fetch or persistence.
- GitHub Actions selected-actions source-refresh public-output hardening now publishes only labels such as `github actions selected actions capy/spaces` in registration receipts, source catalog, job-list receipts, and search hits while preserving a hidden clean canonical fetch origin for exact `/repos/{owner}/{repo}/actions/permissions/selected-actions` JSON refreshes. The parser reconstructs summaries only from safe repository path, GitHub-owned/verified booleans, allowed pattern count, and bounded safe pattern samples; legacy raw origins, route-shaped or credential-bearing source ids, snippets with routes beyond the truncation boundary, unsafe traversal-like or URL/domain-like patterns, lookalike-host routes, suffixed selected-actions routes, encoded suffixes, explicit-port authorities, API-auth/query/fragment markers, prompts, scripts, renderer/source/data/html fields, and secret-like markers fail closed or scrub to metadata-only labels before public receipt/catalog/job/search/vault output. Refresh final-URL validation now also requires the post-fetch URL to equal the exact sanitized selected-actions endpoint, so query/fragment auth or raw-prompt drift fails closed before any response-body read, JSON parsing, vault persistence, catalog/search output, or Spaces relevant-memory surfacing.
- GitHub Actions caches source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/actions/caches` API payloads, reconstructing summaries only from the safe repository path, cache count, total safe byte size, bounded cache ids, refs, sizes, and safe timestamps while omitting cache keys, versions, URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; query/fragment origins sanitize to a durable display label and exact safe fetch route, while text fallback payloads, JSON Feed bypasses, malformed rows, userinfo/port origins, and lookalike-host route-shaped URLs fail closed before persistence/search output. Expanded final-URL drift evidence now proves query/fragment auth, userinfo, repository drift, and route-tail drift fail before response-body read, JSON parsing, vault persistence, search, or relevant-memory surfacing.
- GitHub Actions cache usage source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/actions/cache/usage` API payloads, reconstructing summaries only from the safe repository path, active cache count, total safe byte size, and full-name match while omitting URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, secret-like markers, and cache-version material; query/fragment origins sanitize to durable public label `github actions cache usage {owner}/{repo}` plus exact safe fetch route, while missing/mismatched `full_name`, negative/boolean/string counts, `version`/JSON Feed-shaped bypasses, non-JSON fallback content, userinfo/port origins, and lookalike-host route-shaped URLs fail closed before persistence/search output.
- GitHub Actions cache usage-by-ref source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/actions/cache/usage-by-ref` API payloads, reconstructing summaries only from the safe repository path, bounded Git refs, active cache counts, and active cache byte totals while omitting cache keys, raw URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, tokens, secret-like markers, and cache-version material; query/fragment origins sanitize to durable public label `github actions cache usage by ref {owner}/{repo}` plus exact safe fetch route, while text/JSON Feed fallbacks, unsafe refs, malformed counts, route tails, encoded suffixes, userinfo, explicit ports, and lookalike hosts fail closed before fetch or persistence.
- GitHub repository custom-properties source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/properties/values` API payloads, reconstructing summaries only from the safe repository path, property count, bounded property names, and value shape/count metadata while omitting raw custom-property values, URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; userinfo/query/fragment origins sanitize to the exact safe fetch route, while text fallback payloads, JSON Feed bypasses, malformed route tails, explicit ports, and lookalike-host route-shaped URLs fail closed before fetch or persistence.
- GitHub milestone-list source registration now fail-closes route-shaped lookalike hosts, malformed milestone tails, and encoded milestone suffixes to local `capy-memory://...` origins before jobs are queued, so source/job rows cannot retain raw API routes, query/fragment markers, access tokens, or raw-prompt markers for invalid `/repos/{owner}/{repo}/milestones` sources.
- `api/capy_memory.py` registers source references idempotently, queues metadata-only `source.refresh` jobs, strips credential/query/fragment markers from public `origin_uri`, requeues terminal jobs, preserves active leased payloads, and now exposes bounded metadata-only job-list receipts through `GET /api/capy-memory/source/jobs` so operators/UI can inspect pending refresh work without triggering a fetch or leaking raw queued payloads.
- `run_source_refresh_jobs(...)` leases queued source-refresh jobs, enforces allow-listed HTTP(S) origins, ingests safe HTML/plain/Markdown, RSS/Atom, XML sitemap URLSets, JSON Feed, and allow-listed GitHub issue/PR/repository/readme/contents/release/latest-release/release-list/release-assets/release-reactions/branch/branch-list/tag/labels/milestones/topics/workflow/workflow-list, workflow-run, workflow-runs-list, workflow-run timing, workflow-jobs, workflow-artifacts, repository-artifacts, repository-custom-properties, Actions repository permissions, Actions cache usage, Actions cache usage-by-ref, Actions private-name, Actions secrets public-key, environment private-name, environment public-key, environment deployment branch policies, environment variables, environment single-variable, pages, check-runs, commit-statuses, commit-comments, commit-comment-reactions, issue-list, issue-event-list, PR-list, PR requested-reviewers-list, PR commit-list, PR review-list, PR file-list, issue/PR comments, contributors, stargazers, subscribers/watchers, fork-list, commit, commit-list, participation stats, code-frequency stats, punch-card stats, commit-activity stats, traffic views, traffic clones, environments, rulesets, Dependabot-alerts, Dependabot public-key, Dependabot organization public-key, collaborators, and community-profile API metadata, runs `auto_fetched_source` prompt preflight, persists only sanitized advisory summaries, and emits metadata-only `memory.ingest.*` progress events; the manual Memory refresh trigger and product-home scheduled tick trigger now return metadata-only action-policy receipts with destructive-external-action approval and `hint:summarize` routing evidence.
- GitHub Actions workflow run job-list and attempt job-list source-refresh now reject exact `/repos/{owner}/{repo}/actions/runs/{run_id}/jobs` and `/repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}/jobs` JSON payloads above 100 `jobs` rows, leaving oversized refreshes pending without vault artifacts while public result/job/search output omits raw job metadata, logs/html URLs, steps, labels, query/fragment auth, raw prompts, renderer/source/html/script/API-auth fields, credentials, and secret-looking fixtures.
- GitHub Actions repository private-name source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/actions/secrets` API payloads, reconstructing summaries only from the safe repository path, count, bounded safe names, and safe created/updated timestamps while ignoring raw values, row/global URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; text fallback payloads and lookalike-host route-shaped URLs fail closed before persistence/search output.
- GitHub Actions single repository private-name source-refresh metadata now has a narrow JSON-only parser for exact `https://api.github.com/repos/{owner}/{repo}/actions/secrets/{secret_name}` payloads, reconstructing summaries only from safe repository path, uppercase/underscore private-name label, and safe created/updated timestamps while omitting raw URLs, values, key material, API-auth/query/fragment markers, prompts, script/html/source/renderer/data/body fields, tokens, and secret-looking fixtures; malformed names, encoded slashes/suffixes, explicit ports, and lookalike hosts fail closed before fetch.
- GitHub Actions repository secrets public-key source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/actions/secrets/public-key` API payloads, reconstructing summaries only from the safe repository path and key id while ignoring raw public-key material, row/global URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; text fallback payloads and lookalike-host route-shaped URLs fail closed before persistence/search output.
- GitHub Dependabot secrets public-key source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/dependabot/secrets/public-key` API payloads, reconstructing summaries only from the safe repository path and key id while omitting raw public-key material, raw fetch URLs in job payloads/catalog/receipts, API-auth/query/fragment fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; malformed tails, lookalike hosts, explicit ports, unrelated encoded public-key paths, legacy requeue payloads, and non-JSON content fail closed before unsafe fetch or persistence.
- GitHub Dependabot organization secrets public-key source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/orgs/{org}/dependabot/secrets/public-key` API payloads, reconstructing summaries only from the safe organization slug and key id while omitting raw public-key material, raw fetch URLs in job payloads/catalog/receipts, API-auth/query/fragment/userinfo fields, prompts, scripts, renderer/source/data/html/body fields, credentials, tokens, and secret-like markers; malformed tails, lookalike hosts, explicit ports, uppercase authority drift, unrelated org Dependabot secret subroutes, JSON Feed/text fallbacks, and non-JSON content fail closed before unsafe fetch or persistence.
- GitHub environment private-name source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/environments/{environment_name}/secrets` API payloads, reconstructing summaries only from the safe repository path, environment name, count, bounded safe names, and safe created/updated timestamps while ignoring raw values, row/global URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; text fallback payloads, lookalike-host route-shaped URLs, encoded path tricks, and malformed environment-secret routes fail closed before persistence/search output.
- GitHub environment secrets public-key source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/environments/{environment_name}/secrets/public-key` API payloads, reconstructing summaries only from the safe repository path, environment name, and safe key id while ignoring raw public-key material, raw/global/row URLs, API-auth/query/fragment/userinfo markers, prompts, scripts, renderer/source/data/html fields, and secret-like markers; text/JSON Feed fallback payloads, unsafe key ids, malformed/encoded public-key tails, lookalike hosts, explicit ports, and redirect route drift fail closed before fetch or persistence.
- GitHub environment deployment-branch-policies source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies` API payloads, reconstructing summaries only from the safe repository path, environment name, policy count, and bounded safe policy id/name/type rows while ignoring branch-policy URLs, node IDs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; text/JSON Feed fallback payloads, unsafe policy names, malformed/encoded route tails, lookalike hosts, explicit ports, raw slash tails, and redirect route drift fail closed before fetch or persistence.
- GitHub environment variables source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/environments/{environment_name}/variables` API payloads, reconstructing summaries only from the safe repository path, environment name, variable count, bounded variable names, and safe created/updated timestamps while ignoring raw values, row/global URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed bypass payloads and lookalike-host route-shaped URLs fail closed before persistence/search output.
- GitHub environment single-variable source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/environments/{environment_name}/variables/{variable_name}` API payloads, reconstructing summaries only from safe repository path, environment name, variable name, and safe created/updated timestamps while omitting variable values, payload URLs, API/auth fields, raw prompts, renderer/source/html/script fields, tokens, and secret-like markers; dirty registered origins canonicalize to the exact clean fetch URL, while feed/text bypasses, malformed routes, lookalike hosts, and redirect route drift fail closed before persistence.
- GitHub security-advisories source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/security-advisories` API payloads, reconstructing summaries only from the safe repository path, advisory count, bounded GHSA/CVE ids, allow-listed severity/state, and safe timestamps while ignoring advisory summaries/descriptions, vulnerabilities, raw URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; userinfo/query/fragment origins sanitize before fetch, while JSON Feed/text fallback payloads, malformed tail rows, unsafe timestamps, and lookalike-host route-shaped URLs fail closed.
- GitHub Dependabot alerts source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/dependabot/alerts` API payloads, reconstructing summaries only from the safe repository path, alert count, and a bounded prefix of alert numbers/states/package ecosystems/package names/manifest paths/severities while ignoring advisory summaries/descriptions, CVE/GHSA ids, vulnerable/patched version ranges, dismissed comments/reasons, raw URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed bypass payloads, unsafe tail rows, legacy queued userinfo payloads, non-JSON fallback content, and malformed Dependabot-alert routes fail closed.
- GitHub repository events source-refresh metadata now has a narrow JSON-only parser for exact HTTPS/raw-authority `/repos/{owner}/{repo}/events` API payloads, reconstructing summaries only from the safe repository path, event count, type counts, and a bounded prefix of event ids/types/actors/public flags/timestamps while ignoring event payloads, commit messages, repo/actor URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed bypass payloads, unexpected raw/non-schema row keys, malformed rows, malformed event-shaped paths, unsafe hosts, legacy queued userinfo payloads, userinfo/HTTP/explicit-port authorities, cross-repo redirects, non-JSON fallback content, and secret-looking fixture values fail closed before persistence/search output.
- GitHub repository environments source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/environments` API payloads, reconstructing summaries only from the safe repository path, total environment count, bounded environment ids/names, and safe created/updated timestamps while ignoring raw URLs, protection-rule/branch-policy payloads, descriptions, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed-looking bypass payloads and malformed tail rows fail closed.
- GitHub repository community-profile source-refresh metadata now has a narrow JSON-only parser for exact HTTPS `/repos/{owner}/{repo}/community/profile` API payloads, reconstructing summaries only from the safe repository path, health percentage, content-report flag, update timestamp, and bounded allow-listed file name/path metadata while ignoring raw URLs, API-auth fields, prompts, scripts, renderer/source/data/html fields, descriptions, bodies, and secret-like markers; JSON Feed-looking payloads, invalid health percentages, unsafe file fields, non-JSON fallback content, insecure HTTP origins, malformed/encoded/case-mismatched routes, and response final-URL drift with query/fragment/userinfo/auth material fail closed before response-body read or persistence.
- GitHub repository traffic-clones source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/traffic/clones` API payloads, reconstructing summaries only from the safe repository path, total clone count, unique cloner count, bounded timestamped samples, and per-sample counts while ignoring raw URLs, API-auth fields, prompt/summary/body fields, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed-looking bypass payloads and malformed tail rows fail closed.
- GitHub repository traffic-views source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/traffic/views` API payloads, reconstructing summaries only from the safe repository path, total view count, unique visitor count, bounded timestamped samples, and per-sample counts while ignoring raw URLs, API-auth fields, prompt/summary/body fields, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed-looking bypass payloads and malformed tail rows fail closed.
- GitHub repository source-refresh metadata now has its own narrow allow-listed parser for `/repos/{owner}/{repo}` API payloads, reconstructing summaries only from repository name, description or safe fallback, default branch, visibility/private/archived status, counts, topics, and timestamps while ignoring raw body/URL/clone/homepage/auth fields and query/fragment markers.
- GitHub README source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/readme` API payloads, reconstructing summaries only from safe repository path, README name/path, file size, and SHA prefix while ignoring raw README content, encoding, download/html/git URLs, link maps, API-auth fields, prompts, scripts, renderer/source fields, and secret-like markers; JSON Feed/content-type bypass payloads, uppercase hosts, README-like suffix paths, and malformed payloads fail closed.
- GitHub repository contents source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/contents` and `/repos/{owner}/{repo}/contents/{path...}` API payloads, reconstructing summaries only from safe repository path, content path, item count, bounded item type/name/path rows, file size, and SHA prefixes while ignoring raw/encoded file content, download/html/git/API URLs, link maps, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed/text fallback payloads, explicit-port authorities, percent-encoded route tricks, case-mismatched routes, and malformed tail rows fail closed.
- GitHub latest-release source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/releases/latest` API payloads, reconstructing summaries only from the safe repository path, release id, release name/tag, draft/prerelease flags, and publish timestamp while ignoring release bodies, assets, URLs, API-auth fields, scripts, prompts, renderer fields, and secret-like markers; JSON Feed-looking bypass payloads at the same endpoint fail closed.
- GitHub release-list source-refresh metadata now has a narrow JSON-only allow-listed parser for `/repos/{owner}/{repo}/releases` API payloads, reconstructing summaries only from the safe repository path, release count, bounded release names/tags, draft/prerelease flags, and publish timestamps while ignoring release bodies, assets, archive URLs, API-auth fields, scripts, prompts, and secret-like markers; text fallback, JSON Feed-shaped bypass payloads, and malformed tail rows fail closed before unsafe vault/search persistence.
- GitHub single-release source-refresh now hardens the exact `/repos/{owner}/{repo}/releases/{release_id}` boundary by requesting JSON-only metadata for valid numeric release ids and fail-closing malformed route tails before text fallback, keeping raw release notes, URLs, API-auth fields, prompts, scripts, renderer/source fields, and secret-like markers out of receipts/search/vault output.
- GitHub release-assets source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/releases/{release_id}/assets` API payloads, reconstructing summaries only from the safe repository path, release id, asset count, bounded asset names, ids, sizes, download counts, states, content types, and timestamps while ignoring download URLs, uploader objects, API-auth fields, raw prompts, scripts, renderer/source fields, and secret-like markers; JSON Feed/text fallback bypass payloads at the same endpoint plus encoded/case-mismatched release-assets-shaped path variants fail closed.
- GitHub branch source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/branches/{branch}` API payloads, reconstructing summaries only from the branch name, protected flag, and commit SHA prefix while ignoring commit URLs, protection details, bodies, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub branch-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/branches` API payloads, reconstructing summaries only from the safe repository path, branch count, bounded branch names, protected flags, and commit SHA prefixes while ignoring branch protection details, URLs, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub Actions workflow source-refresh metadata now has narrow allow-listed parsers for `/repos/{owner}/{repo}/actions/workflows/{workflow_id}` and `/repos/{owner}/{repo}/actions/workflows` API payloads, reconstructing summaries only from workflow id/list count, names, states, and timestamps while ignoring workflow paths/YAML/jobs, URLs, body fields, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub Actions workflow-run source-refresh metadata now has narrow allow-listed parsers for `/repos/{owner}/{repo}/actions/runs/{run_id}` and `/repos/{owner}/{repo}/actions/runs` API payloads, reconstructing summaries only from run id/list count, run names, statuses, conclusions, events, run numbers/attempts, branches, SHA prefixes, and timestamps while ignoring logs/jobs/html URLs, commit payloads, job payloads, API-auth fields, scripts, prompts, and secret-like markers; JSON Feed-looking bypass payloads and malformed rows at the list endpoint fail closed.
- GitHub Actions workflow-run timing source-refresh metadata now has a narrow JSON-only parser for `/repos/{owner}/{repo}/actions/runs/{run_id}/timing` API payloads, reconstructing summaries only from the safe run id, run duration, and billable OS total/job counts while ignoring nested job-run details, URLs, API-auth fields, prompts, scripts, renderer/source fields, and secret-like markers; JSON Feed bypass payloads fail closed before persistence/search output.
- GitHub Actions workflow-jobs source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/actions/runs/{run_id}/jobs` API payloads, reconstructing summaries only from run id, total count, and a bounded prefix of safe job names/status/conclusion/timestamps while ignoring job steps, runner labels/names, logs/html URLs, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub Actions workflow attempt jobs source-refresh metadata now has a narrow JSON-only parser for exact `https://api.github.com/repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}/jobs` payloads, reconstructing summaries only from run id, attempt number, total count, and bounded job names/status/conclusion/timestamps while omitting HTML/log URLs, steps, runner names/labels, API-auth fields, raw prompts, scripts, and secret-like markers.
- GitHub repository Actions artifacts source-refresh metadata now has a narrow JSON-only parser for `/repos/{owner}/{repo}/actions/artifacts` API payloads, reconstructing summaries only from the safe repository path, artifact count, and bounded artifact ids/names/sizes/expired flags/safe timestamps while ignoring archive/download/API/html URLs, workflow-run commit payloads, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed-looking bypass payloads, unsafe userinfo authorities, and non-JSON fallback content fail closed.
- GitHub Pages source-refresh metadata now has a narrow JSON-only parser for `/repos/{owner}/{repo}/pages` API payloads, reconstructing summaries only from the safe repository path, Pages status, build type, public/custom-404/HTTPS booleans, safe CNAME, and protected-domain state while ignoring raw URLs, certificate descriptions/domains, source branch/path objects, API-auth fields, prompts, scripts, renderer/source/data/html fields, and secret-like markers; JSON Feed bypass payloads, malformed Pages-shaped paths, legacy queued userinfo payloads, and non-JSON fallback content fail closed.
- GitHub check-runs source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/commits/{sha}/check-runs` API payloads, reconstructing summaries only from the safe repository path, commit SHA prefix, check-run count, bounded check-run ids/names/statuses/conclusions, and timestamps while ignoring output summaries, details/html URLs, pull-request rows, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub commit-status source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/commits/{sha}/statuses` API payloads, reconstructing summaries only from the safe repository path, commit SHA prefix, status count, state counts, bounded status ids/contexts/creator logins, and timestamps while ignoring status descriptions, target URLs, row URLs, avatar URLs, API-auth fields, scripts, prompts, renderer/source fields, and secret-like markers.
- GitHub deployment-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/deployments` API payloads, reconstructing summaries only from the safe repository path, deployment count, bounded deployment ids/environments/refs/SHA prefixes/tasks/production-transient flags, and timestamps while ignoring deployment descriptions, payloads, status URLs, creator URLs, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub single-deployment source-refresh metadata now has a narrow JSON-only parser for `/repos/{owner}/{repo}/deployments/{deployment_id}` API payloads, reconstructing summaries only from the exact safe repository path, matched deployment id, environment, ref, SHA prefix, task, production/transient flags, safe creator login, and timestamps while ignoring descriptions, payloads, status/repository/creator URLs, API-auth query/fragment markers, scripts, prompts, renderer/source fields, and secret-like markers; JSON Feed bypass payloads and malformed encoded deployment-route variants fail closed.
- GitHub deployment-status source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/deployments/{deployment_id}/statuses` API payloads, reconstructing summaries only from the safe repository path, deployment id, status count, state counts, bounded status ids/states/environments/creator logins, and timestamps while ignoring descriptions, target/log/deployment/repository/environment URLs, payloads, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub tag source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/tags` API payloads, reconstructing summaries only from bounded tag names, tag count, and commit SHA prefixes while ignoring archive URLs, raw URLs, tag bodies, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub label-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/labels` API payloads, reconstructing summaries only from a non-URL labels origin label, safe repository path, label count, and bounded safe label names/colors/default flags while ignoring descriptions, row URLs, ids/node_ids, and failing closed on URL-like label names, non-boolean default values, raw/body/code/content/API-auth fields, scripts, prompts, and secret-like markers.
- GitHub repository-languages source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/languages` API payloads, reconstructing summaries only from the safe repository path, language count, total byte count, and a bounded top-language list while ignoring generic JSON feed fields, query/fragment markers, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub repository-topics source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/topics` API payloads, reconstructing summaries only from the safe repository path, topic count, and bounded topic names while ignoring raw bodies, JSON Feed-looking fields, query/fragment markers, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub commit source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/commits/{sha}` API payloads, reconstructing summaries only from the path-matched commit SHA prefix, first safe commit-message line, safe author/committer dates, parent count, changed-file count, and aggregate line stats while ignoring emails, signatures, file paths, patches, raw URLs, API-auth fields, scripts, raw message bodies, prompts, and secret-like markers.
- GitHub commit-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/commits` API payloads, reconstructing summaries only from the safe repository path, commit count, bounded commit SHA prefixes, first safe commit-message lines, author dates, and parent counts while ignoring raw message bodies, emails, signatures, avatars, HTML/API URLs, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub pull-request list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/pulls` API payloads, reconstructing summaries only from the safe repository path, PR count, bounded PR numbers, safe titles, allow-listed states, draft flags, safe authors, and timestamps while rejecting non-JSON fallback content and ignoring PR bodies, head/base refs, URLs, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub single pull-request source-refresh metadata now has a narrow JSON-only parser for exact `/repos/{owner}/{repo}/pulls/{number}` API payloads, canonicalizes fetch origins to lowercase route markers, reconstructs only safe PR title/state/draft/merged/author/timestamp/count/branch-ref summaries, and omits PR bodies, URLs, renderer/source/html/script/data/API-auth fields, raw prompts, tokens, query/fragment markers, URL-like branch refs, and secret-like markers; malformed, singular, suffixed, encoded, mixed-case, invalid owner/repo, JSON Feed, and text-fallback lookalikes fail closed before fetch or persistence while adjacent PR list/subresource routes remain intact. Focused final-URL drift evidence now proves exact single pull-request refreshes reject userinfo/query/fragment auth, repository drift, and PR-number drift before body reads, with no vault/search artifact or public leakage of raw final URLs, hostile PR body sentinels, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, or secret-looking fixtures.
- GitHub pull-request requested-reviewers source-refresh metadata now has a narrow JSON-only parser for `/repos/{owner}/{repo}/pulls/{number}/requested_reviewers` API payloads, reconstructing summaries only from PR number, reviewer/team counts, bounded safe reviewer logins, and bounded safe team slugs while ignoring avatar/profile/API URLs, team descriptions, raw prompt/source/renderer/html/script/data fields, API-auth/query/fragment/userinfo markers, tokens, and secret-like markers; JSON Feed/text fallback payloads, explicit-port authorities, malformed requested-reviewers-shaped paths, non-string reviewer login/team slug rows, and `users`/`teams` lists above 100 rows fail closed.
- GitHub pull-request review-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/pulls/{number}/reviews` API payloads, reconstructing summaries only from PR number, review count, safe reviewer logins, allow-listed review states, review ids, and submitted timestamps while ignoring review bodies, HTML/review URLs, commit ids, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub pull-request commit-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/pulls/{number}/commits` API payloads, reconstructing summaries only from PR number, commit count, bounded commit SHA prefixes, first safe commit-message lines, author dates, and parent counts while ignoring raw message bodies, emails, signatures, files, patches, HTML/API URLs, API-auth fields, scripts, prompts, renderer/source fields, and secret-like markers.
- GitHub pull-request file-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/pulls/{number}/files` API payloads, reconstructing summaries only from PR number, file count, aggregate additions/deletions/changes, and bounded status counts while ignoring filenames, previous filenames, patches, raw/content URLs, API-auth fields, scripts, prompts, and secret-like markers. Exact file-list arrays above 100 rows now fail closed, leave the oversized refresh pending without a vault artifact, and public output omits raw/rejected row metadata, URL/auth markers, prompts, renderer/source/html/script/API-auth fields, credentials, and secrets.
- GitHub issue-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/issues` API payloads, reconstructing summaries only from the safe repository path, issue count, bounded issue/pull-request numbers, safe titles, allow-listed states, safe labels, and update timestamps while ignoring issue bodies, HTML, URLs, raw pull-request payloads, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub issue-label source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/issues/{number}/labels` API payloads, reconstructing summaries only from the safe repository path, issue number, label count, bounded label names, colors, and default flags while ignoring descriptions, URLs, ids/node_ids, query/fragment markers, API-auth fields, scripts, prompts, renderer/source fields, and secret-like markers.
- GitHub issue and pull-request comments source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/{issues|pulls}/{number}/comments` API payloads, reconstructing summaries only from issue/PR number, comment count, safe commenter logins, comment ids, and timestamps while ignoring comment bodies, HTML, reactions, URLs, author association, API-auth fields, scripts, prompts, and secret-like markers.
- GitHub contributors source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/contributors` API payloads, reconstructing summaries only from the safe repository path, contributor count, bounded safe logins, and contribution counts while ignoring avatar/user/profile URLs, API-auth fields, scripts, prompts, and secret-like markers. Exact contributors JSON arrays above 100 rows fail closed with no vault artifact and no public leakage of rejected contributor logins/counts, URL/auth markers, raw prompts, renderer/source/html/script/API-auth fields, credentials, or secret-looking fixtures.
- GitHub teams source-refresh metadata now has a narrow JSON-only parser for exact `https://api.github.com/repos/{owner}/{repo}/teams` payloads, reconstructing summaries only from safe repository path, team count, and bounded team names/slugs/ids/privacy/permission while mapping secret privacy to private and omitting descriptions, HTML/member/API URLs, access tokens, raw prompts, scripts, renderer/API keys, and secret-like markers. Exact teams arrays above 100 rows now fail closed with no vault artifact/search hit and no public leakage of rejected team names/slugs/ids or raw GitHub API routes.
- GitHub repository invitations list source-refresh jobs now preserve public labels such as `github repository invitations {owner}/{repo}` while keeping canonical GitHub API routes only in hidden `fetch_origin_uri`, and propagate `source_refresh_kind: github_repository_invitations` through raw/public-label registration, due requeue, legacy pending jobs, custom fetcher fallback, source rows, and vault output. Query/fragment auth, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, and secret-looking fixtures remain absent from public output, while single-invitation routes stay distinct.
- Legacy GitHub teams uppercase-host public-origin hardening is complete: stale source/job rows with raw uppercase or whitespace-wrapped uppercase `API.GITHUB.COM` teams origins now fail closed to local `capy-memory://...` labels in catalog/job public surfaces without leaking normalized raw Teams API routes, auth query markers, raw-prompt fragments, hostile prompts, scripts, API-key markers, or secret-looking values.
- GitHub subscribers/watchers source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/subscribers` API payloads, reconstructing summaries only from the safe repository path, subscriber count, and bounded safe logins while ignoring avatar/user/profile URLs, query/fragment/userinfo markers, API-auth fields, scripts, prompts, renderer/source/data fields, and secret-like markers; JSON Feed-looking bypass payloads and malformed tail rows fail closed.
- GitHub issue-event-list source-refresh metadata now has a narrow allow-listed parser for exact lowercase-host `/repos/{owner}/{repo}/issues/{number}/events` API payloads, reconstructing summaries only from issue number, event count, event-type counts, bounded event ids/types, safe actor logins, safe label names, and timestamps while failing closed on raw event bodies, commit ids/aliases, URLs, non-allow-listed fields, non-JSON fallback content, malformed/encoded path variants, non-lowercase GitHub hosts, API-auth fields, scripts, prompts, renderer/source fields, and secret-like markers.
- GitHub stargazer-list source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/stargazers` API payloads, reconstructing summaries only from the safe repository path, stargazer count, bounded safe logins, and `starred_at` timestamps when present while ignoring avatar/profile/API URLs, JSON Feed/generic JSON bypass fields, API-auth fields, scripts, prompts, and secret-like markers. Uppercase and whitespace-wrapped uppercase raw `API.GITHUB.COM` stargazer origins now fail closed to local `capy-memory://...` public origins before registration/fetch normalization, so unsafe authority drift cannot become an eligible lowercase GitHub fetch target.
- GitHub fork-list source-refresh metadata now has a narrow allow-listed parser for exact lowercase-host `/repos/{owner}/{repo}/forks` API payloads, reconstructing summaries only from the safe repository path, fork count, bounded safe fork full names, owner logins, default branches, and update timestamps while ignoring fork descriptions, clone/profile/API URLs, JSON Feed/generic text bypass fields, malformed/encoded path variants, non-lowercase GitHub hosts, API-auth fields, scripts, prompts, renderer/source fields, and secret-like markers.
- Legacy GitHub forks uppercase-host public-origin hardening is complete: stale source/job rows with raw `API.GITHUB.COM` forks origins now fail closed to local `capy-memory://...` labels in catalog/job public surfaces without leaking raw route, auth query, or fragment prompt markers.
- GitHub participation-stats source-refresh metadata now has a narrow allow-listed parser for `/repos/{owner}/{repo}/stats/participation` API payloads, reconstructing summaries only from safe repository path, week count, all/owner commit totals, and active-week count while ignoring raw bodies, URLs, query/fragment/userinfo auth markers, API-auth fields, scripts, prompts, renderer/source fields, and secret-like markers; JSON Feed-looking bypass payloads and malformed aggregate rows fail closed.
- `GET /api/capy-memory/status` returns local-only source/chunk/stale/error/refresh-job counts.
- `static/spaces.js` renders the product-home Memory freshness card from `api/capy-memory/status` with hostile fields ignored/redacted.
- `api/knowledge.py` / Memory Tree bridge registers local knowledge sources as metadata-only Memory Tree source records with local provenance/freshness status.
- GitHub pull-request list source-refresh now fail-closes non-canonical raw authorities before URL normalization, so uppercase-host PR-list routes downgrade to local `capy-memory://` receipts and never fetch or persist query/fragment/auth prompt markers.
- GitHub branch-protection source-refresh now supports encoded slash branch names such as `feature%2Fsource-refresh` while preserving exact-route JSON-only fetching, sanitized public origins, decoded branch metadata in summaries, and fail-before-fetch guards for raw slash branches, double-encoded branches, empty branch segments, traversal-like segments, lookalike hosts, and malformed protection suffixes.
- GitHub commit-statuses source-refresh now rejects oversized status arrays above 100 rows. Exact bounded `/repos/{owner}/{repo}/commits/{sha}/statuses` metadata refreshes still persist only status counts/state counts/safe context metadata, while oversized payloads fail closed with no vault artifact and no row-context, raw URL/query/fragment, raw prompt, renderer/script/API-auth, token, or secret-looking fixture leakage in public job output.
- GitHub Pages builds source-refresh metadata now supports exact `https://api.github.com/repos/{owner}/{repo}/pages/builds` JSON arrays with hidden canonical fetch origins and public labels such as `github pages builds capy/spaces`; persisted summaries are metadata-only (repo, build count, bounded build ids, allow-listed statuses, commit SHA prefixes, pusher logins, safe timestamps, duration, and error-present booleans). Exact Pages builds final-URL drift now fails closed before any response-body read or vault persistence with no vault/search/relevant-memory artifact, and oversized arrays, JSON Feed/text fallbacks, malformed/lookalike/userinfo/port/uppercase/encoded/extra-tail routes, raw final URLs/API URLs, query/fragment/auth markers, raw prompts, renderer/source/html/script/API-auth fields, credentials, tokens, and secret-looking fixtures remain absent from public result/job/catalog/search/relevant-memory output.
- GitHub check-run annotations source-refresh now has explicit no-body-read + relevant-memory-empty final-URL drift evidence for exact `https://api.github.com/repos/{owner}/{repo}/check-runs/{check_run_id}/annotations` refreshes. Query/fragment/auth, userinfo, repository/check-run drift, raw prompt fragments, renderer/source/html/script/API-auth markers, annotation paths/messages/raw details, credentials, tokens, and secret-looking fixtures fail closed without vault/search/relevant-memory artifacts or public leakage.

Remaining:
- Broaden cron/daemon triggering and additional source-specific fetchers while keeping existing manual/scheduled UI trigger receipts, GitHub issue/PR/repository/readme/contents/release/latest-release/release-list/release-assets/release-reactions/branch/tag/labels/milestones/topics/workflow/workflow-list/workflow-run/workflow-runs-list/workflow-run-timing/workflow-jobs/workflow-artifacts/repository-artifacts/actions-cache-usage/actions-private-name/actions-public-key/environment-private-name/environment-public-key/environment-deployment-branch-policies/environment-variables/environment-single-variable/check-runs/commit-statuses/issue-list/issue-event-list/PR-list/PR-requested-reviewers-list/PR-review-list/PR-commit-list/PR-file-list/issue-PR-comments/contributors/stargazers/subscribers/fork-list/commit/commit-list/participation-stats/code-frequency-stats/punch-card-stats/traffic-views/traffic-clones/environments/collaborators/community-profile metadata ingestion, and all public UI metadata-only.

### Phase 4 — Spaces-aware memory integration

Status: **Space detail, creator preview, production artifact auto-ingest, and active-space context advisory/preflight surfaces implemented; broader advisory enforcement remains**.

Delivered:
- `GET /api/spaces/memory?space_id=...` returns bounded relevant Memory Tree slices with metadata-only snippets.
- `static/spaces.js` renders a Space detail `Memory Tree context` card and creator-preview `Memory assist` evidence without leaking renderer/source/API-auth/raw-prompt markers.
- New artifact canonicalizers cover Space manifests, revision events, widget events, and visual/UI QA reports as searchable local artifacts.
- Production boundaries now call the canonicalizers automatically for Space manifest/revision writes (including rollback/restore anchors), widget events, recovery/repair events, and creator visual-QA commit reports.
- Active-space context now includes cited advisory relevant-memory slices only through a prompt-preflighted, metadata-only envelope with compaction, context status, progress-event, and action-policy evidence. The source-style `space.current.context` / `space.context` / `space.current.prompt_context` helper responses now also return a server-generated Memory Tree advisory/no-authority receipt on both active and no-current paths, and their output-compaction evidence threads only allow-listed advisory metadata (`untrusted_advisory`, gate-bypass false, required prompt-preflight/approval/sandbox-preview/visual-QA/rollback gates) without exposing raw context payloads or caller-forged memory authority.
- No-active-space `space.current.context` / `space.context` / `space.current.prompt_context` reads now preserve the same trust envelope shape as active context reads: metadata-only `memory_context` prompt-preflight required evidence, `space.current.context` autonomy-policy receipts, `context:none` structured progress, and output-compaction lines for preflight/policy/progress/advisory status, while still returning an empty context body and no raw memory/source/widget content.
- Public Memory Tree retrieval payloads (`search_memory`, `/api/capy-memory/search`, `relevant_memory_for_space`, and `/api/spaces/memory`) now carry explicit machine-readable advisory/no-authority fields (`metadata_only`, `advisory_context`, `context_authority: untrusted_advisory`, `can_bypass_safety_gates: false`, and required gate labels), so downstream Spaces/tool consumers cannot treat retrieved snippets as trusted instructions or gate bypasses. `/api/capy-memory/search` now also returns metadata-only `memory_context` prompt-preflight and `space.memory.read` autonomy-policy receipts with `hint:summarize`, rejects unsafe encoded query/limit markers without echoing them, and escapes LIKE wildcards before retrieval.
- Creator preview `memory_assist` now preserves that same advisory/no-authority envelope at the wrapper and per-hit levels and renders a visible Memory trust boundary in `static/spaces.js`, so memory-informed creator drafts remain explicitly unable to bypass prompt-preflight, approval, sandbox preview, visual QA, or rollback/recovery gates.
- Research Harness artifact/export updates (`set_research_artifact` and `space.research.artifact.set` / `space.research.report.set` adapters) now return the server-generated Memory Tree advisory/no-authority envelope and thread that boundary into metadata-only compaction evidence, so exported research summaries remain untrusted context and cannot leak caller-forged memory authority, raw memory context, markdown bodies, renderer/script/API-auth fields, or secret-looking fixtures.
- Research Harness progress updates (`set_research_progress` and `space.research.progress.*` adapters) now return the server-generated Memory Tree advisory/no-authority envelope and thread that boundary into metadata-only compaction evidence, so research/source status can become agent-visible context without treating memory as trusted authority or leaking caller-forged memory authority, raw memory context, renderer/script/API-auth fields, or secret-looking fixtures.
- Receipt-only development terminal/shell responses now include the same generated Memory Tree advisory/no-authority envelope (`untrusted_advisory`, gate bypass false, required prompt-preflight/approval/sandbox/visual-QA/rollback gates) and thread safe advisory-boundary metadata into compaction evidence without letting request payloads forge authority or exposing raw commands, prompts, renderer/source/API-auth fields, scripts, paths, or credentials.
- Shared-data set/list/read/delete tool responses now include the shared generated Memory Tree advisory/no-authority envelope and thread it into metadata-only compaction evidence, keeping shared context untrusted while caller-forged `trusted_system_memory` authority, raw slot values, renderer/source/API-auth fields, scripts, tokens, and secret-looking fixtures remain omitted.
- Space collection/current read helpers (`space.list`, `space.spaces`, `space.spaces.list`, `space.spaces.listSpaces`, `space.spaces.items`, `space.spaces.all`, `space.spaces.byId`, `space.current`, `space.current.get`, `space.spaces.current`, and `space.spaces.getCurrentSpace`) now include the same server-generated Memory Tree advisory/no-authority envelope beside prompt-preflight, autonomy-policy, progress, and compaction receipts, while caller-forged memory authority, raw memory context, renderer/source/html/API-auth fields, scripts, tokens, and secret-looking fixtures stay out of serialized responses.
- No-space widget SDK helpers (`space.spaces.widgetApiVersion`, size/position/rendered-size helpers, source-style ID normalizers, and `space.spaces.currentId`) now include the same server-generated Memory Tree advisory/no-authority envelope beside prompt-preflight, autonomy-policy, progress, and compaction receipts, while caller-forged memory authority and hostile renderer/source/html/API-auth/raw-prompt/script/token fixtures stay out of serialized responses.
- Source-style logical storage path helpers (`space.spaces.buildSpace*Path`) now include the same server-generated Memory Tree advisory/no-authority envelope beside prompt-preflight, autonomy-policy, progress, and compaction receipts, while caller-forged memory authority, raw memory context, logical-path payload markers, renderer/source/html/API-auth fields, scripts, tokens, and secret-looking fixtures stay out of serialized responses.
- Source-style layout planning/read helpers (`space.spaces.buildCenteredFirstFitLayout`, `space.spaces.findFirstFitWidgetPlacement`, and `space.spaces.resolveSpaceLayout`) plus current-space viewport/reposition helpers (`space.spaces.repositionCurrentSpace`, `space.current.reposition`, and `space.current.reposition_viewport`) now include server-generated Memory Tree advisory/no-authority envelopes beside prompt-preflight, autonomy-policy, progress, and output-compaction receipts; forged memory authority, raw memory context, renderer/source/html/API-auth/raw-prompt/script, and secret-looking fixtures stay omitted.
- Source-style layout repair/rearrange mutation helpers (`space.spaces.repairLayout` and `space.spaces.rearrangeWidgets`) now include server-generated Memory Tree advisory/no-authority envelopes beside prompt-preflight, autonomy-policy, progress, and output-compaction receipts; caller-forged memory authority, raw memory context, renderer/source/html/API-auth/script, bypass flags, and secret-looking fixtures stay omitted while layout context remains unable to bypass creator, approval, sandbox preview, visual QA, or rollback/recovery gates.
- Source-style widget definition/blueprint/source/render helper responses (`space.spaces.defineWidget`, `space.spaces.createWidgetSource`, `space.spaces.previewWidgetRecord`, and `space.spaces.renderWidget`) now include server-generated Memory Tree advisory/no-authority envelopes beside widget-runtime prompt-preflight, autonomy-policy, progress, and output-compaction receipts, so blueprint/render context stays untrusted advisory metadata and cannot bypass prompt-preflight, approval, sandbox preview, visual QA, or rollback/recovery gates.
- Browser-facing widget event inbox reads (`GET /api/spaces/widget/events`) now return the same server-generated Memory Tree advisory/no-authority envelope beside required prompt-preflight, generated-widget-execution policy, `widget.events:<space_id>` progress, output-compaction evidence, and sanitized event summaries; `static/spaces.js` renders a “Widget event inbox receipt” card without trusting caller/event-forged memory authority or exposing raw prompts, renderer/source/html/script/data/API-auth fields, bearer/token values, generated widget bodies, or secret-looking fixtures.
- Source-style/current widget mutation helpers (`space.spaces.upsertWidget(s)`, `space.spaces.patchWidget`, `space.current.patchWidget`, `space.spaces.toggleWidgets`, `space.current.toggleWidgets`, widget delete/remove/delete-all aliases, generic `widget.patch` aliases, and direct `patch_widget(..., include_safety_receipts=True)`) now include server-generated Memory Tree advisory/no-authority envelopes and advisory-gate compaction evidence, so widget mutation context and caller-forged memory authority remain untrusted and cannot bypass prompt-preflight, approval, sandbox preview, visual QA, or rollback/recovery gates.
- Source-style current instruction helpers (`space.current.agentInstructions` and `space.current.specialInstructions`) now include server-generated Memory Tree advisory/no-authority envelopes beside active-space instruction prompt-preflight, `instructions:<space_id>` progress, autonomy-policy, and output-compaction receipts, so caller-forged memory authority cannot turn active instructions into trusted context or bypass prompt-preflight/approval/sandbox/visual-QA/rollback gates.
- Source-style Space metadata/layout mutation helpers (`space.spaces.saveSpaceMeta` / `space.current.saveMeta` and `space.spaces.saveSpaceLayout` / `space.current.saveLayout`) now include server-generated Memory Tree advisory/no-authority envelopes beside prompt-preflight, autonomy-policy, progress, and output-compaction receipts, so caller-forged memory authority and raw memory context cannot turn Space mutations into trusted context or bypass safety gates.
- Direct Space deletion (`delete_space(..., include_safety_receipts=True)`) and source-style deletion helpers (`space.spaces.deleteSpace` / `space.spaces.removeSpace`) now include server-generated Memory Tree advisory/no-authority envelopes beside prompt-preflight, autonomy-policy, progress, and output-compaction receipts, so destructive Space removal cannot treat deletion context or caller-forged memory authority as trusted instructions or gate bypasses.
- Recovery quarantine/disable/enable toggle responses for whole Spaces, widgets, and recovery modules now include the same server-generated Memory Tree advisory/no-authority envelope beside recovery prompt-preflight, autonomy-policy, structured progress, and output-compaction receipts, so recovery/admin toggles cannot treat stored generated content, operator reasons, or caller-forged memory authority as trusted instructions or gate bypasses.
- Session recovery repair-safe responses now include a server-generated Memory Tree advisory/no-authority envelope beside required prompt-preflight, autonomy-policy, structured progress, and output-compaction receipts, so recovered session context remains metadata-only and cannot treat local session state or caller-forged memory authority as trusted instructions or gate bypasses.
- Whole-Space and single-widget revision restore responses now include server-generated Memory Tree advisory/no-authority envelopes beside recovery prompt-preflight, autonomy-policy, structured progress, and output-compaction receipts, so rollback/time-travel context remains metadata-only and cannot bypass prompt-preflight, approval, sandbox preview, visual QA, or recovery gates.
- Revision history/recovery list tool responses (`space.revisions`, `space.revision.list`, `space.history`, and current-space aliases) and the HTTP `/api/spaces/revisions` route now include server-generated Memory Tree advisory/no-authority envelopes beside required recovery prompt-preflight, autonomy-policy, structured progress, and output-compaction receipts, so revision timeline context stays untrusted and caller-forged memory authority cannot bypass recovery gates.
- Direct Spaces GET/read HTTP routes (`/api/spaces`, `/api/spaces/current`, `/api/spaces/get`, `/api/spaces/widgets`, and `/api/spaces/widget`) now reuse the same receipt-bearing Space/widget read adapters, preserving sanitized list/current/detail/widget payloads while adding metadata-only prompt-preflight, autonomy-policy, progress, Memory Tree advisory/no-authority, and output-compaction receipts without leaking renderer/source/html/script/API-auth/raw-prompt/secret fixture values.
- Direct active-space session context switches (`/api/spaces/activate` and `/api/spaces/deactivate`) now include server-generated Memory Tree advisory/no-authority envelopes beside required active-space-switch prompt-preflight, `space.activate` / `space.deactivate` autonomy-policy, paired metadata-only lifecycle progress, and output-compaction receipts, so choosing or clearing the Space whose context may influence a session remains advisory-only and cannot leak caller/session-forged renderer/source/html/script/API-auth/raw-prompt/generated-body/secret fixture values or bypass creator, approval, sandbox, visual-QA, or rollback gates.
- High-risk template install/reset responses now include server-generated Memory Tree advisory/no-authority envelopes beside prompt-preflight, autonomy-policy, structured progress when present, and output-compaction receipts, so template setup/reset context cannot treat caller-forged memory authority as trusted instructions or gate bypasses.
- Source-style Space duplicate/clone responses (`space.spaces.duplicateSpace` / `space.spaces.cloneSpace`) now include server-generated Memory Tree advisory/no-authority envelopes beside prompt-preflight, autonomy-policy, structured progress, and output-compaction receipts, so copied Space context and caller-forged memory authority cannot become trusted instructions or bypass creator/approval/sandbox/visual-QA/rollback gates.

Remaining:
- Extend the same preflighted advisory-memory envelope to remaining source/tool/browser boundaries where memory can influence actions.

### Phase 5 — Autonomy policy, prompt-injection preflight, model-routing hints

Status: **metadata-only status surface plus creator preview/commit, active-space instruction/context, repair prompt, route-level source-refresh preflight, camera-stream tool receipts, package policy/import-export receipts including no-prompt import required preflight, logical path-helper policy receipts, widget reload/refresh required-preflight receipts, safe model-route resolution receipts, and initial actual model-route invocation for safe summary workloads implemented; broader per-action enforcement and model invocation routing remain**.

Delivered:
- `api/capy_policy.py` exposes allow-listed autonomy modes, approval gates, prompt-preflight status, model-routing hints, and bounded/deduplicated action-policy receipts without echoing raw env/provider/model secrets.
- `static/spaces.js` renders the product-home Autonomy policy card from `api/capy-policy/status`.
- Native `update_space(...)` / `/api/spaces/update` active-instruction writes now run `active_space_instructions` prompt preflight before persistence, fail hostile instruction updates closed without revisioning or raw-prompt leakage, preserve safe instruction text, and can return optional metadata-only prompt-preflight/action-policy/progress receipts when `includeSafetyReceipts` is requested.
- The same direct `/api/spaces/update` active-instruction safety-receipt path now emits a server-generated Memory Tree advisory/no-authority envelope (`untrusted_advisory`, gate-bypass false, required prompt-preflight/approval/sandbox-preview/visual-QA/rollback gates) and threads safe advisory evidence into output compaction while ignoring forged caller `memory_advisory` authority and omitting raw instruction/source/html/renderer/API-auth/secret fixture values.
- Creator preview responses now include a metadata-only `autonomy_policy` receipt with approval gates, prompt-preflight status, and model-route hint evidence, and the creator-preview UI renders this Action policy card without exposing raw prompts, generated bodies, renderer/source fields, API-auth fields, or credentials.
- Source-style layout repair (`space.spaces.repairLayout`) now returns metadata-only prompt-preflight and action-policy evidence beside its structured repair progress receipt, so automated repair boundaries expose approval/preflight/model-route safety metadata without raw layout/source/renderer leakage.
- Source-style first-fit layout helpers (`space.spaces.buildCenteredFirstFitLayout` and `space.spaces.findFirstFitWidgetPlacement`) now return metadata-only prompt-preflight, autonomy-policy, structured progress, and output-compaction receipts beside their sanitized placement results, so untrusted canvas-planning payloads expose visible trust evidence without leaking renderer/source/API-auth/script/secret markers.
- Creator commit responses now carry forward the preview prompt-preflight receipt and add a commit-scoped `autonomy_policy` receipt after the sandbox preview, visual-QA, and explicit approval gates pass, giving the revisioned-commit result metadata-only policy evidence without persisting raw prompts, generated bodies, renderer/source fields, API-auth fields, credentials, or unsafe screenshot paths.
- Direct active-space instruction aliases (`space.current.agentInstructions` / `space.current.specialInstructions`) now run `active_space_instructions` prompt preflight, return metadata-only action-policy receipts, server-generated Memory Tree advisory/no-authority envelopes, structured `instructions:<space_id>` progress, and output-compaction evidence, and withhold hostile instruction text before it can be injected into agent context.
- Source-style Space creation (`space.create` / `space.spaces.create`) and direct `/api/spaces/create` safety-receipt responses now run `active_space_instructions` prompt preflight before persisting supplied instructions, block hostile create-time instruction injection before manifest creation, return server-generated Memory Tree advisory/no-authority envelopes (`untrusted_advisory`, gate-bypass false, required prompt-preflight/approval/sandbox-preview/visual-QA/rollback gates), and thread that advisory evidence into metadata-only compaction without echoing raw instruction text or caller-forged memory authority.
- Source-style logical app URL resolution (`space.spaces.resolveAppUrl`) now returns a server-generated Memory Tree advisory/no-authority envelope beside metadata-only prompt-preflight, action-policy, structured progress, and output-compaction receipts, so browser/app navigation helpers expose trust metadata while caller-forged memory authority, raw memory context, unsafe paths, renderer/source/API-auth fields, scripts, and secret-looking fixtures stay omitted.
- Browser-surface tool actions now run `browser_surface` prompt-preflight classification over an internal-only, bounded corpus of high-risk browser payload fields for receipt-only `open`, `snapshot`, `click_ref`, `type_ref`, `back`, `forward`, `press`/`key`, and `scroll` adapters; hostile prompt/text/URL/DOM/source/renderer/API-auth/token/ref/history payloads, including nested object values and camelCase aliases, block with metadata-only categories, oversized or deeply nested high-risk payloads fail closed, browser execution remains disabled, and raw URL/history/key/ref/text/source/renderer/API-auth payloads are not stored or echoed.
- Receipt-only development terminal/shell tool actions now run `development_tool` prompt preflight over common command/args/message/auth payload shapes, thread pass/block status into autonomy-policy receipts, return a generated Memory Tree advisory/no-authority envelope that cannot be overridden by payload-supplied memory authority, and return metadata-only progress/output-compaction evidence without executing commands, enabling filesystem writes, or storing raw command/prompt/source/html/renderer/API-auth payloads. Development preflight now scans full bounded high-risk payload corpora rather than only first/last list samples, covers compact/camelCase raw prompt and API-key aliases, and fails closed on oversized sparse payloads so hostile instructions cannot hide in large development-tool requests.
- Source-style Space open/reload navigation helpers (`space.spaces.open` / `openSpace` and `space.spaces.reloadCurrentSpace` / `reloadSpace`) now return metadata-only required prompt-preflight receipts, action-policy receipts, `space.open:<space_id>` / `space.reload:<space_id>` progress receipts, and server-generated Memory Tree advisory/no-authority envelopes while ignoring forged caller memory authority and keeping `get`/`read` aliases read-only.
- Local Service Dashboard template installs now return metadata-only `local_service_template` prompt-preflight and `space.template.install.local_service` autonomy-policy receipts for direct, route, and tool-adapter boundaries, keeping service connector/browser-surface setup redacted; high-risk template installs for Browser Surface, Camera Dashboard, Local Service Dashboard, Model Provider Setup, Game Sandbox, and Music Sequencer plus Big Bang template resets now also include server-generated Memory Tree advisory/no-authority envelopes and metadata-only `output_compaction` receipts that cite only template/space/widget-count/preflight/policy/progress/advisory metadata, and the template-install UI renders Local Service / Model Provider compaction evidence without leaking hostile fixture fields.
- Source-style current-space viewport/reposition helpers (`space.spaces.repositionCurrentSpace`, `space.current.reposition`, and `space.current.reposition_viewport`) now return metadata-only prompt-preflight, action-policy, structured progress, server-generated Memory Tree advisory/no-authority, and output-compaction receipts with safe `layout.reposition:<space_id>` run ids, so browser/canvas viewport control exposes visible trust evidence without executing browser movement or leaking renderer/source/API-auth/script/secret request markers, top-level or nested viewport raw memory context, or caller-forged memory authority.
- Source-style logical storage path helpers (`space.spaces.buildSpace*Path`) now return metadata-only prompt-preflight, action-policy, progress, output-compaction, and server-generated Memory Tree advisory/no-authority receipts with destructive-external-action approval and `hint:fast` route evidence, so development/path-boundary helpers expose visible trust metadata without echoing hostile renderer/source/API-auth/script/secret or forged memory-authority request markers.
- Space metadata mutation aliases (`space.spaces.saveSpaceMeta` / `space.current.saveMeta`) now preflight `agentInstructions`/`specialInstructions` before persistence, block hostile instruction writes without creating revisions, and return metadata-only action-policy, progress, Memory Tree advisory/no-authority, and output-compaction receipts for passing instruction updates.
- Source-style layout mutation aliases (`space.spaces.saveSpaceLayout` / `space.current.saveLayout` and `space.spaces.rearrangeWidgets`) now preflight raw prompt-bearing payload fields before persistence/mutation, block hostile prompt injection before revisioning widget layouts, and keep public receipts/state metadata-only. Save-layout aliases now also return Memory Tree advisory/no-authority and output-compaction evidence so layout context cannot bypass prompt-preflight/approval/sandbox/visual-QA/rollback gates.
- Widget mutation receipts now preserve required Memory Tree gates in bounded output-compaction text before high-volume revision ids, and `space.spaces.toggleWidgets` / `space.current.toggleWidgets` now return metadata-only required prompt-preflight receipts beside existing action-policy and progress evidence.
- Camera-stream tool actions (`space.camera.add_stream` / `camera.add_stream`) now return metadata-only required prompt-preflight receipts, autonomy-policy receipts with the `destructive_external_action` gate and `hint:vision` model-route hint, and structured `camera.stream.add:<space_id>` progress receipts, while approved stream references remain url-digest/host-class metadata only.
- Shared data slot delete tool actions (`space.data.delete` / `space.current.data.delete`) and the direct `/api/spaces/data/delete` WebUI route now return/render top-level metadata-only required prompt-preflight receipts plus `space.shared_slot.delete` autonomy-policy receipts with creator-commit approval, `hint:summarize` route evidence, structured progress, and compaction evidence while preserving redacted telemetry.
- Space Agent package export responses now include a top-level metadata-only required `prompt_preflight` receipt plus a `space.agent.export` autonomy-policy receipt with supervised-mode approval gates, required prompt-preflight status, and `hint:reasoning` model-route evidence; the export UI renders preflight, policy, package progress, and compaction evidence without displaying package YAML, widget bodies, renderer/source/API-auth fields, prompts, scripts, archives, or credentials.
- Space Agent package import tool aliases now return per-invocation metadata-only autonomy-policy receipts, so `space.import`, `space.package.import`, and `space.agent.import` can be distinguished in product/tool evidence while preserving prompt-preflight status, approval gates, model-route hints, and import quarantine redaction.
- Space Agent package imports with no active instructions now return a top-level metadata-only required `prompt_preflight` receipt for the `space_agent_package_import` boundary, and propagate `prompt_preflight_status: required` through autonomy-policy and output-compaction evidence for both direct imports and `space.import` tool aliases while preserving server-generated Memory Tree advisory/no-authority and generated-body/API-auth redaction.
- Action-policy receipts now include a metadata-only `model_route_resolution` decision derived from safe configured route fields or deterministic default fallbacks, and Spaces UI receipts render the selected route plus fallback reason without exposing raw provider config, API-auth fields, renderer/source/script markers, prompts, or credentials.
- Active-space context receipts, repair prompts and empty-prompt whole-Space/widget/module repair queues, whole-Space and recovery-module repair-event list responses, source-style Space revision/history list responses, widget-event list responses, widget-runtime prompts (including direct sandbox queue-status UI receipts), widget detail/see helper receipts, widget-runtime contract helper receipts, fixed widget reload/refresh events without free-form prompts, source-style layout repair, source-style widget definition/blueprint preview/source helpers, and source-refresh ingestion paths now use protected prompt-preflight boundaries before advisory context or user-provided instructions can influence an agent/tool action. Queued widget reload/refresh events also carry the server-generated Memory Tree advisory/no-authority envelope in immediate and listed receipts, and thread that boundary into compaction evidence so caller-forged memory authority cannot bypass gates.
- Recovery-module repair queue responses now also carry and persist the server-generated Memory Tree advisory/no-authority envelope in immediate and listed receipts, thread the no-bypass gate boundary into repair output-compaction evidence, and redact raw operator-note payload fields/values plus caller-forged memory authority so recovery repair context cannot understate prompt-preflight, approval, sandbox-preview, visual-QA, or rollback gates.
- Whole-Space repair queue responses and repair-event list tool aliases now carry, persist, and replay server-generated Memory Tree advisory/no-authority receipts in immediate/listed results and output-compaction evidence, while omitting caller-forged memory authority, benign/raw memory-context payload fields, raw prompts, renderer/source/html/script/data/API-auth fields, credentials, and secret-looking fixtures.
- Recovery-module repair event-list tool responses now include their own top-level server-generated Memory Tree advisory/no-authority receipt and thread the same no-bypass gate boundary into list output-compaction evidence, so reading recovery-module repair history cannot rely on caller/event-forged `trusted_system_memory`, `context_authority`, bypass flags, or raw operator-note payloads to understate required prompt-preflight, approval, sandbox-preview, visual-QA, or rollback gates.
- Manual and scheduled Memory Tree source-refresh route responses now include a top-level metadata-only `capy_memory_source_refresh` prompt-preflight receipt in addition to per-job source preflight rows, and product-home source-refresh result cards render that route-level receipt beside action-policy, progress, Memory advisory, and output-compaction evidence. Aggregate pass/warn/block/required status is deterministic and stays aligned with autonomy-policy and compaction evidence while job/result-forged prompt hashes, raw prompts, renderer/source/API-auth fields, credentials, tokens, and secret-looking fixtures remain omitted.
- Source-refresh ingest can now use the configured safe `hint:summarize` route for actual model-backed metadata-only source summaries after auto-fetched-source prompt preflight passes; missing/unsafe/unconfigured/unavailable routes still fall back to deterministic sanitized source records, custom providers can resolve through their configured connection when generic runtime lookup is unavailable, and public receipts/prompts omit raw renderer/source/API-auth fields, prompts, scripts, and credentials.
- The trusted system-widget add flow now renders backend-produced action-policy, system-widget progress, and output-compaction receipts in `static/spaces.js` after the widget manager refresh, keeping first-party panel attachment visibly metadata-only without leaking hostile renderer/script/API-auth/raw-prompt/secret fixture fields.
- Confirmed Space deletion now renders the backend-produced prompt-preflight, autonomy-policy, `space.delete:<space_id>` progress, Memory Tree advisory/no-authority, and output-compaction receipts after the Spaces home refresh, so destructive Space removal has visible metadata-only trust evidence without leaking raw prompts, renderer/source/API-auth fields, scripts, secrets, unsafe paths, memory context, or generated/widget bodies.
- Confirmed widget deletion now renders the backend-produced prompt-preflight, autonomy-policy, `widget.delete:<space_id>` progress, and output-compaction receipts after the widget manager refresh, so destructive widget removal has visible metadata-only trust evidence without leaking raw prompts, renderer/source/API-auth fields, scripts, secrets, or deleted widget bodies.
- The no-space Source-style `space.spaces.widgetApiVersion` runtime compatibility helper now returns metadata-only prompt-preflight, autonomy-policy, server-generated Memory Tree advisory/no-authority, neutral no-space progress, and compaction evidence while preserving its functional widget API version response and omitting renderer/source/API-auth/raw-prompt/script/secret request markers.
- Source-style Capy Spaces health helpers (`space.api.health` and `space.health`) now return metadata-only prompt-preflight and autonomy-policy receipts with neutral no-space progress/compaction evidence while preserving the functional health payload and omitting renderer/source/API-auth/raw-prompt/script/secret request markers.
- Source-style current-space id lookup (`space.spaces.currentId`) now returns metadata-only prompt-preflight, autonomy-policy, server-generated Memory Tree advisory/no-authority, and action-scoped progress/compaction receipts while preserving its functional current-id payload and omitting renderer/source/API-auth/raw-prompt/script/secret request markers.
- Source-style Space collection/current read helpers (`space.list`, `space.spaces.listSpaces`, `space.spaces.items`, `space.spaces.all`, `space.spaces.byId`, `space.current.get`, and `space.spaces.getCurrentSpace`) now return metadata-only required prompt-preflight and autonomy-policy receipts while preserving sanitized Space list/current payloads, using `space.collection:list`, `space.current.read:<space_id>`, or no-current `space.current.read:none` progress ids, and omitting renderer/source/API-auth/raw-prompt/script/secret request markers.
- Source-style Space duplicate/clone helper responses (`space.spaces.duplicateSpace` and `space.spaces.cloneSpace`) now return server-generated Memory Tree advisory/no-authority envelopes beside existing active-instruction prompt-preflight, autonomy-policy, structured progress, and compaction evidence while ignoring caller-forged trusted memory authority and omitting raw source/renderer/API-auth/script/secret request markers.
- Direct create-from-session responses now return a metadata-only required `create_from_session` prompt-preflight receipt, `space.create_from_session` autonomy-policy receipt, server-generated Memory Tree advisory/no-authority envelope, safe `space.create_from_session:<space_id>` progress receipt, and output-compaction evidence, while using a minimal allow-listed session receipt and sanitized title/widget-count metadata so chat messages, pending prompts, composer drafts, compression summaries, whitespace API-key/API-auth title markers, renderer/source/html/script/API-auth fields, caller-forged memory authority, generated widget bodies, and secrets stay out of route responses and the UI receipt.
- Direct active-space activate/deactivate route responses now return metadata-only required `active_space_switch` preflight receipts and `space.activate` / `space.deactivate` autonomy-policy receipts beside sanitized session receipts, so switching session context exposes the same visible policy boundary as other context-affecting actions while omitting request/session prompts, drafts, renderer/source/API-auth fields, generated bodies, and secrets.

Remaining:
- Extend pass/warn/block preflight and action-policy receipts to additional high-risk recovery/tool boundaries beyond the current creator/context/repair/repair-list/widget-refresh/source-refresh/browser/development/camera/package-export coverage.
- Wire model-routing resolution into actual Capy/Hermes invocation selection while preserving Brendan's provider-agnostic OpenAI/xAI/LM Studio setup.

### Phase 6 — Structured progress events

Status: **recorder/status/product-home and Space-detail cards implemented; broader producer coverage remains**.

Delivered:
- `api/capy_progress.py` records and summarizes bounded metadata-only progress events.
- The structured progress taxonomy now accepts the full roadmap's non-content delta/event types (`thinking.delta`, `text.delta`, `tool.args.delta`, `subagent.spawned`, and `subagent.progress`) as metadata-only events, with product-home/status family chips for thinking/text streams and no persistence of raw prompt/text/tool arguments.
- `static/spaces.js` renders product-home Progress events stats, family-count chips, recent event types, timestamps, recent stream rows, and queued widget-event prompt-preflight/compaction evidence without raw payload/prompt leakage; backend local widget runtime no-ops now return metadata-only progress/compaction receipts for receipt-aware clients without persisting widget events.
- Space detail now loads `/api/capy-progress/status?space_id=...` and renders a Space-scoped progress card next to Memory Tree context/revision history, showing bounded active-run/recent-event metadata without unsafe event families, generated bodies, renderer/source fields, raw prompts, or credentials.
- Recent progress family counts cover conservative event families such as `run`, `thinking`, `text`, `tool`, `subagent`, `taskboard`, `memory.ingest`, and `space.visual_qa`.
- Research Harness progress updates now emit metadata-only `taskboard.updated` structured progress events so product-home progress status can reflect real research taskboard/workflow activity without storing raw prompts, source text, renderer fields, or credentials.
- Creator-loop commits that pass sandbox preview, visual QA, and explicit approval now emit `space.visual_qa.completed` progress events and render the creator visual-QA progress receipt in the commit success card, giving the product-home stream and creator receipt real visual-QA gate coverage without storing prompts, generated bodies, renderers, sources, or credentials.
- Source refresh workers now emit metadata-only `memory.ingest.started` / `memory.ingest.completed` / `memory.ingest.failed` progress events from opaque source/job run ids, so Memory Tree ingest activity appears in the product progress stream without storing origin URLs, raw summaries, prompts, renderer fields, exception text, or credentials.
- The metadata-only Space demo smoke suite now emits bounded `run.started` / `run.completed` progress events under a fixed safe run id, so demo-suite compaction/context evidence also appears in the product progress stream without storing raw demo output, prompts, widget bodies, renderers, sources, or credentials.
- Individual Browser Surface demo smoke runs now emit metadata-only `run.started` / `run.completed` / `run.failed` progress events under safe `space-demo:<demo>` run ids, so the product progress stream reflects targeted browser parity smokes without storing raw browser output, prompts, widget bodies, renderer/source fields, exception text, or credentials.
- Source-style `space.spaces.repairLayout` now emits paired metadata-only `tool.started` and `tool.completed` lifecycle progress events with the same safe `repair:<space_id>` run id, preserves the backward-compatible completed `progress_event`, and threads `progress_event_types: tool.started, tool.completed` into compaction evidence without exposing renderer/source/API-auth fields, prompts, script markers, or exception text.
- Creator-loop sandbox previews now emit metadata-only `tool.completed` progress events keyed by their opaque preview receipt ids, so the product progress stream reflects creator preview activity before commit without storing raw prompts, generated widget bodies, renderer/source fields, API-auth data, script markers, or credentials.
- Shared data slot set/read/list/delete tool actions now emit metadata-only `tool.completed` progress events using safe `shared-slot.*` run ids, so Space cooperation/data handoff activity appears in Space-scoped progress without exposing slot values, renderer/source fields, API-auth fields, prompts, or credentials.
- Source-style widget upsert, renderWidget, defineWidget, and widget-blueprint preview/source tool actions plus widget-runtime contract helper inspections now emit metadata-only `tool.completed` progress events with safe `widget.upsert:<space_id>`, `widget.render:<space_id>`, `widget.blueprint.define:<space_id>`, `widget.blueprint.create:<space_id>`, `widget.blueprint.preview:<space_id>`, and `runtime-contract:<space_id>` run ids, so agent-driven Space construction, generated-widget quarantine/preview/render boundaries, and sandbox-contract inspection appear in Space-scoped progress without storing widget bodies, renderer/source/API-auth fields, prompts, scripts, or credentials.
- Source-style layout rearrange/toggle and first-fit placement actions now emit metadata-only `tool.completed` progress events with safe `layout.rearrange:<space_id>`, `layout.toggle:<space_id>`, `layout.first_fit:<space_id>`, and `layout.first_fit.placement:<space_id>` run ids, so Space-scoped progress reflects agent-driven layout planning/mutations without storing renderer/html/source/API-auth fields, prompts, scripts, or credentials.
- Source-style current-space viewport/reposition actions now emit metadata-only `tool.completed` progress events with safe `layout.reposition:<space_id>` run ids, so Space-scoped progress reflects browser/canvas viewport requests without storing raw viewport source fields, renderer/html/API-auth markers, scripts, prompts, or credentials.
- Source-style Space open/reload navigation helpers now emit metadata-only `tool.completed` progress events with safe `space.open:<space_id>` and `space.reload:<space_id>` run ids, so browser/canvas navigation requests appear in scoped progress without exposing hostile renderer/source/html/API-auth/script/token request markers.
- Recovery widget quarantine/enable tool actions and recovery-module disable/enable toggles now emit metadata-only `tool.completed` progress events with safe `recovery.widget.*:<space_id>` and `recovery.module.*:recovery-modules` run ids, so Space/recovery-scoped progress reflects recovery admin toggles without exposing disabled widget bodies, module bodies, renderer/source fields, prompts, scripts, or credentials.
- Space Agent package import/export boundaries now emit metadata-only `tool.completed` progress events with safe `package.import:<space_id>` and `package.export:<space_id>` run ids after successful sanitized package operations, without storing package YAML, widget bodies, renderer/source/API-auth fields, prompts, scripts, or credentials; unsupported export formats fail before recording false completion telemetry.
- Recovery revision restores now return metadata-only autonomy-policy evidence and `tool.completed` progress receipts with `recovery.restore:<space_id>` run ids, so rollback/time-travel actions surface in Space-scoped progress without exposing stored renderer/source/API-auth fields, prompts, scripts, disabled widget bodies, or secret-looking fixture markers.
- Space checkpoint anchors now return metadata-only prompt-preflight and autonomy-policy evidence, `tool.completed` progress receipts with safe `checkpoint:<space_id>` run ids, server-generated Memory Tree advisory/no-authority envelopes, and output-compaction receipts beside the revision event id, and the Space detail checkpoint receipt now renders the returned Action policy, Checkpoint progress, Memory advisory, and compaction evidence without rendering generated widget bodies or exposing hostile reason text, renderer/source/API-auth fields, prompts, scripts, caller-forged memory authority, or secret-looking fixture markers.
- Source-style Space duplicate/clone and deletion/removal aliases now return and persist paired metadata-only `tool.started` and `tool.completed` lifecycle progress receipts with stable `space.duplicate:<target_space_id>` / `space.delete:<source_space_id>` run ids, preserve the backward-compatible completed `progress_event`, and thread `progress_event_types: tool.started, tool.completed` into output-compaction evidence after validated successful sanitized duplication/deletion without exposing hostile renderer/source/html/API-auth fields, prompts, scripts, tokens, or secret-looking fixture markers.
- Direct/source-style Space creation safety receipts and create-from-session responses now return and persist paired metadata-only `tool.started` and `tool.completed` lifecycle progress receipts with stable `space.create:<space_id>` / `space.create_from_session:<space_id>` run ids, preserve the completed event as `progress_event`, and thread `progress_event_types: tool.started, tool.completed` into output-compaction evidence while rejected hostile or duplicate create attempts avoid started-row telemetry and chat/prompt/widget-body content stays omitted.
- Direct Space update active-instruction safety receipts now return and persist paired metadata-only `tool.started` and `tool.completed` lifecycle progress receipts with stable `space.update:<space_id>` run ids after prompt preflight passes, preserve the completed event as `progress_event`, and thread `progress_event_types: tool.started, tool.completed` into output-compaction evidence while blocked hostile instruction updates avoid started-row telemetry and raw prompt/renderer/source/API-auth/forged memory-authority fixture content stays omitted.
- Direct active-space activate/deactivate routes now return and persist paired metadata-only `tool.started` and `tool.completed` lifecycle progress receipts with stable `space.activate:<space_id>` / `space.deactivate:<space_id>` run ids, or neutral `space.deactivate:session` when clearing an empty context, preserving the completed event as `progress_event` and threading `progress_event_types: tool.started, tool.completed` into output-compaction evidence while alias conflicts and missing targets avoid started-row telemetry.
- Source-style logical storage path helpers now emit metadata-only `tool.completed` progress receipts with safe `path.helper:<space_id>` run ids, so development/path-boundary helper calls appear in the product progress stream without storing raw renderer/source/html/API-auth/script/token/secret request markers.
- Individual Space demo smoke results now include product-visible metadata-only `space.demo.run.<demo>` autonomy-policy receipts, so rollback/recovery/browser/demo walkthrough cards expose approval gates, required prompt-preflight status, and `hint:reasoning` route evidence beside compaction/context evidence without rendering prompts, widget bodies, renderer/source/API-auth fields, scripts, tokens, or secret-looking fixture markers.
- Receipt-only development terminal/shell actions now emit metadata-only `tool.started` and `tool.completed` lifecycle receipts with the same safe `development.terminal:<space_id>` run id, preserving the backward-compatible `progress_event` completion receipt so high-risk development-tool requests appear in Space-scoped progress without executing commands or storing raw command text, prompts, renderer/source/html/API-auth fields, scripts, paths, bearer strings, or credentials.
- WebUI streaming tool lifecycle callbacks now emit metadata-only `tool.started`, `tool.completed`, and `tool.failed` progress events under safe stream-scoped run ids, including the newer structured AIAgent `tool_start_callback` / `tool_complete_callback` path. Legacy/structured duplicate callbacks count once, legacy failure status is preserved as `tool.failed`, terminal tool callbacks return and stream metadata-only `output_compaction` evidence that preserves numeric exit status, and path/URL-shaped hostile stream ids fall back to `webui.tool:stream` without persisting tool names, args, previews, outputs, prompts, renderer/source fields, scripts, paths, or credentials.
- WebUI streaming runs now emit metadata-only `run.started`, `run.completed`, and `run.failed` lifecycle events under safe `webui.run:<stream>` run ids, so product progress cards can show active/completed browser-originated agent turns without storing user prompts, assistant text, tool payloads, provider credentials, renderer/source fields, scripts, or secret-looking values.
- WebUI streaming subagent lifecycle/progress callbacks now emit metadata-only `subagent.started`, `subagent.spawned`, `subagent.progress`, `subagent.completed`, and `subagent.failed` progress events under safe stream-scoped run ids, including legacy/current Hermes delegate callback names (`subagent.start`, `subagent.spawn_requested`, `subagent_progress`, `subagent.complete`, `subagent.tool`, and `subagent.thinking`); terminal subagent callbacks now return and stream metadata-only `output_compaction` evidence without persisting subagent goals, previews, tool args, summaries, prompts, renderer/source fields, scripts, paths, or credentials.
- Scheduled Memory Tree source-refresh ticks and manual Memory Tree source-refresh route actions now emit and return paired metadata-only `run.started` and `run.completed` lifecycle progress receipts under fixed `source-refresh.scheduled` / `source-refresh.manual` run ids after queue/run completion, preserve the backward-compatible completed `progress_event`, and thread `progress_event_types: run.started, run.completed` into output-compaction evidence. Autonomous and operator-triggered refresh activity is visible in the product progress stream without storing source URLs, job payloads, raw prompts, renderer/source fields, scripts, credentials, raw memory context, or caller/job/result-forged trusted memory authority; partial progress-recorder failures close active runs, safe legacy completed receipts remain accepted, and manual refresh job responses/connector cards omit raw source URLs while preserving safe source ids/status/freshness and refresh controls.
- Widget-event list tool responses and the browser-facing `GET /api/spaces/widget/events` route now include top-level required prompt-preflight, autonomy-policy, server-generated Memory Tree advisory/no-authority, and `tool.completed` progress receipts with safe `widget.events:<space_id>` run ids in addition to regenerated metadata-only compaction evidence, so inbox reads expose the same widget-runtime trust envelope and appear in Space-scoped progress without echoing forged event prompts, renderer/source/API-auth fields, scripts, tokens, or secret-looking fixture values; the widget manager renders this route-level receipt above queued events.
- Queued widget-event ingress receipts now mark `output_compaction` as metadata-only and thread safe `widget-event:<event_id>` progress run ids/status into regenerated compaction evidence for both immediate responses and listed persisted summaries, without exposing raw prompts, payload text, renderer/source/API-auth fields, scripts, tokens, or secret-looking fixture values.
- Widget detail/see helper responses now include top-level required prompt-preflight, autonomy-policy, and `tool.completed` progress receipts with safe `widget.see:<space_id>` run ids in addition to metadata-only compaction evidence, so composite widget/runtime/event inspections expose the same widget-runtime trust envelope without leaking renderer/source/API-auth fields, scripts, prompts, or credentials.
- Source-style, current-space, and legacy generic widget read/get helper responses now include top-level required prompt-preflight, autonomy-policy, and `tool.completed` progress receipts with safe `widget.read:<space_id>` run ids in addition to metadata-only compaction evidence after the target widget exists, and widget list/detail prompt metadata now uses the same allow-listed metadata-only summarizer, so widget metadata reads expose the generated-widget-execution trust envelope without leaking renderer/source/API-auth fields, raw prompt text, nested prompt metadata, scripts, revision escape markers, or credentials and missing-widget reads do not emit false completion telemetry.
- Widget collection/list aliases (`space.current.widgets`, `space.current.widget.list`, `space.current.listWidgets`, `space.current.byId`, `space.current.widgetsById`, `space.spaces.listWidgets`, `space.spaces.widgets`, `space.widget.list`, `space.widgets.list`, `space.current.widgets.list`, and `widget.list`) now share the same metadata-only widget-runtime receipt envelope as widget reads, including required preflight, generated-widget-execution policy, `widget.read:<space_id>` progress, and compaction evidence while preserving sanitized widget summaries/maps only.
- Recovery-module and whole-Space repair-event list responses plus revision-history/recovery list tool responses now emit metadata-only `tool.completed` progress receipts with safe `recovery.module.repair_events:recovery-modules`, `recovery.space.repair_events:<space_id>`, or `recovery.revision.list:<space_id>` run ids and thread those run ids into compaction evidence beside prompt-preflight/action-policy status, without exposing module source, renderer, prompt text, session ids, bearer strings, or secret-looking fixture values.
- Source-style Capy Spaces health helpers (`space.api.health` and `space.health`) now emit paired metadata-only `tool.started` and `tool.completed` lifecycle receipts with the neutral `space.health:api` run id, preserve the backward-compatible completed `progress_event`, and thread the lifecycle event types into compaction evidence without a synthetic Space id, raw prompt, renderer/source/API-auth fields, scripts, tokens, or secret-looking fixture values.
- Receipt-only Browser Surface tool actions now emit paired metadata-only `tool.started` and `tool.completed` lifecycle events with the same safe `browser.<action>:<space_id>` run id, preserving the backward-compatible completed `progress_event` and threading the lifecycle event types into output-compaction evidence without executing browser actions or storing raw URLs, prompts, renderer/source/html/API-auth fields, caller-forged memory authority, credentials, tokens, or secret-looking values.

Remaining:
- Emit progress events from more real long-running browser, development, repair, and creator workflows.

### Phase 7 — Optional integration catalog/sidecar exploration

Status: **not started; hold until remaining Phase 1-6 integration work is proven end-to-end**.

Only after the remaining Phase 1-6 integration items are working:
- Evaluate connector catalog UX.
- Explore sidecar/native app integration only if it clearly improves Capy's existing self-hosted server product.

---

## Next implementation slices for autonomous sprints

Selector note (2026-07-06 04:56 CDT): the source-refresh relevant-memory evidence-alignment backlog is currently exhausted. The strict name/count lag scan, missing-assert scan, and no-relevant-call scan all returned `0` candidates against `tests/test_capy_memory_tree.py`, with the evidence-index count recipe still at 135 GitHub drift regressions, 129 before-body-read regressions, 129 relevant-memory-empty regressions, and 154 positive metadata-only GitHub ingestion regressions. Unless a future source-refresh route adds new evidence debt, scheduled sprints should move to the implementation priorities below instead of spending another cycle on name/count alignment.

1. **Remaining prompt-preflight + memory/context enforcement**
   - Extend pass/warn/block receipts from the implemented creator preview/commit, active-space instruction/context, source-style Space creation, repair prompt, widget-runtime prompt, source-style widget definition/blueprint preview/source helpers, source-refresh paths, receipt-only browser adapters, and receipt-only development terminal/shell adapters to remaining high-risk recovery/tool boundaries.
   - Keep Memory Tree content untrusted advisory context that cannot bypass creator gates, approval gates, sandbox preview, visual QA, or rollback/recovery controls; development terminal/shell receipts, receipt-only browser-surface tool receipts, source-style Space create receipts, direct Space update receipts, no-space widget SDK helper receipts, Space collection/current-read receipts, and Space open/reload navigation receipts now carry this no-authority envelope, so the next advisory-memory targets should be remaining source/tool boundaries that can consume memory context.

2. **Broader compaction producers**
   - Extend the implemented run-all, individual-demo, creator preview/commit, active-context, Research Harness progress/artifact, scoped-progress, receipt-only Browser Surface, receipt-only development terminal/shell, WebUI streaming tool/subagent terminal callbacks, high-risk template installs including Camera Dashboard, approved camera-stream, queued widget-event, shared data slot set/read/list/delete, source-style Space create/duplicate/delete, source-style Space metadata/layout/repair/rearrange/reposition, source-style widget upsert, source-style/current widget patch/delete/bulk-delete/toggle, source-style/current widget read/list/see, source-style Space read/get (`space.get`, `space.spaces.get`, `space.spaces.read`, `space.spaces.getSpace`, `space.spaces.readSpace`), source-style Space collection/current read, source-style Space open/reload navigation, logical app URL helper, Big Bang template reset, widget recovery toggle, and recovery-module toggle `output_compaction` receipt pattern to remaining long tool/subagent/recovery-output boundaries where it adds product value.
   - Preserve failures, approval prompts, and artifact handles/citations; never compact away safety-relevant evidence.

3. **Broader source-refresh scheduling/fetcher coverage**
   - Expand due-job scheduler/cron coverage and source-specific fetchers now that the safe metadata-only refresh worker and manual trigger path exist; keep raw fetched content out of public receipts/UI.

4. **Progress producer expansion**
   - Record structured events from browser/development/repair flows so the product-home and Space-detail streams reflect real autonomous work. Research Harness progress updates, creator visual-QA commit gates, Memory Tree source-refresh ingest workers, the demo smoke suite, individual browser demo smokes, source-style layout-repair actions, shared-data set/read/list/delete and widget-upsert actions, widget-event list reads, recovery widget toggles, receipt-only development terminal/shell actions, no-space Capy Spaces health helper lifecycle receipts, WebUI streaming run lifecycle events, WebUI streaming `tool.started`/`tool.completed`/`tool.failed` lifecycle callbacks, source-style Space open/reload navigation helpers, direct Space update lifecycle receipts, source-style widget defineWidget/upsert/renderWidget/blueprint preview actions, recovery widget enable/disable toggles, Space checkpoint receipts, and sanitized Space Agent package import/export operations now cover the first workflow/gate/ingest/run/browser/repair/cooperation/construction/recovery/development producers.

5. **Model-route invocation plumbing**
   - Reuse the metadata-only `model_route_resolution` receipts as the visible decision envelope, then continue wiring safe configured hints into actual Capy/Hermes invocation selection without exposing provider config or weakening Brendan's provider-agnostic OpenAI/xAI/LM Studio setup. Initial safe summary workloads (handoff/source-refresh summaries) now prove the pattern; next targets should stay internal and metadata-only before any creator/widget execution path.

---

## Current reusable components inventory

### Hermes core memory/session components

- `/Users/bschmidy10/.hermes/hermes-agent/agent/memory_manager.py`
  - Existing persistent memory injection/management surface.
  - Reuse conceptually for memory lifecycle boundaries; do not overload durable user/agent memory with bulky source records.
- `/Users/bschmidy10/.hermes/hermes-agent/tools/memory_tool.py`
  - User/agent memory CRUD tool; useful redaction/behavior reference for what should **not** become bulky task logs.
- `/Users/bschmidy10/.hermes/hermes-agent/tools/session_search_tool.py`
  - Existing searchable session-history recall tool; Memory Tree should complement this, not replace it.
- `/Users/bschmidy10/.hermes/hermes-agent/hermes_state.py`
  - SQLite session store and FTS search foundation.
- `/Users/bschmidy10/.hermes/hermes-agent/cron/scheduler.py`
  - Existing autonomous recurring execution; future auto-fetch/freshness jobs can integrate here or with WebUI-local jobs.
- `/Users/bschmidy10/.hermes/hermes-agent/plugins/memory/`
  - Optional external memory-provider plugins. Capy Memory Tree should remain local-first and provider-agnostic.

### Capy WebUI local knowledge surfaces

- `/Users/bschmidy10/hermes-webui/api/knowledge.py`
  - Local-only knowledge adapter with status/search/read/ask/capture note helpers, source redaction, and Obsidian URL support.
  - Strong candidate for integration or reuse when implementing Memory Tree search/read and freshness cards.
- `/Users/bschmidy10/hermes-webui/api/routes.py`
  - Existing routes: `/api/knowledge/status`, `/api/knowledge/search`, `/api/knowledge/read`, `/api/knowledge/ask`, `/api/memory`, and rollback/checkpoint routes.
  - Existing model/provider context handling around session load paths can inform model-routing hints.
- Local knowledge default paths from `api/knowledge.py`:
  - `~/.hermes/local-knowledge`
  - `~/.hermes/local-knowledge/index_config.json`
  - Obsidian vault default `~/Documents/Obsidian Vault`

### Capy Spaces context/revision/progress surfaces

- `/Users/bschmidy10/hermes-webui/api/spaces.py`
  - `build_agent_context(space_id)` already builds compact active-Space prompt context.
  - `queue_widget_event(...)` and `list_widget_events(...)` provide widget-event history.
  - `list_revision_events(...)`, restore helpers, checkpoint/recovery helpers, and creator preview/commit receipts provide revision/provenance anchors.
  - `set_research_progress(...)` already updates Research Harness progress metadata.
- `/Users/bschmidy10/hermes-webui/api/routes.py`
  - Existing Spaces routes include `/api/spaces/revisions`, `/api/spaces/widget/events`, creator/recovery/research routes, and local rollback endpoints.
- `/Users/bschmidy10/hermes-webui/static/spaces.js`
  - Existing rendering for creator previews, revision history, recovery/admin, widget events, smoke receipts, and Space detail canvas.
  - Future Memory Tree/freshness/relevant-memory cards should load the actual checked-out file in real-static tests and browser harnesses.
- `/Users/bschmidy10/hermes-webui/tests/test_spaces_foundation.py`
  - Main backend regression surface for Spaces metadata safety and route/tool contracts.
- `/Users/bschmidy10/hermes-webui/tests/test_spaces_ui_js_behaviour.py`
  - Main real-`static/spaces.js` fake-DOM regression surface for UI safety and behavior.

### Gaps to close / next integration targets

The first roadmap pass has now delivered the Memory Tree module, source/job schema, safe refresh worker, compaction helper, memory freshness card, relevant-memory UI, policy card, active-context advisory/preflight receipts, and product-home plus Space-detail progress cards. Current remaining work is narrower:

- **Automated artifact ingestion:** Core Space manifest/revision, rollback/restore anchor, repair, widget-event, and visual-QA production boundaries now auto-ingest metadata-only Memory Tree records; keep future artifact producers on the same canonicalizer path.
- **Local knowledge bridge:** Local knowledge now registers metadata-only Memory Tree source records with freshness/provenance rather than copying raw files wholesale into prompts; next work is safe refresh scheduling/fetcher breadth.
- **Advisory active context:** Active-Space context and creator preview now use cited, redacted Memory Tree snippets through metadata-only/preflighted envelopes; remaining source/tool/browser/development boundaries should use the same pattern before memory can influence actions.
- **Compaction execution path:** `api/capy_compaction.py` now backs run-all, individual-demo, creator preview/commit, active-context, Research Harness progress/artifact, scoped-progress, receipt-only Browser Surface, high-risk template installs, approved camera-stream, queued widget-event, source-style Space create/duplicate/delete, source-style Space metadata/layout/repair/rearrange/reposition, source-style widget upsert, source-style widget definition/blueprint preview/source/render, source-style/current widget patch/delete/bulk-delete/toggle, source-style/current widget read/list/see, legacy generic `widget.list`/`widget.read`/`widget.get`, source-style Space read/get (`space.get`, `space.spaces.get`, `space.spaces.read`, `space.spaces.getSpace`, `space.spaces.readSpace`), source-style Space collection/current read, widget-runtime contract helper inspections, source-style Space open/reload navigation, logical app URL helper, logical storage path helper, Big Bang template reset, whole-Space recovery toggle, widget recovery toggle, recovery-module toggle, and trusted session recovery repair-safe receipts; remaining long tool/subagent/browser/development/recovery outputs still need product-visible compaction integration where useful.
- **Source refresh worker:** Metadata-only `source.refresh` jobs can now be consumed safely for allow-listed HTML/plain/Markdown, RSS/Atom, JSON Feed, and GitHub issue/PR/repository/readme/contents/release/latest-release/release-list/single-release/release-assets/release-reactions/branch/branch-list/tag/labels/languages/topics/workflow/workflow-list/workflow-run/workflow-run-timing/workflow-jobs/actions-cache-usage/check-runs/check-run-annotations/commit-statuses/code-scanning-alert/code-scanning-alert-instances/issue-list/issue-event-list/PR-list/PR-requested-reviewers-list/PR-review-list/PR-commit-list/PR-file-list/issue-PR-comments/contributors/stargazers/subscribers/fork-list/commit/commit-list/participation-stats/traffic-views/traffic-clones/environments/collaborators API metadata, emitting progress events without raw bodies, README content/encoding/download URLs, issue bodies, event bodies, comment bodies/HTML/reactions/URLs/author association, commit-comment-reactions rows above 100, contributor avatar/user/profile URLs, commit-list avatar/html/API URLs, branch protection details, archive URLs, release notes/assets/download URLs, commit URLs/messages beyond the first safe commit-title line, PR filenames/patches/raw-content URLs, commit emails/signatures/file paths/patches, workflow paths/YAML, workflow-list URLs/jobs, workflow-run logs/jobs URLs, workflow-job steps/runner labels, check-run annotation paths/messages/raw details/blob URLs, code-scanning messages/paths/API URLs, Actions cache usage cache-version material, repository-language generic JSON feed fields, URLs, scripts, prompts, or credentials; remaining work is broadening production scheduler/cron coverage and additional source-specific fetchers around the existing manual trigger path.
- **Per-action policy/preflight:** Product-home policy visibility exists, and creator/context/source-style Space creation/repair/runtime/source-refresh/demo-smoke boundaries have initial pass/warn/block or required-preflight receipts; source-style open/reload navigation helpers, path helpers, camera/package/recovery/checkpoint boundaries, and remaining high-risk browser/development/recovery/tool boundaries need equivalent action-policy evidence as they are added.
- **Progress producers:** Structured progress recording/status/card exist, and Research Harness progress updates, creator visual-QA commits, Memory Tree source-refresh ingest, demo-suite run events, source-style layout/open/reload/repair events, shared-data/widget-upsert actions, recovery widget toggles, recovery-module toggles, and Space Agent package import/export operations now emit metadata-only events; more real long-running browser/development/repair workflows should emit events.

---

## Demo acceptance additions

A roadmap slice is not complete until the product can show bounded evidence, not just backend state:

- Memory freshness card: source counts, stale counts, last run status, and local-only indicator.
- Relevant memory panel: cited, redacted memory snippets attached to a Space or creator preview.
- Compaction evidence: receipt showing original size, compacted size, retained artifact handles/citations, and redaction status.
- Autonomy mode: visible mode label and explanation for the current action/Space.
- Prompt-injection preflight: visible pass/block/warn status for high-risk ingested content.
- Progress panel: structured tool/subagent/creator events with bounded metadata and no raw prompt or secret-like leakage.

---

## Validation defaults

- Plan-doc-only slices: `read_file`, `search_files`, and `git diff --check`; commit plan files only.
- Backend code slices: strict RED/GREEN pytest, `py_compile`, targeted full regression subset, `git diff --check`.
- Frontend-visible slices: real-`static/spaces.js` tests, `node --check static/spaces.js`, browser console/leak check, screenshot, and Visual/UI QA.
- Production WebUI changes: restart `com.capy.webui` and verify local/tailnet health after commit.
